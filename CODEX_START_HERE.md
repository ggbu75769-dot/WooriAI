# 우리아이 작업 시작점

## 1. 현재 작업부터 확인

우리아이는 **지출 기록 → 총액 확인 → 시기별 준비템 확인 → 구매 링크 클릭 → 구매 후 기록/상태 체크**를 잇는 앱이다. 사용자의 현재 요청을 수행하고 제품 범위·화면·기술 계약을 임의로 재해석하지 않는다.

- 저장소·브랜치·변경 파일·진행 중인 실행을 먼저 확인하고 기존 작업을 보존한다.
- [AGENTS.md](AGENTS.md), [CLAUDE.md](CLAUDE.md)와 현재 변경에 필요한 계약·구현·테스트만 읽는다. 문서 전체나 스킬 전체를 일괄 로드하지 않는다.
- 패키지 매니저·명령은 루트 `package.json`, Android 패키지명은 `apps/mobile/app.json`이 기준이다.
- 작업이 특정되지 않았을 때만 `docs/5차/`의 최신 관련 라운드와 [알려진 제한](docs/operations/known-limitations.md)을 확인한다. 과거 노트의 완료·차단 판단은 현재 소스와 실행 결과로 확인한다.
- 초기 Batch 00~11은 이미 완료됐다. 초기 구현 이력이 필요한 경우에만 `docs/4차/prompts/08_codex_iteration_prompts_v0_4.md`를 참고하고, 그 순서나 고정 보고 양식을 매 작업에 다시 적용하지 않는다.

## 2. 충돌 시 우선순위

1. [docs/dev/do-not-change.md](docs/dev/do-not-change.md) — DNC 계약의 현행 단일 소스(v0.5). 잠긴 동작을 변경해야 한다면 명시적인 변경 요청과 승인 계보를 확인한다.
2. `docs/4차/prompts/04_do_not_change_v0_4.md`, `docs/4차/contracts/do_not_change_contract_v0_4.yaml` — 승인 계보의 **원본 보존본**. 수정하지 않으며, 개정된 값이 현행 계약과 다르면 1번을 따른다.
3. 현재 범위의 승인된 수락 기준과 `docs/3차/db_api/`의 API·DB·구조 계약. 최신 변경 승인이 없는 한 제품 문서의 잠긴 범위를 유지한다.

## 3. 실행

- 이미 승인된 범위는 구현·검증·요청된 게시까지 진행한다. 통상적인 선택은 문맥으로 해결하고, 결과를 바꾸는 정보가 없을 때만 질문한다.
- 변경한 경계의 검사를 한 번 실행한다. 릴리스는 `pnpm release:gate`, API 동작 검증은 실제 PostgreSQL을 사용한다. 문서만 바뀌면 관련 문서 계약·경로·diff를 확인한다.
- 동일 소스·입력·환경·명령의 유효한 검증을 재사용한다. 실패 원인이나 변경 영향이 없으면 전체 빌드·브라우저·기기 검사를 반복하지 않는다.
- 빌드·배포·데이터 작업의 실행 소유자를 하나로 유지한다. 시간 초과만으로 새 실행을 띄우지 않고 기존 핸들의 상태를 확인한다.

## 4. 절대 변경 금지

[docs/dev/do-not-change.md](docs/dev/do-not-change.md)(v0.5)가 현행 단일 소스다. DNC 목록을 진입 문서에 복제하지 않는다. 금액·권한·삭제·제휴·승인 전 저장 금지·개인정보·토큰·의료 단정 금지 등 해당 계약 조항을 직접 확인한다. `docs/4차/**`의 승인 원본은 변경하지 않는다.

## 5. 완료

- 구현·로컬 검사·기기 증거·GitHub·운영·스토어 상태를 구분한다. 로컬 전용 요청에 새로운 운영·스토어 출시를 추가하지 않는다.
- 결과는 변경·검증·반영 여부·남은 조건만 간결하게 보고한다. 완료한 검사를 반복하거나 이미 끝난 Batch를 다시 시작하지 않는다.
