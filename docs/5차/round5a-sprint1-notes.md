# Round 5A Sprint 1 — 운영 노트

## 픽셀락 재측정 필요 (QA-101 대상)

- **ITEM-002 (아이템 상세, `/items/[itemTemplateId]`)**: COM-101로 "왜 필요해요?"(reasonText) / "이런 경우엔 안 사도 돼요"(skipReasonText) 카드 2개가 정보 카드와 제휴 고지 사이에 추가됨.
  - 소스 계약 테스트(`ui-pixel-lock-flow.test.ts`)는 영향 없이 통과하나, **기준 뷰포트(390×844) 캡처 기준 이미지는 재측정·재승인 필요**. 스크롤 콘텐츠 높이가 늘어나 고정 뷰포트 캡처에서 보이는 영역이 달라질 수 있음.
  - 원칙: 기준 이미지 강제 갱신 금지 — QA-101(Pixel Lock 9화면 재측정) 절차에서 승인 후 갱신.

## 후속 개선 후보 (이번 스프린트 범위 밖, 검수에서 식별)

- `IdempotencyInterceptor`가 body+path만 해시하고 query를 보지 않음 — DELETE에서 같은 멱등키+다른 `expectedVersion` 재사용 시 첫 응답 재생. 실사용 클라이언트는 mutation별 고유 키를 쓰므로 실질 위험 낮음.
- 모바일 멀티 디바이스 완전 델타 병합(커서 영속화 + 로컬 병합 파이프라인)은 축소 구현됨 — 현재는 재연결 시 outbox flush + sync/changes 1회 조회 + react-query invalidate. MOB-103 모바일 완성은 Sprint 2 후보.
- Admin MFA 잠금 카운터는 인메모리(기존 비밀번호 리미터와 동일 트레이드오프) — INF-005 Redis 공유 저장소 도입 시 함께 이전.
- `app/index.tsx` 온보딩 진행 조회에 타임아웃/로딩 UI 없음 — 서버 무응답 시 빈 화면. 후속 폴리시 필요.

## 이번 스프린트에서 대신 결정한 기본값 (09_결정필요 시트 갱신 필요)

- **D-09 Admin MFA 방식**: TOTP + 1회용 복구코드 10개 (해시 저장), 5회 실패 15분 잠금.
- **D-10 오프라인 충돌 UX**: 사용자 선택형 3분기 — 다른 기기 값 유지 / 내 변경 다시 적용 / 두 값 나란히 보기.
