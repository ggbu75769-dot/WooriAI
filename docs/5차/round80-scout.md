# 라운드 80 정찰 노트 (GAP-080)

> master 2af00ea(라운드 79 머지, PR #84) 기준 · 2026-08-30 실측. do-not-change.md(DNC-001~020) ·
> known-limitations A~T절 · runtime-verification-required.md · launch-readiness-status.md ·
> gap-analysis 제외 판정 · round55-plan §6 비범위표 · round56~79-scout 완료분 대조 완료.
>
> **80라운드째다. 그래서 이번 정찰의 축은 새 기능 발굴이 아니라 *누적 부채 정리와 판정
> 현행화*다** — 지시가 가중한 그 방향으로 A~T절·확인의 표·출시 현황을 훑었고, 아래 다섯은
> 전부 ⓐ **코드로 닫을 수 있는데 아직 열려 있는 것**이거나 ⓑ **판정이 낡아 재실측이 필요한 것**
> 이다. 새로 지어야 하는 한국어 문장은 다섯 트랙 통틀어 **0건**이고, 제품 소스를 여는 트랙은
> **둘**(A·B)뿐이다.
>
> **이번 라운드의 관측도 하나로 모인다 — 라운드 79가 "정확한 문장"과 "도달하는 문장"이 다른
> 축임을 이름 붙였는데, 그 축을 세는 그물이 *대장*을 모집단으로 삼았다. 대장은 잘못된 단위였다.**
> ① **같은 화면이 성공은 읽어 주고 실패는 읽어 주지 않는다** — `app/settings/privacy.tsx`는
>    `announceForA11y`를 이미 두 번 부르는데(`:307`·`:625`) **둘 다 성공 안내**이고, 되돌릴 수
>    없는 흐름 넷의 **실패** 문장 일곱 자리는 맨 `<Text>`다(후보 1).
> ② ⚠️ **그 화면의 낭독 계약은 이미 있고, 이름만 낭독이었다** —
>    `src/a11y-contract.test.ts:2534`의 describe가 *"되돌릴 수 없는 흐름의 실패 문구 **낭독**
>    계약"* 인데, 실제로 묻는 것은 `:2548` *"보이는 Text로 읽힌다"* 뿐이다. **가시성을 낭독이라고
>    부른 자리**다(후보 1).
> ③ **T-1이 실기기로 넘긴 질문의 절반은 소스가 이미 답하고 있었다** — 저장/조회를 가르는 근거가
>    *포커스가 어디 남는가*였는데, 조회 대장 열넷 안에 **뮤테이션이 세우는 문장이 셋** 있다
>    (파기 미리보기 — `privacy.tsx:799`·`:828`·`:856`). **단위는 대장이 아니라 방아쇠였다**(후보 1).
> ④ **T-2가 값으로만 남긴 형제 둘의 병은 지연이 아니라 영구 손실이다** — 실패 한 행이 남은
>    기기에서 `record_gap`·`monthly_wrapup`이 **영영** 발화하지 않는다(후보 2).
> ⑤ **이 저장소에 라우트 표면의 그물이 0건이다** — 그리고 재어 보니 `app/(onboarding)/budget.tsx`와
>    `app/budget.tsx`가 **같은 URL(`/budget`)** 로 등록된다(후보 4).

## 선행 확인 열둘 (후보 아님)

1. **라운드 79의 여섯 트랙은 전부 머지돼 있다**(실측). A=`app/settings/children.tsx:774`·`:792`·`:845`
   등 저장 실패 일곱 자리의 프롭 둘 + `announceForA11y` 배선, B=`src/notifications/generators.ts:432`의
   `hasRecoverablePendingRecordsForMonth`, C=`src/api/api-error.test.ts`의 `FAMILY_JOURNEY_SERVER_FILES`,
   D=`apps/admin/src/lib/revision-rows.ts`의 워커 갈래, E=`apps/admin/test/admin-api-source-parser.ts`,
   F=known-limitations **T절**·확인의 표 **127~130**(§0 합계 **130**). **재제안 대상이 아니다.**
2. **시드 카탈로그·DNC 배선은 그대로다**(2026-08-30). `item-ranking.ts` 수수료 grep 0건 ·
   `recommendation.ts`의 부정 단언 · `src/items/link-marker.ts` 한 자리의 마커·고지·공유 문장.
   **이 축에 후보 없음이고, 이번 라운드의 어느 트랙도 그 파일들을 열지 않는다.**
3. **출시 현황이 인용한 수치 셋은 오늘도 맞다**(실측 재확인 — 인용이 실측을 대신하지 않게):
   `grep -c '^chk ' scripts/qa/server-smoke.sh` → **37**, `check-env.ts`의 선택 카탈로그 → **41**,
   `grep -c 'await runStep(' scripts/qa/admin-e2e.mjs` → **18**. 세 값 다
   `launch-readiness-status.md`가 적어 둔 값과 같다. **이 축에 후보 없음.**
4. ⚠️ **그런데 같은 문서의 "문서 측" 한 문단은 낡았다**(후보 아님 · 트랙 F의 몫).
   `docs/5차/launch-readiness-status.md:33-37`은 *"§5 머리말과 §6 표가 서로 반대를 말해요"* 라고
   적고 정정을 다음 트랙에 넘기는데, **그 정정은 이미 끝났다** — `docs/store/play-listing.md` §5
   머리말은 오늘 *"제출 가능 여부는 장수가 아니라 계보 판정이고 오늘 그 판정은 ⛔예요"* 라고
   적고, known-limitations **N-2**의 마지막 줄도 *"§5 머리말의 §6 모순 — 해소됨"* 이다.
   **한 문서만 옛 상태를 말하고 있다.**
5. ⚠️ **known-limitations **Q절**의 "남은 사실"도 낡았다**(후보 아님 · 트랙 F의 몫).
   `known-limitations.md:1554`는 *"`app/family/invite.tsx`가 훅에게 완성된 문장을 받아 놓고
   불리언 하나로만 읽고 버린다"* 고 적는데, 실측은 반대다 — `app/family/invite.tsx:179-183`이
   `serverCopy: inviteSaveErrorCopy`를 **함께 넘기고**, `src/family/invite-permissions.ts:177-184`가
   그 값을 셋째 갈래로 쓴다(라운드 77 E). 그 파일의 머리말이 *"종전에는 앞의 하나만 넘어와 뒤의
   문장이 버려졌다"* 고 **과거형으로** 적는다.
6. **저장 실패 대장 다섯 화면의 일곱 자리는 오늘 전부 낭독 출구를 갖는다**(라운드 79 A + 리뷰
   M-1). `src/a11y-contract.test.ts:3344`가 출구 분포를 `{ announce: 6, toast: 1 }`로 못박고 있다.
   ⚠️ **후보 1은 그 스윕을 고치지 않는다 — 모집단을 한 겹 넓힌다.**
7. ⚠️ **api vitest는 이번 라운드도 돌리지 않았다**(정찰에 불필요 · 지시로 금지).
   **이번 라운드는 `apps/api/**` 소스를 여는 트랙이 0건이다**(트랙 D가 라우트를 세지만 모바일이고,
   트랙 C가 여는 것은 QA 스크립트 하나다).
8. **라운드 78 E의 슬라이스 가드 래칫이 이번에도 공통 제약이다.**
   `packages/test-utils/src/source-contract-slice-guard.test.ts:44`의 `SCAN_ROOTS`는
   `apps/mobile/src`·`apps/admin/src`·`packages` 셋이고 모집단은 `.test.ts` 파일뿐이다.
   이번 다섯 트랙이 여는 계약 파일 중 대장에 있는 것은 하나(`a11y-contract.test.ts` **7**)이고
   나머지는 대장에 없다 = **오늘 미가드 0건**이다. **공통 금지 하나: 새 `indexOf` 자리에는 예외
   없이 실재 확인을 함께 세운다** — 그러면 트랙 E 외에는 `packages/test-utils` 무접촉이다.
9. **`hasRecoverablePendingRecordsForMonth`의 두 성질(회복 가능 상태 × 그 달)은 예산 전용이다**
   (`generators.ts:432-450`). 형제 둘은 여전히 `hasPendingRecordsForChild`
   (`generators.ts:399-405` — `syncState !== "synced"` **전부** · 달 무관)를 본다. ⚠️ **두 술어가
   공존한다는 사실 자체는 결함이 아니다**(T-2가 그 이유를 적었다). 후보 2가 묻는 것은 형제 둘의
   술어가 **그 알림이 실제로 단언하는 것**과 같은 단위인가 하나다.
10. **S-3(어드민 `disabled`)은 재실측해도 값이 같다** — `apps/admin/app/items/page.tsx` **여섯** ·
    `apps/admin/app/links/page.tsx` **다섯** = **열하나**(`disabled={readOnly}`). 판정은 여전히
    **브라우저 확인 선행**이라 **보류 유지**이고, 이번 라운드의 어느 트랙도 그 둘을 열지 않는다.
11. **`withdrawn_at`은 재실측해도 0건이다** — 저장소 전체 grep에서 그 이름이 나오는 자리는 **셋**
    이고 셋 다 *"그 컬럼이 없다"* 를 말하는 자리다(`data-retention-purge.job.ts:379` ·
    `apps/api/test/withdrawn-row-write-paths.test.ts:121-122`). 라운드 75 P-1 → 76 Q-4 → 77 R-6 →
    78·79 P3의 구조가 변하지 않았다. **보류 유지(마이그레이션 0건 원칙).**
12. **T절이 값으로 닫은 축 셋은 재론하지 않는다** — 공유 카드/리포트 공유 왕복(후보 0건) ·
    서버 알림 층(선언된 설계) · 홈의 손 폴 다섯(결함 아님). ⚠️ `Share.share` 여섯 중 catch 없는
    `void` **둘**은 오늘도 그대로다(`app/items/[itemTemplateId].tsx:581`·**`:860`** — 라운드 79가
    `:855`로 적었는데 오늘 자리가 밀렸다). **여전히 사용자에게 보이는 차이가 0이라 결함으로
    올리지 않고**, 이번 라운드의 어느 트랙도 그 파일을 열지 않는다.

## 상위 후보

### 1. **같은 화면이 성공은 읽어 주고 실패는 읽어 주지 않는다 — 대장 밖 열셋** — 모바일·접근성 — M

- **근거**: 다섯이 한 줄로 이어진다.
  - ⓐ ⚠️ **가장 값싼 증거는 한 파일 안에 있다.** `app/settings/privacy.tsx`는 `announceForA11y`를
    이미 import하고(`:68`) 두 번 부른다 — `:307`(아이 삭제 완료 안내) · `:625`(동의 재수집 완료).
    **둘 다 성공이다.** 같은 화면의 **실패** 문장 일곱 자리는 맨 `<Text style={{ color:
    theme.colors.danger }}>`이고, 프롭도 announce도 **0건**이다:

    | 자리 | 문장 | 방아쇠 |
    | --- | --- | --- |
    | `:773` | `consentUpdateFailureText` | `reconsent`/`consentToggle` 뮤테이션 |
    | `:799` | `childPreviewLoadErrorCopy.title` | `childPreview.mutate()` — 눌린 [확인] |
    | `:810` | `childDeleteFailureText` | `childDelete` — 눌린 DangerButton |
    | `:828` | `householdPreviewLoadErrorCopy.title` | `householdPreview.mutate()` |
    | `:839` | `householdLeaveFailureText` | `householdLeave` |
    | `:856` | `accountPreviewLoadErrorCopy.title` | `accountPreview.mutate()` |
    | `:867` | `accountDeleteFailureText` | `accountDelete` |

    **일곱 다 눌린 버튼 바로 아래에 선다** — 라운드 79 T-1이 저장 실패 일곱 자리에 대해 적은
    그 조건과 **글자까지 같다.**
  - ⓑ ⚠️⚠️ **그 화면의 낭독 계약은 이미 있고, 이름만 낭독이었다.**
    `src/a11y-contract.test.ts:2534`의 describe 제목이 *"GAP-071 #2 되돌릴 수 없는 흐름의 실패
    문구 **낭독** 계약 (SET-004)"* 이고 머리말이 *"이 문장이 뜨는 순간은 사용자가 결과를 가장 알고
    싶은 순간이다(‘내 계정이 지워졌나?’)"* 라고 적는데, 실제로 묻는 셋은 ⓐ 각자 다른 문장인가 ·
    ⓑ **보이는 Text로 서는가**(`:2548`) · ⓒ 화면이 문장을 다시 짓지 않는가다. **가시성을 낭독이라
    부른 자리**이고, 라운드 79가 다섯 라운드에 걸쳐 이름 붙인 병(*정확도만 세고 도달은 세지
    않았다*)의 **가장 오래된 사례**다(라운드 71 B).
  - ⓒ ⚠️⚠️ **T-1이 실기기로 넘긴 질문의 절반은 소스가 이미 답하고 있었다.** T-1의 판정은
    *"저장과 조회를 가르는 근거는 포커스가 어디 남는가 하나"* 이고, 조회 대장 열넷은 그 답이
    올 때까지 열지 않기로 했다(`a11y-contract.test.ts:3610`의 ⓔ). ⚠️ **그런데 그 대장 안에
    뮤테이션이 세우는 문장이 셋 있다** — 파기 미리보기 셋(`privacy.tsx:799`·`:828`·`:856`)은
    `useLoadErrorCopy`를 쓰지만 방아쇠는 **`useMutation`의 `.mutate()`** 다. 대장 자신이 그 사실을
    이미 적고 있다: `src/offline/offline-aware-screens.ts:126` — *"그 자리의 재시도 수단은 바로
    위 [확인] 버튼이고 실패해도 계속 눌린다."*
    **즉 단위는 대장(조회/저장)이 아니라 방아쇠(쿼리/뮤테이션)였고, 그 답은 기기가 아니라
    소스에 있다.**
  - ⓓ **같은 모양이 대장 밖에 여섯 더 있다**(2026-08-30 전수 — `app/**`의 맨 danger Text 스무 자리
    중 뮤테이션 방아쇠):
    `app/import/index.tsx:478`(`validationMessage` — 파일 선택 검증) · `:489`(`upload.error`) ·
    `app/import/[importJobId].tsx:1132`(일괄 중간 실패) · `:1180`(행 편집 실패) · `:1210`(확정 실패) ·
    `app/settings/notifications.tsx:268`(**푸시 설정 실패** — ⚠️ T-1 다음 라운드 후보 ⓑ 그 자리다).
    ⚠️ **`notifications.tsx:268`만 성질이 다르다**: 프롭 둘은 **걸려 있고** announce만 없다
    (`:184`의 `useEffect`가 `toggleDevice` 하나만 읽는다) — **안드로이드는 읽고 iOS는 조용한**
    라운드 79 리뷰 M-1의 정확한 잔여이고, **한 줄이면 닫힌다.**
  - ⓔ **제외의 근거도 같은 기준에서 나온다(파생이지 손 목록이 아니다).** 쿼리가 세우는 자리
    (`privacy.tsx:711` · `notifications.tsx:291` · `settings/children.tsx:701` ·
    `import/[importJobId].tsx:1063`·`:1151` · `family/accept/[token].tsx:316` · 카드 자리 여덟)는
    **화면 영역이 통째로 바뀌고 포커스가 눌린 버튼에 없다** — T-1이 실기기로 넘긴 그 질문
    (A-20 #85)이 아직 답할 자리이고, **이번 트랙은 그것을 열지 않는다.**
    `app/index.tsx:396`(아이 복구 실패)도 같은 이유로 제외다 — 화면 전체가 그 카드로 바뀐다.
- **실패 시나리오**: TalkBack을 켠 사람이 계정을 지우려 한다. [삭제 전 확인] → 서버가 500으로
  답한다. `accountPreviewLoadErrorCopy.title`이 버튼 **바로 아래** 뜨지만 포커스는 버튼에 남아
  **아무 소리도 나지 않는다.** 그는 다시 누른다. 또 조용하다. ⚠️ **바로 그 화면이 성공했을
  때는 소리로 말해 준다**(`:307`) — 이 사람은 "성공하면 알려 주고 실패하면 침묵하는 앱"을 쓴다.
  그리고 그 화면의 계약 파일은 이 상황을 **낭독 계약**이라고 부르며 초록이다.
- **최소안**: **프롭 둘 + `useEffect` 한 벌. 새 한국어 문장 0건 · 새 컴포넌트 0건 · 렌더 0건 ·
  서버 0건 · 보이는 화면 한 픽셀 무변경.**
  ⓐ 위 **열셋**(privacy 일곱 · import 다섯 · notifications 하나)에 저장소 관례 조합
  (`accessibilityRole="alert"` + `accessibilityLiveRegion="polite"`) + `announceForA11y` 배선.
  ⚠️ `notifications.tsx:268`은 **announce 한 칸만** 는다(프롭은 이미 있다).
  ⓑ **계약은 방아쇠에서 파생한다** — `app/**` 전수에서 *"뮤테이션의 `isError` 아래 서는 실패
  문장"* 을 세고, 각 자리가 라운드 79가 세운 **출구 세 칸**(`announce`·`live-region`·`toast`) 중
  하나를 가질 것. `live-region`만인 자리 **0건**(= iOS에서 조용한 자리 0건)이 부정 단언이다.
  ⓒ **제외는 이유가 적힌 값으로** — 쿼리 방아쇠 자리는 *"화면 영역이 통째로 바뀌어 사용자가
  다시 훑는다 — A-20 #85의 답이 선행"*(T-1의 판정 문장 그대로 재사용).
  ⓓ **`a11y-contract.test.ts:2534`의 describe를 실제 낭독 계약으로 만든다** — 종전 셋(각자 다른
  문장 · 보이는 Text · 화면이 짓지 않음)은 **바이트 불변**으로 두고 넷째를 잇는다.
  ⓔ **`LOAD_ERROR_ANNOUNCE_OUT_OF_SCOPE_REASON`을 정정한다** — 오늘 그 문장은 "조회 대장은
  범위 밖"이라고만 적는데, 그 대장 안 셋이 이번에 열린다. 정정된 사유는 **대장이 아니라
  방아쇠**를 말해야 한다.
- **범위 밖(값으로만)**: 조회 대장 열넷의 **쿼리 자리**(A-20 #85 선행) · `role="alert"` 단독인
  축하 배너 하나(`app/(tabs)/items.tsx:969` — 실패가 아니다).

### 2. **형제 둘의 게이트는 지연이 아니라 영구 손실이다 — 그리고 게이트의 달과 알림의 달이 다르다** — 모바일·알림 — S

- **근거**: 넷이 한 줄로 이어진다(T-2 "다음 라운드 후보 ⓐ·ⓑ"의 실측).
  - ⓐ **형제 둘은 종점 상태를 센다.** `record_gap`(`generators.ts:516`)과
    `monthly_wrapup`(`:621`)은 `hasPendingLocalRecords`가 참이면 `null`을 돌려주고, 그 값은
    `hasPendingRecordsForChild`(`:399-405`)다 — `syncState !== "synced"` **전부**. ⚠️ 라운드 79
    리뷰가 예산에 대해 이미 이름 붙인 사실이 형제 둘에는 그대로 남아 있다:
    `failed`·`conflict`는 **큐가 스스로 다시 보내지 않는 종점**이고
    (`RECOVERABLE_PENDING_SYNC_STATES` — `:414`의 주석), 사용자가 재시도하거나 폐기할 때까지
    남는다. **라운드 57~59가 "영구 실패(4xx) 행"을 정식 상태로 만들어 두었으므로 이 행은
    가정이 아니라 실재하는 상태다.**
  - ⓑ ⚠️⚠️ **dedupe 단위가 그 상태를 손실로 바꾼다.** `record_gap`은 주 단위, `monthly_wrapup`은
    **달 단위**(`monthlyWrapupDedupeKey` — `:546`)다. 그런데 게이트가 참인 동안에는 **키를 태우지
    않으므로** 문제는 dedupe가 아니라 **평가 자체가 영원히 `null`을 낸다**는 것이다 — 4xx 실패
    한 행이 남은 기기에서 그 둘은 **영영** 발화하지 않는다. T-2는 이것을 *"지연"* 이라고 적었는데
    (형제 둘에 대해서는) **지연이 아니라 정지다.**
  - ⓒ **그런데 답은 "상태를 좁힌다"가 아니다 — 기존 의미를 보존하는 답이 따로 있다.**
    두 알림이 단언하는 것을 보면 어떤 행이 판정을 바꿀 수 있는지가 정해진다.
    · `record_gap`은 *"마지막 기록 이후 N일"* 을 말한다 → **`lastRecordedOn`보다 뒤인 행**만
      그 판정을 바꾼다. 3월에 실패한 행은 8월의 공백 판정을 바꿀 수 없다.
      ⚠️ **그 값은 이미 같은 입력 안에 있다**(`RecordGapInput.lastRecordedOn` — `:513`).
    · `monthly_wrapup`은 *"지난달 총액"* 을 말한다 → **지난달 행**만 그 판정을 바꾼다.
      ⚠️ **그 달도 이미 그 평가 안에 있다**(`useHomeNotificationEvaluation.ts`의
      `lastYearMonth = previousYearMonth(seoulCalendarDate(nowMs))`).
    **즉 좁히는 축은 상태가 아니라 범위이고, 좁혀도 "서버가 모르는 기록을 두고 단언하지
    않는다"는 규율은 한 글자도 약해지지 않는다** — 오히려 그 규율이 원래 뜻하던 것에 정확해진다.
  - ⓓ ⚠️ **그리고 예산 게이트에는 달이 갈리는 창이 있다**(T-2 다음 라운드 후보 ⓑ의 실측).
    `app/(tabs)/index.tsx:1266-1270`이 게이트 값을 만들 때 쓰는 달은 **기기 서울 달력**
    (`thisYearMonth` — `:1014`)인데, 그 게이트가 막는 알림이 태우는 키는 **서버의 달**이다
    (`generators.ts:139`·`:150` — `budget_100:${childId}:${yearMonth}`, 그 `yearMonth`는
    `input.monthly.yearMonth`). 자정·월초 경계나 **지난달 `/home` 캐시로 그리는 콜드 스타트**에서
    둘이 갈리면, 게이트는 8월 대기 행을 보고 **7월 알림을 막거나** 그 반대를 한다.
    ⚠️ **최소안은 표현식 하나다** — 게이트의 달을 **그 알림이 키를 태우는 달**로 맞춘다.
    두 표면을 맞추려던 T-2의 목적은 그대로다(둘이 갈리지 않는 대다수 경로에서 값이 같다).
- **실패 시나리오**: 4월에 태블릿에서 만든 지출 한 건이 서버에서 400으로 거절돼 `failed`로
  남는다. 사용자는 그 행을 고치지 않고 그냥 둔다(동기화 화면을 열어 본 적이 없다).
  ⚠️ **그날부터 그 기기에서 "지난달 정리" 알림은 다시는 오지 않는다.** 5월에도, 6월에도,
  12월에도. 기록 공백 리마인더도 마찬가지다 — **핵심 루프의 재진입 유도 둘이 조용히 죽는다.**
  아무 단언도 깨지지 않고, 사용자는 알림을 껐다고 생각한다.
- **최소안**: **문구 0건 · dedupeKey 형식 바이트 불변 · 서버 0건 · 새 요청·새 구독 0건.**
  ⓐ `record_gap`의 게이트를 *"`lastRecordedOn`보다 뒤인 대기 행"* 으로, `monthly_wrapup`의
  게이트를 *"지난달의 대기 행"* 으로 좁힌다. **상태 집합은 형제 둘에서 종전 그대로**(종점 포함)
  — 그 행도 사용자가 실제로 만든 기록이므로 범위 안에 들면 여전히 침묵해야 한다.
  ⓑ 게이트의 달을 알림이 키를 태우는 달로 맞춘다(예산).
  ⓒ **계약은 파생·부정·대조 셋** — 대기 행이 범위 밖이면 형제 둘의 답이 **종전과 바이트 불변**
  (오늘 대다수 경로) · 범위 안이면 **여전히 `null`** · 그리고 ⚠️ **세 게이트의 단위를 한 표로
  나란히 돌린다**(record_gap=시점 · monthly_wrapup=지난달 · budget=이번 달 × 회복 가능).
  ⓓ ⚠️ **`app/(tabs)/index.tsx`를 여는 비용을 값으로 적는다** — T-2의 판정 그대로다
  (*"그 파일을 열지 않는다는 설계 목표가 아니라 비용이고, 비용이 정직성과 부딪히면 정직성이
  이긴다 — 대신 그 파일을 여는 걸음은 핀을 함께 낮추는 걸음이어야 한다"*). 이번에 여는 것은
  **표현식 둘**(`:1255`·`:1266`)이고 훅 시그니처는 **한 칸도 늘지 않는다.**

### 3. **총계를 읽는 자리가 응답만 기다리고 렌더를 기다리지 않는다 — admin-e2e 9·10단의 레이스** — QA — S

- **근거**: 관찰된 플레이크(`4189=4189 non-narrowing`, 재실행 2회 그린)의 자리가 실측으로 하나다.
  - ⓐ `scripts/qa/admin-e2e.mjs:784-791` — `#filter-action`에 `admin.login`을 넣고 [필터 적용]을
    누른 뒤 `page.waitForResponse(...)`로 기다리고, **곧바로** `readAuditTotal(page)`를 부른다.
  - ⓑ ⚠️ **`waitForResponse`가 보장하는 것은 HTTP 응답의 도착이지 React 커밋과 heading 재렌더가
    아니다.** `readAuditTotal`(`:367-373`)은 `h2`의 `innerText`를 그 자리에서 읽으므로, 응답이
    막 도착한 프레임에서는 **직전(무필터) 총계를 그대로 읽는다.** 그때 `filteredTotal === total`
    이 되어 `:790`의 *"strict narrowing"* 단언이 `4189=4189`로 깨진다 — **관찰된 문자열과
    정확히 같은 모양이다.**
  - ⓒ ⚠️ **같은 창이 한 번 더 있다.** `filteredTotal`은 `:789`에서 읽고 CSV는 `:793`에서 내보내
  `:794`에서 그 값과 비교한다 — 두 시점 사이에 감사 로그가 한 줄이라도 쌓이면(그 e2e 자신이
  `admin.login`을 만든다) **행 수 비교가 어긋난다.** 오늘까지 관측되지 않았을 뿐 같은 종류다.
  - ⓓ **9단에도 같은 모양이 있다**(`:627`) — 다만 거기서는 비교가 `total < rowCount`라 stale 값이
    통과하는 방향이라 조용하다. **한 헬퍼가 두 자리에서 같은 가정을 진다.**
- **실패 시나리오**: 출시 직전 재인증에서 admin-e2e가 18/18이 아니라 17/18로 끝난다. 재실행하면
  초록이다. ⚠️ **그 순간 판단해야 하는 것은 "제품 결함인가 도구 결함인가"인데, 오늘 그 답이
  스크립트 안에 없다** — 실패 메시지가 `4189=4189`뿐이라 필터가 안 먹은 것인지 화면이 아직
  안 그려진 것인지 구분되지 않는다. **72시간 계획의 게이트가 그 자리에서 멈춘다.**
- **최소안**: **단언 0건 완화 · 스텝 수 18 불변 · 검증 항목 0건 삭제.**
  ⓐ `readAuditTotal` 옆에 **조건이 참이 될 때까지 다시 읽는** 한 벌을 세운다
  (`waitForAuditTotal(page, 술어)` — Playwright의 폴 상한 안에서 heading을 다시 읽는다).
  필터 적용 뒤에는 *"총계가 `total`에서 바뀌었다"* 를, 초기화 뒤에는 *"총계가 `total` 이상으로
  돌아왔다"* 를 기다린다.
  ⓑ 필터 내보내기의 기대 행 수는 **내보내기 직후에 다시 읽은 총계**와 비교한다(두 번째 창).
  ⓒ ⚠️ **실패 메시지가 원인을 말하게 한다** — 좁혀지지 않았을 때 *"heading이 아직 갱신되지
  않았을 수 있다"* 를 함께 적으면 다음 사람이 재실행 대신 판단을 한다.
- ⚠️ **제품 소스 0건 · 어드민 화면 0건.** 이 트랙은 도구 하나만 연다.

### 4. **이 저장소에 라우트 표면의 그물이 0건이고, 재어 보니 같은 URL에 두 화면이 있다** — 모바일·계약 — M

- **근거**: 라운드 76이 전수로 확인한 뒤 **네 라운드 연속 산문으로만 남은** 축을 실제로 쟀다.
  - ⓐ **그물이 없다.** `apps/mobile/src`의 계약 파일 서른아홉 중 라우트 파일을 **열거하는** 것은
    0건이다(`readdirSync`를 쓰는 열넷은 문구·토큰·env·슬라이스를 센다). DNC-003(하단 탭 넷)을
    지키는 단언은 **간접 셋**뿐이다(`src/settings-flow.test.ts:21` ·
    `src/import-flow.test.ts:20`이 `Tabs.Screen` 문자열의 존재만 · `src/settings/support-links.test.ts:224`) —
    **탭이 다섯 번째로 늘어도 빨개지는 자리가 없다.**
  - ⓑ ⚠️⚠️ **그리고 같은 URL에 두 화면이 있다.** `app/(onboarding)/`에 다섯 화면이 있고
    (`budget`·`child-profile`·`child-status`·`prepared-items`·`resume`), `app/onboarding/`에는
    **같은 이름의 한 줄짜리 재수출 다섯**이 있다(`export { default } from "../(onboarding)/budget";`).
    앱이 실제로 부르는 경로는 전부 `/onboarding/...`이다(`app/settings/privacy.tsx:312` ·
    `app/(auth)/login.tsx:228` · `app/index.tsx:450`·`:470` · `src/onboarding/steps.ts:5` 등).
    ⚠️ **그런데 expo-router에서 `(onboarding)`은 URL에 나타나지 않는 그룹이라, 그 다섯은
    `/budget`·`/child-profile`·`/child-status`·`/prepared-items`·`/resume` 로도 등록된다.**
    그중 **`/budget`은 `app/budget.tsx`(예산 수정 화면)와 같은 URL**이다.
  - ⓒ ⚠️ **`/budget`은 한가한 라우트가 아니다** — 더보기 메뉴(`src/settings/more-menu.ts:191`) ·
    설정 화면(`app/settings/index.tsx:280`) · 홈 예산 진행바(`src/home/budget-progress.ts:216`) ·
    **예산 경계 알림의 착지 지점**(`src/notifications/notification-route.ts:181`) 넷이 그 문자열을
    쓴다. 어느 화면이 이기는지는 **소스가 답할 수 없다**(expo-router의 충돌 해소 규칙이 답한다).
    이기는 쪽이 온보딩 예산 화면이면 *"예산 수정"* 을 누른 사람이 **초기 예산 설정 화면**
    (기본값 500,000 · 저장 후 홈으로)에 착지한다.
  - ⓓ **정찰은 이것을 결함으로 단정하지 않는다.** 80라운드 동안 예산 화면을 밟는 여정이 여러 번
    검증됐으므로 오늘 이기는 쪽은 `app/budget.tsx`일 가능성이 높다. ⚠️ **그러나 그 사실이 어디에도
    적혀 있지 않고, 어떤 단언도 그것을 지키지 않는다** — 다음에 `(onboarding)` 그룹에 파일이
    하나 늘면 같은 방식으로 또 하나가 겹치고, **아무도 모른다.**
  - ⓔ **참조 0건인 라우트 파일도 값으로 세어야 한다** — `app/pixel-lock.tsx`(캡처 전용 ·
    `__DEV__ || EXPO_PUBLIC_PIXEL_LOCK === "1"` 게이트) · `app/launch-animation.tsx` ·
    `app/(tabs)/more.tsx`(`href: null`로 탭 바에서 빠진 다섯째 파일 — **DNC-003을 지키는 바로
    그 장치**). 셋 다 이유가 있고, **그 이유가 값으로 적힌 자리는 오늘 없다.**
- **실패 시나리오**: 출시 뒤 예산 경계 푸시를 누른 사람이 초기 예산 설정 화면에 착지해 500,000이
  적힌 칸을 보고 저장한다. **그 사람의 이번 달 예산이 덮어써진다.** 오늘 그 일이 일어나지 않는다는
  근거는 "여태 안 그랬다"뿐이고, 그 근거는 라우트 파일이 하나 늘어나는 순간 사라진다.
- **최소안**: **제품 소스 0건 · 라우트 파일 이동 0건 · 화면 0건.** 신설 계약 하나가
  ⓐ `app/**`의 라우트 파일을 **전수 열거**하고 URL로 정규화한다(그룹 `(...)` 세그먼트 제거).
  ⓑ **하단 탭 넷을 파생으로 못박는다**(DNC-003 — `_layout.tsx`의 `Tabs.Screen` 중 `href: null`이
  아닌 것이 정확히 넷이고 그 이름이 `index`·`records`·`items`·`reports`일 것).
  ⓒ **같은 URL에 파일이 둘 이상인 자리를 센다** — 오늘 그 답은 **하나**(`/budget`)이고,
  그 하나가 **이유와 함께** 적힌 목록에 있을 것. ⚠️ **둘째가 생기는 날 빨개진다.**
  ⓓ **참조 0건 라우트를 센다** — 각 자리는 참조되거나 **이유가 적힌 제외**에 있을 것
  (`pixel-lock`=캡처 전용·env 게이트 · `launch-animation`=스플래시 · `more`=`href: null` ·
  `(onboarding)` 그림자 다섯=재수출 다섯이 정본 URL을 진다).
  ⓔ ⚠️ **`/budget`이 어느 화면에 착지하는지는 실행 확인 항목으로 신설한다**(확인의 표 §1-1 —
  표면 칸 **실기기**). 계약은 *"오늘 겹친다"* 는 사실만 고정하고, *"어느 쪽이 이긴다"* 는
  주장하지 않는다(모르는 것을 말하지 않는다).

### 5. **자르기를 실제로 하는 코드가 자르기 계약의 모집단 밖에 있다 (T-5 리뷰 S-5가 다음 라운드로 넘긴 결정)** — 계약·부채 — S

- **근거**: 라운드 79가 스스로 열어 둔 결정이고, 값이 이미 적혀 있다.
  - ⓐ `packages/test-utils/src/source-contract-slice-guard.test.ts:44`의 `SCAN_ROOTS`는
    `apps/mobile/src`·`apps/admin/src`·`packages` 셋이고, 모집단은 그 아래의 **`.test.ts`뿐**이다.
  - ⓑ ⚠️ 라운드 79 E가 공용 파서를 `apps/admin/test/admin-api-source-parser.ts`에 두었다 —
    **번들에 실리지 않고 어느 스윕도 걷지 않는 자리**를 일부러 고른 것이고 그 판단은 옳았다.
    그런데 T절이 적듯 *"바로 그 '어느 스윕도 걷지 않는다'가 그대로 두면 구멍"* 이라, 리뷰 S-5는
    **예외를 값으로 적고 같은 검출기를 그 파일에 직접 돌리는** 임시안으로 닫으며
    **모집단 확장 결정을 다음 라운드로 명시적으로 넘겼다.**
  - ⓒ ⚠️ **임시안의 비용이 값으로 적혀 있다** — 검출기가 두 벌로 돌고, 새 테스트 전용 헬퍼가
    생길 때마다 **사람이 그 예외를 기억해야** 한다. 라운드 78 E가 래칫을 세운 이유(*"기억이 아니라
    대장이 센다"*)와 정확히 반대 방향이다.
  - ⓓ **결정의 축은 하나다: 모집단을 "파일 이름"으로 가를 것인가 "하는 일"로 가를 것인가.**
    `.test.ts`는 이름이고, *"소스를 문자열로 읽어 `indexOf`로 자르는 파일"* 은 하는 일이다.
    뒤를 고르면 헬퍼·픽스처가 함께 들어오고(그래서 라운드 79가 미뤘다), **그때 필요한 것은
    모집단이 아니라 이유가 적힌 제외 목록**이다 — 이 저장소가 이미 다섯 번 쓴 형식이다.
- **실패 시나리오**: 다음 라운드가 테스트 전용 파서를 하나 더 만든다(어드민 미러 스크레이프 ·
  모바일 JSX 스캐너 — 둘 다 후보다). 그 파일은 `.test.ts`가 아니라 대장 밖이고, 라운드 79가
  남긴 예외는 **파일 이름 하나를 가리키므로** 새 파일을 세지 않는다. ⚠️ **자르기 계약을 지키는
  코드가 자르기 계약 밖에서 조용히 늘어난다** — R-6 P-3이 이름 붙인 *"한쪽의 단위가 조용히 다른
  쪽을 덮는다"* 가 도구 자신에게 세 번째로 나타난다.
- **최소안**: **제품 소스 0건 · 대장 수치 비증가 · 기존 가드 하한 불변.**
  ⓐ 모집단을 **하는 일**로 옮긴다: 스캔 뿌리 아래의 **비제품 소스 전수**(`.test.ts` +
  테스트 전용 헬퍼 뿌리)에서 이 모양을 센다.
  ⓑ ⚠️ **`apps/admin/test/**`를 뿌리에 더하는 것이 이 트랙의 본체다**(라운드 79의 한 파일 예외를
  대장으로 승격 — 예외 줄은 사라지고 대장 한 줄이 대신 선다).
  ⓒ **늘어난 모집단이 세는 새 자리는 이유와 함께 제외되거나 가드된다**(빈 문자열 금지 규율).
  ⓓ **자기 무효화** — 승격 뒤 라운드 79의 임시 예외가 소스에 남아 있으면 빨개진다(두 벌로 도는
  검출기가 하나로 줄었다는 사실이 단언으로 선다).

## P3

- **S-3(어드민 `disabled`가 앗아가는 것) — 보류 유지, 재평가만.** 2026-08-30 재실측: 자리는
  여전히 **열하나**(`app/items/page.tsx` 여섯 · `app/links/page.tsx` 다섯)이고 라운드 79가 정정한
  전제(`<select>`는 활성 상태에서도 드래그 복사가 되지 않는다 → `disabled`가 앗아가는 것은
  **복사가 아니라 도달**)도 그대로다. **브라우저 확인(A-20)이 선행**이고, 이번 라운드의 어느
  트랙도 그 둘을 열지 않는다.
- **`withdrawn_at` — 보류 유지, 재평가만.** 컬럼 0건 · 그 사실을 아는 자리 셋(전부 "없다"를
  말한다). 구조 변화 0. **컬럼 신설은 여전히 별도 결정이고, 이번 라운드는 `apps/api/**`를
  열지 않는다.**
- **조회 실패 대장 열넷의 플랫폼 축 — 이월 후보 중 유일하게 *단위를 정정한 뒤* 절반만 채택한다.**
  실측 결과 대장은 이 질문의 단위가 아니었다: 열넷이 그리는 자리 중 **여덟이 EmptyStateCard**
  (`src/ui.tsx:1119` — 영역 교체), 맨 Text 중 **셋이 뮤테이션**(→ 트랙 A), 남는 것이 순수 쿼리
  자리다. ⚠️ **남은 쿼리 자리는 여전히 A-20 #85 선행이고, 이유는 T-1의 문장 그대로다** —
  다만 이제 그 문장은 "저장/조회"가 아니라 **"뮤테이션/쿼리"** 로 적혀야 한다(트랙 A ⓔ).
- **`app/settings/notifications.tsx`의 푸시 실패 줄 — 채택(트랙 A).** T-1 다음 라운드 후보 ⓑ가
  *"프롭 둘은 걸렸지만 announce 배선은 없다"* 고 적었고 실측이 같다(`:268` 프롭 O ·
  `:184`의 `useEffect`는 `toggleDevice`만). ⚠️ **결함으로 올리지 않은 종전 사유("대장이 세지
  않는 자리")가 이번에 사라진다** — 새 모집단은 대장이 아니라 방아쇠이므로 그 자리를 센다.
- **`app/items/[itemTemplateId].tsx`의 `Share.share` 둘 — 재론 없음(값만 갱신).**
  catch 없는 `void` 둘은 오늘 `:581`·**`:860`** 이다(라운드 79가 `:855`로 적었다 — 자리가 밀렸다).
  사용자에게 보이는 차이 0이라는 판정 그대로이고, 이번 라운드도 그 파일을 여는 트랙이 0건이다.
- **`"시도해 주세요"` vs `"시도해주세요"` — 재론 없음(기각 확정).** 다섯 트랙이 더하는 한국어
  문장이 **0건**이라 그 수치를 흔들 자리가 없다.
- **낡은 판정 둘 — 트랙 F가 한 줄씩 갱신한다(위 선행 확인 4·5).**
  `launch-readiness-status.md:33-37`의 §5/§6 모순 서술 · known-limitations `:1554`의 초대 화면
  "남은 사실". 둘 다 **이미 닫힌 사실을 열린 것으로 말하고 있다.** ⚠️ **이것이 이번 라운드가
  가중한 "판정 현행화"의 정확한 모양이다** — 열린 것을 닫는 것만큼 **닫힌 것을 닫혔다고 적는
  것**이 부채다(다음 사람이 그 문단을 근거로 같은 일을 다시 한다).
- **CSV 왕복 다섯 열 손실 · `refund` 생성 불가 · `link_health`의 `errors` 카운터 · 서버 알림 층 ·
  홈의 손 폴 다섯 · 공유 카드 왕복 · 어드민 카탈로그 전량 조회 · M-3 잔여 ·
  `ApplicationPrimitives.tsx` 정규식 제목 판정 · 미출처 틴트 둘 · `itemMatchesBand` 사문 ·
  첫돌 이후 마일스톤 고착 · `AuthService.refresh`의 `user.status` · api 하네스 동시 실행 구멍 ·
  서버 중복 아이 가드 부재 · 발행 `before` 경합 · `monthly_wrapup` 콜드 스타트 시점 ·
  크래시 파이프라인 부재 · 서버 stdout 두 로그 형식(O-1)** — 라운드 62~79가 남긴 그대로이고
  **상태 변화가 없다.**
- **제외 목록 준수 확인**: 준비템 목록 **가격 표시**(사용자 결정 대기) · 오프라인 로컬 아이 복구 ·
  외부 계정/키/자산(**스토어 그래픽 ⛔ 포함 — 재캡처는 실기기 몫**) · **C-3 잠금 오버레이 낭독**
  (오늘로 **열네 라운드 연속** 미확인) · **P-2 법무 대조** · **P-3 테스트 건수 자동화**(기각 확정) ·
  **표기 방언**(기각 확정) · **S-4 파기 `targetId`**(기각 확정) · 40주 초과 달력 ·
  `onBudgetRelevantChange` · 4가구/`viewedHouseholdId` · **지출→리포트 정확성 축**(라운드 78이
  후보 0건으로 닫음 — 재론 금지) · **공유 왕복 축**(라운드 79가 후보 0건으로 닫음).
  **이번 라운드의 어느 트랙도 이 자리들을 열지 않는다.**

## 코드 건강 판정

- **⚠️ 이번 라운드의 가장 값진 관측: 그물의 모집단이 잘못된 단위였다.** 라운드 79는 낭독을
  **대장**(`OFFLINE_AWARE_SAVE_ERROR_SCREENS`)으로 셌고, 그것이 옳은 축이라고 판단해 조회 대장
  열넷을 실기기 답이 올 때까지 미뤘다. ⚠️ **그런데 그 두 대장을 가르는 축(조회/저장)과 낭독이
  실제로 기대는 축(포커스가 어디 남는가)이 같지 않다** — 조회 대장 안에 뮤테이션 자리가 셋
  있었고, 대장 **밖**에 같은 모양이 열이 더 있었다. **대장은 문구의 단일 소스를 세는 단위이지
  도달을 세는 단위가 아니었고, 그 사실은 기기가 아니라 소스가 답했다.**
  **일반형: 어떤 그물을 새 축에 재사용할 때는 그 그물의 모집단이 새 질문의 단위인지 먼저 묻는다.**
- **⚠️ 두 번째 관측: "이름이 낭독인 계약"이 아홉 라운드 동안 초록이었다.**
  `a11y-contract.test.ts:2534`는 라운드 71 B가 세운 자리이고 제목이 **낭독 계약**인데 묻는 것은
  **가시성**이다. 라운드 79가 T-1에서 *"정확도만 다섯 번 올랐고 도달은 한 번도 세어지지
  않았다"* 고 적었는데, ⚠️ **그 문장의 가장 이른 사례는 계약 파일 자신이었다.**
  **일반형: 계약의 제목이 묻는 것과 본문이 묻는 것이 갈리면, 그 계약은 갈린 만큼 조용하다.**
- **⚠️ 세 번째 관측: 80라운드째의 부채는 "열린 것"보다 "닫혔는데 열렸다고 적힌 것"이 많다.**
  이번 총점검에서 **코드로 닫을 수 있는데 아직 열린 것**은 다섯이었고(후보 1~5), **판정이
  낡은 것**은 둘이었다(선행 확인 4·5). 그리고 낡은 판정 둘은 **둘 다 "다음 트랙의 몫"이라고
  적힌 채로** 남아 있었다 — 소유자를 지목하는 문장이 소유자를 만들지는 않는다.
  **일반형: 이월은 값으로 적히면 살아남지만, 완료도 값으로 적혀야 사라진다.**
- **네 번째 관측: 이 저장소에서 아직 그물이 없는 축은 "파일이 아니라 표면"이다.**
  문구·토큰·env·역할·슬라이스·여정에는 전수 스윕이 있는데 **라우트에는 없다**(후보 4).
  그래서 하단 탭 넷은 네 라운드째 산문으로 재어졌고, 같은 URL에 두 화면이 있다는 사실은
  80라운드 동안 아무도 세지 않았다. ⚠️ **DNC 계약 스무 줄 중 기계가 지키지 않는 것이 무엇인지
  세어 보는 것이 다음 라운드의 후보다**(오늘 DNC-003이 그 답의 하나였다).
- **이번 라운드도 계약은 전부 파생/부정/전수다.** 다섯 후보가 살아남은 이유가 같다 —
  *뮤테이션이 세우는 문장이 소리로 오는지 세는 것이 없다* · *게이트의 범위가 알림의 단언과
  같은지 묻는 것이 없다* · *총계를 읽는 자리가 렌더를 기다리는지 묻는 것이 없다* ·
  *라우트 표면을 세는 것이 없다* · *자르기를 하는 코드를 자르기 계약이 세는지 묻는 것이 없다*.
  **다섯 다 어떤 단언도 깨지 않는 사실이다.**
- **큰 파일 판정.** 트랙이 여는 파일 중 1,000줄을 넘는 것은 **둘**
  (`src/a11y-contract.test.ts` **3,624** — 트랙 A가 스윕 하나를 더한다 · `app/(tabs)/index.tsx` —
  트랙 B가 **표현식 둘**만 만진다). 제품 소스 중 트랙 A가 가장 많이 만지는
  `app/settings/privacy.tsx`도 더하는 것은 **프롭 열넷 + `useEffect` 한 벌**이고 렌더는 0건 바뀐다.
- **이월 정산.** 이월 여섯 중 **셋을 채택**(대장 밖 푸시 실패 줄 → 트랙 A · 형제 둘의 종점 상태와
  게이트의 달 → 트랙 B · admin-e2e 플레이크 → 트랙 C), **하나를 단위 정정 후 절반 채택**
  (조회 대장 열넷 → 뮤테이션 셋만 트랙 A · 쿼리 자리는 A-20 선행으로 보류), **둘을 보류 유지**
  (S-3 · `withdrawn_at`). 자유 발굴로 **둘**을 더했고(라우트 표면 · 슬라이스 가드 모집단),
  둘 다 **누적 부채**이지 새 기능이 아니다. **판정 현행화 둘**은 트랙 F가 한 줄씩 갱신한다.

## 트랙 구성 (파일 단위 상호 배타)

- **A 눌러서 나타난 실패가 소리로 온다 — 대장 밖 열셋** (#1) — **즉시 착수 · 사용자 도달**
  - 소유: `apps/mobile/app/settings/privacy.tsx`(⚠️ **일곱 자리** — 프롭 열넷 + announce 배선) ·
    `apps/mobile/app/settings/notifications.tsx`(⚠️ **announce 한 칸만** — `:268`. 프롭은 이미 있다) ·
    `apps/mobile/app/import/index.tsx`(**두 자리**) ·
    `apps/mobile/app/import/[importJobId].tsx`(**세 자리** — `:1132`·`:1180`·`:1210`) ·
    `apps/mobile/src/a11y-contract.test.ts`(⚠️ **방아쇠 파생 스윕 + 부정 단언 + `:2534` describe의
    넷째 단언 + `LOAD_ERROR_ANNOUNCE_OUT_OF_SCOPE_REASON` 정정**)
  - 읽기: `apps/mobile/src/offline/offline-aware-screens.ts`(**두 대장 — 읽기만**) ·
    `apps/mobile/src/ui.tsx`의 `announceForA11y`·Toast · `apps/mobile/app/(auth)/login.tsx`와
    `app/family/invite.tsx`(**관례의 본보기**) · `apps/mobile/src/settings/destructive-flow-messages.ts` ·
    `apps/mobile/src/import/import-failure-messages.ts`
  - 금지: ⚠️ **한국어 문장 0건 · 스타일/레이아웃 속성 0건**(프롭 둘 + `useEffect`만 — 픽셀락 무관) ·
    `src/offline/**` **무접촉**(대장 셋·모듈 대장 둘) · `src/settings/destructive-flow-messages.ts`·
    `src/import/*.ts` **무접촉**(문구·판정은 모듈의 것) · **쿼리 방아쇠 자리 무접촉**
    (`privacy.tsx:711` · `notifications.tsx:291` · `import` 조회 둘 · `children.tsx:701` ·
    `accept/[token].tsx:316` · 카드 여덟 · `app/index.tsx:396`) · `app/settings/children.tsx`·
    `app/budget.tsx`·`app/family/**` **무접촉**(라운드 79가 이미 닫았다 — **읽기만**) ·
    `app/(tabs)/index.tsx` **0건**(트랙 B 소유) · `src/notifications/**` **0건**(트랙 B) ·
    ⚠️ **`ROUND79_ANNOUNCE_PROPS_ADDED`·`SAVE_ERROR_ANNOUNCE_*` 값 바이트 불변**(라운드 79의 기록은
    오늘도 참이다 — 새 목록은 따로 선다) · ⚠️ 새 `indexOf` 자리에는 실재 확인 필수(대장 **7**
    비증가 — `packages/test-utils` 무접촉) · 서버 0건 · 마이그레이션 0건 · DNC-018
  - 계약: ⓐ **방아쇠 파생 전수** — `app/**`에서 *뮤테이션 `isError` 아래 서는 실패 문장* 자리를
    세고, 각 자리가 출구 셋(`announce`·`live-region`·`toast`) 중 하나를 가질 것. 손 목록 금지.
    ⓑ **부정 단언 둘** — `live-region`만인 자리 **0건**(iOS 침묵 0건) · `silent` **0건**.
    ⓒ **제외는 이유가 적힌 값으로** — 쿼리 방아쇠 자리는 *"화면 영역이 통째로 바뀐다 — A-20 #85
    선행"*, 축하 배너 하나는 *"실패가 아니다"*. 이유는 빈 문자열일 수 없다.
    ⓓ **`:2534`의 낭독 계약이 실제로 낭독을 묻는다** — 종전 셋은 **바이트 불변**, 넷째가 잇는다.
    ⓔ **재현 단언** — 프롭·announce를 뺀 소스가 실제로 빨개질 것.
    ⓕ **바이트 불변** — 열셋의 **문장·스타일·조건**이 한 글자도 바뀌지 않을 것.
    ⓖ **판정을 값으로** — *단위는 대장이 아니라 방아쇠다*가 값으로 적힐 것(다음 라운드가 같은
    스윕을 대장으로 다시 돌리지 않도록).

- **B 게이트의 범위를 그 알림이 단언하는 것에 맞춘다** (#2) — **A와 완전 독립**
  - 소유: `apps/mobile/src/notifications/generators.ts`(⚠️ **술어 둘 + 게이트 둘**) ·
    `apps/mobile/src/notifications/generators.test.ts` ·
    `apps/mobile/src/notifications/useHomeNotificationEvaluation.ts`(**주석·전달 정합**) ·
    `apps/mobile/app/(tabs)/index.tsx`(⚠️ **표현식 둘** — `:1255`·`:1266`)
  - 읽기: `apps/mobile/src/home/budget-edit.ts`의 `hasPendingMonthAdjustments` ·
    `apps/mobile/src/offline/types.ts`의 `SyncState` ·
    `apps/api/src/push/push-dispatch.service.ts`(**"세 표면" 주석 — 읽기만**)
  - 금지: ⚠️ **훅 시그니처 무변경**(새 인자 0건 — 값은 이미 그 스냅샷에 있다) ·
    **`weeklySummaryNotification` 무접촉**(이미 정합이다 — T-2 판정) ·
    `notification.store.ts`·`notification-preferences.store.ts` **무접촉**(dedupe 메모리·muted 규칙
    불변) · **dedupeKey 형식 바이트 불변** · **문구 0건 변경** · `RECOVERABLE_PENDING_SYNC_STATES`
    **무접촉**(예산 전용 상태 집합은 그대로다 — 이 트랙이 좁히는 축은 **범위**다) ·
    ⚠️ **`app/(tabs)/index.tsx`를 열되 그 걸음이 핀을 함께 낮추는 걸음일 것**(T-2 판정 — 바이트 핀을
    만나면 모양 핀으로 내리고 그 의존을 단언으로 적는다) · 서버 0건 · 마이그레이션 0건 ·
    ⚠️ 새 `indexOf` 자리에는 실재 확인 필수(대장 **1** 비증가)
  - 계약: ⓐ **파생 단언 셋** — 세 게이트의 **단위를 한 표로 나란히** 돌린다
    (record_gap=`lastRecordedOn` 이후 · monthly_wrapup=지난달 · budget=이번 달 × 회복 가능).
    ⓑ **부정 단언** — 대기 행이 **범위 밖**이면 형제 둘의 답이 **종전과 바이트 불변**(오늘 대다수
    경로) · 범위 **안**이면 여전히 `null`(규율은 약해지지 않는다).
    ⓒ **영구 정지의 재현** — `failed` 한 행만 있는 스냅샷에서 형제 둘이 **오늘 침묵하고 고친 뒤
    발화**할 것(그 한 행이 범위 밖일 때).
    ⓓ **달의 정합** — 예산 게이트가 보는 달과 dedupeKey가 태우는 달이 **같은 출처**일 것
    (⚠️ 갈리는 창의 존재와 오늘의 답을 값으로 적는다).
    ⓔ **키를 태우지 않을 것** — 억제된 평가가 dedupe 메모리에 기록을 남기지 않아 동기화 뒤 다음
    평가가 **정확히 한 번** 발화할 것(라운드 79 B가 예산에서 세운 성질의 재적용).

- **C admin-e2e가 응답이 아니라 화면을 기다린다** (#3) — **A·B와 독립 · 제품 소스 0건**
  - 소유: `scripts/qa/admin-e2e.mjs`
  - 읽기: `apps/admin/app/audit-logs/**`(**heading 문자열 — 읽기만**) ·
    `apps/admin/src/lib/audit-log-csv.ts`의 열 목록
  - 금지: ⚠️ **스텝 수 18 불변**(`runStep` 호출 수 — `launch-readiness-status.md`가 그 값을 인용한다) ·
    **검증 항목 0건 삭제 · 단언 0건 완화**(레이스를 없애는 것이지 기대를 낮추는 것이 아니다) ·
    `STEP_TIMEOUT`·`NAV_TIMEOUT` **상수 무변경** · 어드민 화면·서버 0건 ·
    `scripts/qa/server-smoke.sh` **무접촉**(`^chk ` 37 불변) · 마이그레이션 0건
  - 계약(스크립트 자신의 규율): ⓐ 필터 적용 뒤 총계를 읽는 자리는 **총계가 실제로 바뀔 때까지**
    다시 읽을 것. ⓑ 내보내기 행 수 비교는 **내보내기 직후에 다시 읽은 총계**와 할 것.
    ⓒ 실패 메시지가 **두 원인을 구분해 말할 것**(필터가 안 먹었다 / 화면이 아직 안 그려졌다).
    ⓓ 같은 헬퍼를 쓰는 9단의 자리도 같은 규율을 지날 것(한 헬퍼가 두 자리에서 다른 가정을 지지
    않게).

- **D 라우트 표면의 첫 그물** (#4) — **A·B·C와 독립 · 제품 소스 0건**
  - 소유: **신설** `apps/mobile/src/route-surface.test.ts`
  - 읽기: `apps/mobile/app/**`(**라우트 파일 전수 — 읽기만**) · `app/(tabs)/_layout.tsx` ·
    `apps/mobile/src/settings/more-menu.ts` · `src/notifications/notification-route.ts` ·
    `src/home/budget-progress.ts` · `src/onboarding/steps.ts`
  - 금지: ⚠️ **제품 소스 0건**(라우트 파일 **이동·삭제·생성 0건** — `(onboarding)` 그룹을 손대는
    것은 별도 결정이다) · **재수출 다섯 무접촉** · `app/(tabs)/_layout.tsx` **무접촉**
    (DNC-003을 **읽어서** 못박는다) · `app/budget.tsx`·`app/(onboarding)/budget.tsx` **무접촉** ·
    `src/settings-flow.test.ts`·`src/import-flow.test.ts`의 기존 `Tabs.Screen` 단언
    **바이트 불변**(⚠️ 합치지 않는다 — 그 둘의 단위는 여정이다) · 서버 0건 · 마이그레이션 0건 ·
    ⚠️ 새 파일은 **대장에 없다 = 오늘 미가드 0건**이라 새 `indexOf` 자리에는 예외 없이 실재 확인
  - 계약: ⓐ **전수 열거 + URL 정규화**(그룹 세그먼트 제거). ⓑ **DNC-003 파생** — `href: null`이
    아닌 `Tabs.Screen`이 정확히 **넷**이고 이름이 `index`·`records`·`items`·`reports`일 것.
    ⓒ **같은 URL에 파일이 둘 이상인 자리**가 **이유가 적힌 목록**과 정확히 일치할 것(오늘 하나 —
    `/budget`). ⚠️ **둘째가 생기는 날 빨개진다.** ⓓ **참조 0건 라우트**가 이유가 적힌 제외에
    있을 것(`pixel-lock`·`launch-animation`·`more`·그림자 다섯). ⓔ ⚠️ **주장하지 않을 것** —
    계약은 *"오늘 겹친다"* 만 고정하고 *"어느 쪽이 이긴다"* 는 말하지 않는다(그 답은 트랙 F가
    확인의 표에 **실기기 항목**으로 세운다).

- **E 자르기 계약이 자르기를 하는 코드를 전부 센다** (#5) — **A·B·C·D와 독립 · 제품 소스 0건**
  - 소유: `packages/test-utils/src/source-contract-slice-guard.test.ts`
  - 읽기: `apps/admin/test/admin-api-source-parser.ts` · `apps/admin/src/lib/admin-api.test.ts`
    (**라운드 79의 임시 예외가 사는 자리 — 읽기만이 원칙이고, 예외 줄 제거가 필요하면 그 한 줄만**)
  - 금지: ⚠️ **대장 수치 비증가**(파일별 미가드 자리 수) · **가드 하한 불변**(라운드 77 M-3·78 E가
    세운 실재 확인이 조용히 사라지지 않는다) · **두 실패 방향 재현 픽스처 무변경** ·
    **줄 번호로 적지 않는다**(단위는 `파일 → 개수` — 이 파일의 규율) · `apps/api` **범위 밖 유지**
    (그 확장은 또 다른 결정이다) · 제품 소스 0건 · 서버 0건 · 마이그레이션 0건
  - 계약: ⓐ **모집단이 이름이 아니라 하는 일**일 것(테스트 전용 헬퍼 뿌리 편입).
    ⓑ **라운드 79의 한 파일 예외가 대장 한 줄로 승격**될 것 — 그 임시 예외가 소스에 남아 있으면
    빨개진다(두 벌로 돌던 검출기가 하나로 줄었다는 사실의 단언).
    ⓒ **늘어난 모집단이 세는 새 자리**는 가드되거나 **이유가 적힌 제외**에 있을 것(빈 문자열 금지).
    ⓓ **유령 방지 유지** — 대장·하한의 키가 전부 실재하는 파일일 것(라운드 78 리뷰 P-1의 규율).

- **F 판정·접근성 표·확인의 표·출시 현황** — **A·B·C·D·E 머지 후**
  - 소유: `docs/operations/known-limitations.md` · `docs/qa/runtime-verification-required.md` ·
    `docs/qa/accessibility-offline-checklist.md` · `docs/5차/launch-readiness-status.md`
  - 금지: **제품 소스 0건** · `packages/test-utils/**` **무접촉**(트랙 E) ·
    `docs/store/**`·`infra/legal/**`·`README.md`·`AGENTS.md`·`CODEX_START_HERE.md` **무접촉** ·
    **행 삭제 0건 · 행 번호 불변** · 각 행의 문장·기대 동작·근거 파일·부정 조건 **바이트 불변** ·
    K~T절의 **판정을 다시 쓰지 말 것**(Q-?·S-3·T-1·T-2·T-5는 **갱신 한 줄**씩만)
  - 계약: ⓐ **known-limitations에 U절을 신설**하고 이번 라운드가 확정한 판정 다섯을 남길 것 —
    (1) **그물의 모집단이 잘못된 단위였다**(대장이 아니라 방아쇠 · 조회 대장 안에 뮤테이션 셋이
    있었다는 실측 · *"어떤 그물을 새 축에 재사용할 때는 모집단이 새 질문의 단위인지 먼저 묻는다"*),
    (2) **이름이 낭독인 계약이 아홉 라운드 동안 가시성만 물었다**(라운드 71 B의 자리 · 계약의
    제목과 본문이 갈리면 갈린 만큼 조용하다),
    (3) **형제 둘의 게이트는 지연이 아니라 정지였다**(종점 상태 × 범위 무관 · 좁히는 축은 상태가
    아니라 **범위**라는 판정 · 게이트의 달과 알림의 달이 갈리는 창),
    (4) **응답을 기다리는 것과 화면을 기다리는 것은 다르다**(`4189=4189`의 원인 · 한 헬퍼가 두
    자리에서 다른 가정을 지고 있었다는 사실),
    (5) **라우트 표면에는 80라운드 동안 그물이 없었다**(같은 URL에 두 화면 · DNC 스무 줄 중 기계가
    지키지 않는 것을 세어 보는 것이 다음 라운드 후보라는 판정).
    ⓑ **T-1 갱신 한 줄**: 저장/조회로 적은 축이 **뮤테이션/쿼리**로 정정됐다는 사실과, 그래서
    A-20 #85가 묻는 범위가 **좁아졌다**는 사실(쿼리 자리만 남는다).
    ⓒ **T-2 갱신 한 줄**: *"다음 라운드 후보 ⓐ"* 가 채택됐고 답이 *"상태가 아니라 범위"* 였다는
    사실 · ⓑ(달이 갈리는 창)의 오늘 답.
    ⓓ **T-5 갱신 한 줄**: 리뷰 S-5가 넘긴 모집단 결정이 내려졌다는 사실.
    ⓔ **Q절·`launch-readiness-status.md` 낡은 판정 둘의 정정**(선행 확인 4·5) — ⚠️ **이유를 함께
    적는다**: 닫힌 것을 닫혔다고 적지 않으면 다음 사람이 그 문단을 근거로 같은 일을 다시 한다.
    ⓕ 접근성 표: 라운드 80분을 **A-21**로 세울 것(⚠️ **실기기 항목** — 뮤테이션 실패 열셋의
    TalkBack/VoiceOver 자동 낭독 · **실기기 항목** — `/budget` 딥링크가 어느 화면에 착지하는가).
    ⓖ **C-3은 오늘로 열네 라운드 연속 미확인**이라는 사실을 갱신.
    ⓗ `runtime-verification-required.md`에 라운드 80 신설분을 **131번부터 편입**하고 §0의 네 수·
    합계·§1-1 머리말 라운드 구간을 함께 갱신할 것(⚠️ 라운드 75 C의 계약이 그 값을 파싱으로 다시
    세므로 틀리면 `@wooriai/test-utils`가 먼저 빨개진다. ⚠️ **트랙 A의 항목과 트랙 D의 `/budget`
    항목은 둘 다 `실기기`다**).
    ⓘ `launch-readiness-status.md`의 **테스트 건수 재실측**. ⚠️ **정찰은 이 수치를 적지 않는다** —
    어림 grep과 저장소의 파싱 계약이 세는 단위가 다르다(O-3). **F가 저장소의 방법으로 다시 센다.**
    ⚠️ 다만 정찰이 실측으로 확인한 인용 셋(**37 · 41 · 18**)은 오늘 그대로이므로 **그 칸은 손대지
    않는다**(선행 확인 3).

- **머지 순서**: **A·B·C·D·E는 서로 완전 독립**이고 즉시 병렬 가능하다 — A=모바일 화면 넷 +
  계약 하나, B=모바일 알림 모듈 하나 + 그 테스트 + 훅 + 홈 화면 **표현식 둘**, C=QA 스크립트 하나,
  D=모바일 계약 하나(**신설 · 제품 소스 0건**), E=`packages/test-utils` 계약 하나.
  **파일이 한 곳도 겹치지 않는다.**
  ⚠️ **A와 B는 같은 워크스페이스지만 층이 다르다**(A=`app/settings/**`·`app/import/**` +
  `a11y-contract.test.ts`, B=`src/notifications/**` + `app/(tabs)/index.tsx`) — **A가
  `app/(tabs)/index.tsx`를 한 글자도 열지 않는 것**이 그 분리의 조건이다.
  ⚠️ **접점 하나(읽기 방향)**: `a11y-contract.test.ts`(A 소유)는 `app/(tabs)/index.tsx`를 소스로
  읽는다(`:1824`·`:2341`의 JSX 자리). **B가 여는 것은 `:1255`·`:1266`의 표현식 둘이라 그 자리와
  겹치지 않지만**, B는 머지 전에 그 사실을 확인한다(T-2가 세운 *"핀을 함께 낮추는 걸음"* 규율).
  ⚠️ **E는 A·B·C·D가 더하는 새 `indexOf` 자리와 무관하다** — 다섯 트랙의 공통 금지(*"새 자리에는
  예외 없이 실재 확인"*)가 지켜지면 대장 수치가 움직이지 않는다.
  **A를 먼저 머지한다** — 되돌릴 수 없는 흐름 넷의 실패가 오늘 **소리로 오지 않는 유일한 자리**
  이고(같은 화면이 성공은 읽어 준다), 라운드 79가 세운 도달의 축을 한 겹 넓히는 첫 트랙이다.
  그다음이 **B**(핵심 루프의 재진입 유도 둘이 조용히 죽는 자리이고, 여는 제품 소스가 표현식
  둘로 가장 작다). **C·D·E는 아무 때나**(가장 독립적이다. ⚠️ **D는 계약이 *주장하지 않는다*는
  경계를 지키는 것이 성패다** — `/budget`이 어느 화면에 착지하는지는 확인의 표가 답한다).
  **F는 마지막이고, 이번 F는 U절 다섯 판정 · T-1/T-2/T-5 갱신 세 줄 · 낡은 판정 둘의 정정 ·
  A-21(실기기 둘) · C-3 열네 라운드 표기 · 확인의 표 131번부터 편입과 §0 재계산 ·
  테스트 건수 재실측이 본체다.**
