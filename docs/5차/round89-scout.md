# 라운드 89 정찰 노트 (GAP-089)

> master `cedaba4`(라운드 88 머지) 기준 · 2026-08-31 실측. do-not-change.md(DNC-001~020) ·
> known-limitations **A~AC절**(AC-1~AC-5 전문 + AC절 머리말의 **결정형 재개 조건 열 전수표** 포함 ·
> 문서 **6,285줄** · AC절 `:5842~6285`) · runtime-verification-required.md(§0·§1-1 · **161행**) ·
> accessibility-offline-checklist.md(**A-29**까지 · C절 C-3 **스물두 라운드**) ·
> round56~88-scout 완료분 대조 완료.
>
> ⚠️ **이 라운드의 첫 의무는 관례가 정한 자리에 있다** — AC-1~AC-5가 각 절 끝에 남긴
> *"다음 라운드가 먼저 세어 볼 만한 것"* **다섯**을 오늘 실측으로 답하는 것, 그리고 **수를 낼 때
> 그 수를 어떻게 냈는지(모집단·바늘·사각)를 함께 적는 것**(AC-5). 순서를 이렇게 잡았다:
> ① AC-1~AC-5의 질문 다섯을 전수로 세고(각각 **모집단을 먼저** 정하고 그다음 바늘),
> ② AC절 머리말의 **결정형 재개 조건 열 전수표**를 다시 세어 전제가 참인 것을 고르고
>    (특히 라운드 88 F가 *"다음 라운드 몫"* 으로 예고한 **소스에 사는 결정형 둘**),
> ③ 라운드 88 F가 **보고에만 남긴 자리**(접근성 표 "수동 증거" 절의 C-3 경과 수)를 실측하고,
> ④ 라운드 88 리뷰가 고치고 남긴 것들(L-1의 사각 · C의 사각 164 · D의 사각 셋)을 재실측하고,
> ⑤ 그다음 미개척 표면을 훑었다(어드민 표의 이름 · 초대 수락 화면의 낭독 출구 · 문서를 읽어
>    지키는 계약의 전수).
>
> **관측은 하나로 모인다 — 라운드 88이 *"계약이 옆 자리의 결함을 바이트로 못 박고 있었다"* 를
> 닫았고, 오늘 세어 보니 그 한 칸 옆에 더 조용한 것이 있다:**
> **판정이 값으로 서고도 그 판정이 가리킨 자리는 아무도 고치지 않았고, 판정을 옮겨 적은 문서의
> 두 자리 중 하나는 세 라운드째 낡아 있었다.**
> 다섯 후보가 이렇게 갈린다(⚠️ **앞 둘은 사람이 오늘 보고 듣는 자리이고, 뒤 셋은 그 자리를
> 다시 낡게 두지 않는 기계다**):
> ① **라운드 88 트랙 E가 판정으로 처음 값이 되게 한 그 한 자리**(끝난 초대 카드가 iOS에서
>    소리로 안 나간다)가 **오늘도 그대로다** — 그 줄의 재개 조건은 *사건형*이고
>    *"이 카드를 여는 트랙이 서는 날"* 이다(후보 1 · AC-4의 답이 만든 자리 · 접근성 C-12).
> ② **라운드 88 트랙 A가 추이 표 하나에 이름을 주자, 같은 화면의 형제 표들에 이름이 0건이라는
>    사실이 처음 보인다** — 어드민 표 **열일곱** 중 이름을 가진 것은 **둘**뿐이다
>    (후보 2 · AC-1·AC-2의 답이 같은 자리에서 만난 곳).
> ③ **라운드 88 F가 예고한 소스의 결정형 둘 중 하나의 전제가 오늘 참이다** —
>    `export const` 축 **652 중 24**가 호출부 0건인데 그 축이 모집단 밖이고, 들이는 판정이
>    *"계약 전용 데이터 모듈을 뿌리에서 가르는 것"* 이다(후보 3 · AC절 표의 10).
> ④ **AA-3의 "형을 괄호로 밝히는 관례" 기계의 재개 조건이 오늘 도래했다** — 그 조건은
>    *"F가 절을 먼저 쓰고 다음 라운드가 그 계약을 세우는 순서"* 였고 **라운드 88 F가 AC절을
>    먼저 썼다**(후보 4 · AC-3의 답 · 순서까지 적힌 재개 조건이 실제로 집히는 첫 사례).
> ⑤ **접근성 표의 C-3 경과 수가 두 자리에 있고, 한 자리가 세 라운드 낡았다** — C절은
>    **스물두**(67~88)인데 "수동 증거" 절은 **열아홉**(67~85)에 멈춰 있다(후보 5 · 라운드 88 F가
>    보고에만 남긴 그 자리 · AC-5의 답이 그 자리에 계약이 0건임을 보인다).

## 선행 확인 (후보 아님)

1. **라운드 88의 다섯 트랙 + F는 전부 머지돼 있다**(실측 · 파일 실재와 값 둘 다 확인).
   A=`apps/admin/app/clicks/page.tsx`(`:206` `{trend.showTable ? (` · `:208` 추이 표
   `aria-label={\`최근 ${summary.days}일 일별 클릭 수 표\`}` · `:236` `{trend.peakSentence ? …}` ·
   `:242` 각주 갈래) + `apps/admin/src/lib/analytics-trend-view.test.ts`(두 화면 루프),
   B=`apps/mobile/src/notifications/usePushDeviceRegistration.ts:100` `buildDeviceRegistrationBody`
   (한 벌) + `apps/mobile/app/settings/notifications.tsx:118`이 그것을 부른다,
   C=`packages/test-utils/src/comment-tolerant-anchor-ledger.ts`(+`.test.ts`) — **신설 그물 열넷째**
   (`CONTRACT_NET_COUNT_WITH_THIS_ONE = 14`),
   D=`packages/test-utils/src/dead-export-ledger.ts`(**1,268줄** · `.test.ts` **681줄**) — 주석 마스킹,
   E=`apps/mobile/src/a11y-contract.test.ts`의 `ROUND79_ANNOUNCE_PROPS_ADDED:3549`·
   `ROUND80_ANNOUNCE_PROPS_ADDED:4507`에 **`crossPlatform` 판정 칸**,
   F=known-limitations **AC절**(`:5842~6285` · 문서 6,285줄) + 짝 문서 둘(#160~#161 · A-29 · C-12).
   **재제안 대상 아님.**

2. **타입 검사는 오늘 초록이다** — `npx tsc --noEmit`을 `apps/admin`·`apps/mobile` 두 tsconfig에
   돌려 **둘 다 exit 0**. ⚠️ **테스트는 돌리지 않았다**(이 정찰의 금지 조항) — 그래서 이 노트는
   *"오늘 초록이다"* 를 계약에 대해 주장하지 않는다.

3. ⚠️⚠️ **AC-5 규율 이행 — AC-1~AC-5가 남긴 질문 다섯 전수에 오늘 답한다**(라운드 84가 만든 고정 절차).
   ⚠️ **답마다 *그 수를 어떻게 냈는지*를 함께 적는다**(아래 4~8이 절차를 진다).

   | # | 자리 | 질문 | 2026-08-31 실측 답 | 발동 |
   | --- | --- | --- | --- | --- |
   | 1 | AC-1 `:6156` | 이 저장소의 계약 중 **한 화면·한 파일만 무는 단언**이 몇이고, 그중 **형제가 실재하는 것**이 몇인가 | 화면 이름을 문자열로 무는 계약 **183** 중 **한 화면만 무는 것 83 · 둘 이상을 함께 무는 것 100** · ⚠️ 그 83 중 **시험 대상 모듈이 화면 둘 이상에 실려 형제가 실재하는 것은 일곱**(아래 4) | ⚠️ **예 — 그러나 그 일곱에 오늘 살아 있는 결함은 0건**이다. 발동한 것은 **후보 2**(같은 축의 다른 모양: 표 이름) |
   | 2 | AC-2 `:6186` | **같은 API를 부르는 호출부가 둘 이상인 자리**가 몇이고, 그중 **보내는 필드 집합이 갈리는 것**이 몇인가 | 모바일 `client.ts` 함수 58 중 데모 거울(`local-backend.ts`)을 뺀 **실 호출부 둘 이상이 20** · 본문 객체를 두 자리 이상에서 세우는 것 **둘** · **갈리는 것 하나**(`updateChild` — PATCH라 의도된 부분 갱신) · 어드민 `admin-api.ts` 함수 48 중 호출부 둘 이상 **15** · 갈리는 것 **둘**(둘 다 `entityId` 선택성·필터 축이라 의도된 것) (아래 5) | **아니오 — 0건이다.** 라운드 88 B가 닫은 그 모양은 오늘 재발이 없다(**0건도 재어 본 값이다**) |
   | 3 | AC-3 `:6219` | 이 저장소의 재개 조건 중 **집을 때의 순서를 함께 적어 둔 것**이 몇인가 | 모집단(known-limitations + `apps/**`·`packages/**`의 ts/tsx + `docs/qa/**`)의 *"재개 조건\|재개 트리거"* 등장 자리 **278** 중 조건 문장 자체가 순서·선행을 함께 적은 것 **열셋(기계) → 여덟(손 판정)** · ⚠️⚠️ **그 여덟 중 하나의 순서가 오늘 도래했다**(AA-3 기계 — *"F가 절을 먼저 쓰고 다음 라운드가 계약을 세우는 순서"*) (아래 6) | ⚠️⚠️ **예 → 후보 4** |
   | 4 | AC-4 `:6253` | 이 저장소의 대장 중 **판정 칸이 있고 그 판정이 모집단에서 파생하는 것**이 몇이고, **손으로 적힌 것**이 몇인가 | 대장꼴 상수(`const NAME = [{…}]` · 항목 키 둘 이상) **75** 중 이유·한 일 칸을 진 것 **40** · **판정 칸을 진 것 둘**이고 **둘 다 모집단에서 파생**한다(라운드 88 E가 세운 `ROUND79/80_ANNOUNCE_PROPS_ADDED`) · **손으로 적힌 판정 0건** (아래 7) | ⚠️ **예 — 그러나 트랙이 아니라 *그 판정이 가리킨 자리를 고치는 것*이 오늘의 값이다 → 후보 1** |
   | 5 | AC-5 `:6282` | 이 저장소가 **문서를 읽어 지키는 계약**이 몇이고 **그 문서들이 어느 것인가** | 계약 파일 **열**과 스크립트 둘이 문서를 읽고, 지켜지는 문서는 **스물여섯**이다(아래 8) · ⚠️⚠️ **`docs/qa/accessibility-offline-checklist.md`를 무는 단언은 존재 확인 하나와 `toContain("44px")` 하나뿐**이고, **`docs/operations/known-limitations.md`를 무는 단언은 N-4 문턱 한 줄 하나뿐**이다 | ⚠️⚠️ **예 → 후보 4·후보 5**(두 문서가 정확히 오늘 낡은 두 자리다) |

   ⚠️ **다섯 중 넷이 발동했고 하나(AC-2)는 0으로 닫혔다.** ⚠️⚠️ **그리고 다섯 중 셋이 같은 곳을
   가리킨다**: AC-4가 판정으로 세운 자리(후보 1) · AC-5가 *"그 문서에는 계약이 없다"* 로 답한 두
   문서(후보 4·5). **판정을 세운 라운드의 다음 라운드가 할 일은 그 판정을 고치는 것과, 그 판정이
   사는 문서가 조용히 낡지 않게 하는 것 둘이다.**

4. ⚠️⚠️ **AC-1의 답 — 한 화면만 무는 계약 83 · 형제가 실재하는 일곱 · 살아 있는 결함 0건.**
   **모집단을 먼저 정했다**: `apps/**`·`packages/**`의 `.test.ts(x)` 전수 중 **화면 파일의 상대
   경로를 문자열 리터럴로 적는 것**(어드민 화면 11 = `app/**/page.tsx` · 모바일 화면 38 =
   `app/**/*.tsx`). **바늘**: `"app/…/page.tsx"`·`"app/….tsx"` 꼴 리터럴을 실재 화면 목록과
   접미 일치로 맞춘다(node 스크래치). **결과: 화면을 하나라도 무는 계약 183 · 그중 한 화면만
   무는 것 83 · 둘 이상을 함께 무는 것 100.**
   **형제의 정의**(두 번째 바늘): 그 계약 파일의 **같은 이름 형제 모듈**(`foo.test.ts` → `foo.ts`)이
   **화면 둘 이상에 import되어 있는데** 계약은 화면 하나만 무는 자리. **일곱이다**:
   - `apps/admin/src/lib/audit-log-filters.test.ts` — 문다: `app/audit-logs/page.tsx` ·
     모듈이 사는 형제: `app/users-lookup/page.tsx`
   - `apps/mobile/src/children/household-join.test.ts` — `app/family/accept/[token].tsx` ↔ `app/(auth)/login.tsx`
   - `apps/mobile/src/consent/legal-links.test.ts` — `app/(auth)/login.tsx` ↔ `app/settings/privacy.tsx`
   - `apps/mobile/src/expenses/draft-storage.test.ts` — `app/expenses/new.tsx` ↔ `app/settings/privacy.tsx`
   - `apps/mobile/src/expenses/recent-items.test.ts` — `app/expenses/new.tsx` ↔ `app/(tabs)/index.tsx`
   - `apps/mobile/src/expenses/text-limits.test.ts` — `app/expenses/[expenseId].tsx` ↔ `app/expenses/new.tsx`
   - `apps/mobile/src/offline/expense-list-reconciliation.test.ts` — `app/(tabs)/records.tsx` ↔ `app/(tabs)/index.tsx`
   ⚠️ **일곱을 손으로 열어 형제 쪽을 확인했고 살아 있는 결함은 0건이다**: 글자 상한은 두 화면이
   **똑같이** 배선돼 있고(`new.tsx:1427-1431` ↔ `[expenseId].tsx:537-541`), 재조정 모듈은 두 화면이
   같은 함수를 지나며(`records.tsx:996` ↔ `index.tsx:152` — 홈의 주석이 그 사실을 스스로 적는다),
   `recent-items`의 라벨 둘(`formatRecentItemChipLabel`·`recentItemChipAccessibilityLabel`)은
   홈이 읽지 않지만 **홈의 칩은 금액을 그리지 않으므로** 읽을 값이 아니다,
   `audit-log-filters`는 형제 화면이 `auditLogsHrefForActor` **하나만** 가져간다.
   ⚠️ **사각 둘을 함께 적는다**: ⓐ 이 바늘은 **경로 문자열을 적지 않는 계약**(헬퍼 상수·변수로
   경로를 만드는 것)을 못 본다 — `readFileSync`를 쓰는 계약 232 중 화면 리터럴이 0건인 것이
   많다. ⓑ **형제의 정의를 *같은 이름 모듈*로 좁혔다** — 다른 이름의 형제(라운드 88 A가 만난
   `analyticsTrendView` 같은 공유 뷰 모듈)는 이 수에 들지 않는다. ⚠️ **그래서 일곱은 하한이다.**
   ⚠️⚠️ **그리고 이 질문의 진짜 답은 후보 2에 있다** — 라운드 88 A가 *"한 화면을 무는 단언"* 을
   *"두 화면을 도는 루프"* 로 바꾼 그 자리에서, **고친 것은 계약의 모양이고 화면에 실제로 준 것은
   표 이름 하나**였다. **그 이름을 형제 표들은 오늘도 0건 갖고 있다**(아래 9).

5. ⚠️ **AC-2의 답 — 호출부 둘 이상이 스물(모바일)·열다섯(어드민)이고, 오늘 갈리는 것은 0건이다.**
   **모집단**: `apps/mobile/src/api/client.ts`의 `export function` **58**과
   `apps/admin/src/lib/admin-api.ts`의 `export function` **48**. **바늘**: 앱 안의 비테스트
   `.ts(x)` 전수에서 그 이름의 호출 자리를 찾고, 괄호 균형을 세어 인자를 잘라 **최상위 키만**
   집계한다(중첩 `payload: { … }` 안의 키를 세면 갈리지 않는 것이 갈려 보인다 — 첫 실측에서
   실제로 그렇게 잘못 나왔고, 괄호 깊이를 세어 정정했다).
   ⚠️ **모집단에서 `apps/mobile/src/api/local-backend.ts`를 뺐다** — 그 파일은 같은 이름을
   *다시 구현하는 데모 거울*이지 호출부가 아니다(빼지 않으면 함수 45가 "호출부 둘 이상"으로
   잡히고 그 수는 아무것도 말하지 않는다).
   - **모바일**: 실 호출부 둘 이상 **20** · 그중 본문 객체를 두 자리 이상에서 세우는 것 **둘**
     (`updateChild`·`listCategories`) · **키 집합이 갈리는 것 하나** — `updateChild`
     (`app/settings/children.tsx:461`은 `transitionToStageMode` 하나,
     `src/onboarding/child-create.ts:56`은 `birthDate,dueDate,manualStage`). **PATCH의 부분 갱신이라
     의도된 갈림이고 결함이 아니다.**
   - **어드민**: 호출부 둘 이상 **15** · 갈리는 것 **둘** — `listContentRevisions`(상태 필터 ↔
     대상 필터)와 `draftAndSubmitContentRevision`(호출 여섯 · 집합 둘 — `entityId`의 유무가
     **새로 만들기 ↔ 고치기**를 가른다. 시그니처가 `entityId?`로 그 선택성을 이미 적는다).
   - ⚠️ **라운드 88 B의 자리를 재실측했다**: `registerDevice`는 오늘 **한 벌**(`buildDeviceRegistrationBody`)을
     두 경로가 공유하고, 화면 쪽 호출은 `app/settings/notifications.tsx:118` 한 줄이다. **닫힌 채로 있다.**
   ⚠️ **사각**: 이 바늘은 **객체 리터럴을 직접 적는 자리**만 센다 — 변수로 만든 본문을 넘기는
   호출(오프라인 큐의 `createExpense(mutation.payload …)` 같은 자리)은 키를 못 본다.
   **오차의 방향은 갈림을 놓치는 쪽**이다.

6. ⚠️⚠️ **AC-3의 답 — 재개 조건 278자리 중 순서를 함께 적은 것 여덟, 그중 하나의 순서가 오늘 도래했다.**
   **모집단**: `docs/operations/known-limitations.md` + `apps/**`·`packages/**`의 `.ts(x)` 전수 +
   `docs/qa/**`의 `.md`. **바늘**: *"재개 조건"* 또는 *"재개 트리거"* 가 나오는 자리마다 그
   지점부터 다음 줄까지를 이어 붙여 **조건 문장 하나**를 자르고, 그 문장 안에서만
   *먼저·그 전에·선행·그다음·순서·배워야·앞서* 를 찾는다(줄 전체나 문단으로 넓히면 산문이
   섞여 66이 나온다 — **넓은 창의 수를 이 답으로 적지 않는다**).
   **기계가 낸 수는 열셋**이고, 손으로 갈라 보면 **여덟**이 진짜다(나머지 다섯은 *질문 자체*(`:6219`) ·
   *세는 방법의 설명*(`:5942`) · *계약의 설명*(`runtime-verification-required.md:1113`) 등이다).
   여덟은 이렇다:
   - `known-limitations.md:3427` — N-4(*"다시 제안하려면 새 실측이 **먼저** 있어야 한다"*) · **미도래**
   - `known-limitations.md:3639` — N-4와 같은 값(*"조건이 도래하면 **먼저** 물을 것은…"*) · **미도래**
   - `known-limitations.md:4010` — 링크 헬스(*"그날 **먼저** 정할 것은 타임아웃이 아니라 배치 크기"*) · **미도래**
   - `known-limitations.md:4795` — *"순서상 호출부를 세는 기계가 주석의 근거를 세는 대장보다 먼저"* ·
     **도래·소진**(라운드 88 D가 그 순서로 집었다)
   - ⚠️⚠️ `known-limitations.md:6077` — AA-3 기계(*"문서 축을 무는 스윕을 F 밖의 트랙이 소유할 수 있게
     되는 날 — 예컨대 **F가 절을 먼저 쓰고 다음 라운드가 그 계약을 세우는 순서**"*) · **오늘 도래했다**
     (라운드 88 F가 AC절을 먼저 썼다) → **후보 4**
   - `dead-export-ledger.ts:382-383` — 회고(*"먼저 마스킹, 그다음 주석"*) · **도래·소진**
   - `dead-export-ledger.ts:1225` — 문자열 축(*"…0을 넘는 날 — 그날 이 그물은 문자열도 마스킹해야
     한다"*) · **미도래**(오늘 실측 = 참조가 전부 문자열뿐인 export **0건**)
   - `accessibility-offline-checklist.md:1040` — C-12(*"그날 **판정과 확인 중 무엇이 먼저였는지**가
     함께 적힌다"*) · **오늘 도래한다면 후보 1이 그 날이다**
   ⚠️ **AC-3의 일반형이 오늘 값을 냈다**: 여덟 중 **둘은 이미 소진됐고 하나는 오늘 도래했다** —
   즉 **순서까지 적힌 조건은 실제로 집힌다**(순서가 없는 조건 스물넷 중 오늘 집히는 것은 0건이다).

7. ⚠️⚠️ **AC-4의 답 — 대장꼴 상수 75 중 판정 칸은 둘이고, 둘 다 파생이며 손으로 적힌 판정은 0건이다.**
   **모집단**: `apps/**`·`packages/**`·`scripts/**`의 `.ts(x)` 전수에서 `(export )?const 대문자이름
   … = [ … ]` 꼴로 **객체 배열**이고 **항목 키가 둘 이상**인 상수 — **75**(⚠️ 라운드 88의
   *"마흔여덟"* 과 바늘이 다르다: 그 라운드는 `export const`만 셌고 오늘은 테스트 파일 안의
   `const` 대장까지 센다. **두 수를 한 낱말로 적지 않는다**).
   - **이유/한 일 칸(`reason`·`why`·`what`·`statement`·`note`)을 진 것 40.**
   - ⚠️⚠️ **판정 칸(*그래서 충분한가*)을 진 것 둘** — `apps/mobile/src/a11y-contract.test.ts:3549`
     `ROUND79_ANNOUNCE_PROPS_ADDED`와 `:4507` `ROUND80_ANNOUNCE_PROPS_ADDED`의 `crossPlatform`.
     **둘 다 모집단에서 파생한다**(`announceLedgerPlacesOf`가 항목의 `after` 바이트에서 출구를
     뽑고, 그 파생이 부를 때만 이 칸이 값이 된다). **손으로 적힌 판정은 0건이다.**
   - **나머지 73은 판정 칸이 아예 없다** — 가장 많은 곳은 `shared-cache-policy.ts`(9) ·
     `dead-export-ledger.ts`(6) · `comment-tolerant-anchor-ledger.ts`(5) ·
     `a11y-contract.test.ts`(4) · `data-retention-promise.test.ts`(4).
   ⚠️ **사각**: ⓐ 이 바늘은 **`Record`·`Map`꼴 대장**(`NON_MODULE_ANNOUNCE_SOURCE_ROOTS` 같은
   객체 대장)을 못 본다. ⓑ **`floor`+`measure` 쌍**(사문 대장·앵커 대장의 사각 칸)은 판정
   *이름*을 갖지 않아 이 수에 들지 않는데, 성질로는 파생 판정에 가깝다 — **그래서 둘은 하한이다.**
   ⚠️⚠️ **그리고 이 답의 값은 수가 아니라 그 둘이 가리킨 자리에 있다**: 라운드 88 E의 파생 판정이
   `crossPlatform`을 **비어 있지 않게** 만든 항목은 **오늘도 하나**이고(실행으로 확인),
   그 하나가 **끝난 초대 카드**다 → **후보 1**.

8. ⚠️⚠️ **AC-5의 답 — 문서를 읽어 지키는 계약은 열이고, 지켜지는 문서는 스물여섯이다.**
   ⚠️ **Y-5가 라운드 84에 남긴 뒤 오늘까지 전수로 답해진 적이 없는 질문이다**(AC-5가 그 사실을
   적으며 같은 모양으로 다시 세웠다).
   **모집단**: `apps/**`·`packages/**`·`scripts/**`의 `.ts(x)` 전수. **바늘**: 파일이
   `readFileSync`/`existsSync`를 쓰면서 **주석을 지운 코드 안에** `.md` 문자열 리터럴이 있는 것
   (주석을 지우지 않으면 *"이 문서를 보라"* 는 안내 주석이 계약으로 잡혀 18로 부푼다 —
   실제로 첫 실측이 그랬다).
   **계약 열**: `apps/admin/src/lib/catalog-size-view.test.ts` ·
   `apps/mobile/src/eas-cloud-build-profiles.test.ts` · `apps/mobile/src/ui-pixel-lock-flow.test.ts` ·
   `packages/test-utils/src/{dnc-guard-ledger,dnc-scope-guard,dnc-secret-scan}.ts` ·
   `packages/test-utils/src/{release-readiness,repo-self-description,runtime-checklist-shape,store-brand-and-asset-provenance}.test.ts`.
   **스크립트 둘**(계약이 아니라 도구): `scripts/ui-pixel-lock.ts` · `scripts/pixel-lock/android-pixel-lock.ts`.
   **지켜지는 문서 스물여섯**: `docs/dev/do-not-change.md`(**계약 다섯이 읽는다 — 가장 두껍다**) ·
   `docs/qa/runtime-verification-required.md`(둘) · `docs/qa/{manual-runbook,release-checklist,rollback-plan,accessibility-offline-checklist,test-coverage,functional-verification-report,completion-audit}.md` ·
   `docs/qa/evidence/{latest-release-gate,release-owner-evidence-template}.md` ·
   `docs/store/{play-listing,submission-checklist,data-safety-answers}.md` ·
   `docs/ui-pixel-lock/{reference-analysis,asset-extraction-report,visual-qa-report,mismatch-log}.md` ·
   `docs/ui-pixel-lock/reports/ui-pixel-lock-final-report.md` ·
   `docs/operations/{known-limitations,incident-response,release-runbook}.md` ·
   `docs/5차/{launch-readiness-status,oracle-free-deploy-runbook,day1-deploy-runbook}.md` ·
   `README.md` · `AGENTS.md` · `CODEX_START_HERE.md` · `docs/4차/prompts/04_do_not_change_v0_4.md` ·
   `apps/mobile/…/apk-build-guide.md`.
   ⚠️⚠️ **그리고 이 답의 값은 *무엇을 무는가*에 있다.** 그 스물여섯 중 **판정 문서 둘**을 무는
   단언은 이렇게 얇다:
   - `docs/operations/known-limitations.md` ← **`catalog-size-view.test.ts:23-25` 한 자리**
     (*N-4의 문턱 200을 인용한 문장 하나*). **6,285줄 중 계약이 아는 것은 그 한 줄뿐이다.**
   - `docs/qa/accessibility-offline-checklist.md` ← **`release-readiness.test.ts:80·113`**
     (*파일이 존재한다* + *`"44px"`가 들어 있다*). **1,082줄 중 계약이 아는 것은 낱말 하나다.**
   ⚠️ **그래서 후보 4와 후보 5가 같은 답에서 나온다** — 오늘 낡은 두 자리가 정확히 그 두 문서다.
   ⚠️ **사각**: ⓐ 문서 경로를 상수로 조립하는 계약은 이 바늘이 놓칠 수 있다(오늘은 놓친 것이
   확인되지 않았다 — 열 전부가 리터럴을 코드에 갖고 있다). ⓑ `apps/api/**`의 e2e는 세지 않았다
   (실 DB가 필요해 이 정찰이 돌리지 않았다).

9. ⚠️⚠️ **후보 2의 실측 — 어드민 표 열일곱 중 이름을 가진 것은 둘이다.**
   `apps/admin/app/**` 전수에서 `<table` 자리를 세면 **열일곱**이고, `aria-label`이 붙은 것은
   **둘**(`app/clicks/page.tsx:208` · `app/analytics/page.tsx:531` — **둘 다 라운드 88 A가 만든
   추이 표**)이다. 나머지 **열다섯**은 이름이 0건이다:
   `app/users/page.tsx:325` · `app/audit-logs/page.tsx:384` · `app/reviews/page.tsx:365`·`:441`·`:534` ·
   `app/categories/page.tsx:230` · `app/items/page.tsx:722` · `app/users-lookup/page.tsx:164`·`:227` ·
   `app/clicks/page.tsx:123`·`:150` · `app/links/page.tsx:639` ·
   `app/analytics/page.tsx:338`·`:417`·`:469`.
   ⚠️⚠️ **한 화면에 이름 없는 표가 둘 이상 서는 자리가 넷이다** — `clicks`(둘) · `analytics`(셋) ·
   `reviews`(셋) · `users-lookup`(둘). **스크린리더로 표를 훑는 운영자는 그 화면에서 "표"를
   두세 번 듣는다.** ⚠️ **그리고 그 표들 위에는 이미 `<h2>`가 서 있다**(예: `clicks:118`
   *"플랫폼별 클릭 수"* · `:145` *"상위 상품 링크 (최근 N일)"* · `analytics:336` *"KPI 퍼널"* ·
   `:468` *"이벤트별 카운트"*) — **이름을 지을 재료가 화면에 이미 있고, 아무도 그것을 표에
   연결하지 않았다.**
   ⚠️ **AC-1·AC-2가 같은 자리에서 만난다**: 라운드 88 A는 *한 화면을 무는 단언*을 *두 화면을 도는
   루프*로 바꿨고(AC-1), 그 루프가 **추이 표 둘**에 이름을 세웠다 — 그런데 **같은 화면의 형제
   표들은 그 질문을 한 번도 받지 않았다**(AC-2의 *"형제를 나란히 놓아야 보인다"*).
   ⚠️ **이 규율에는 오늘 모집단이 0건이다** — `analytics-trend-view.test.ts`가 무는 것은 **추이
   표 둘**뿐이고, *"어드민의 표는 이름을 갖는가"* 를 `app/**` 전수에서 묻는 스윕은 없다.

10. ⚠️⚠️ **후보 1의 실측 — 끝난 초대 카드는 오늘도 크로스플랫폼 출구 0건이다.**
    `apps/mobile/app/family/accept/[token].tsx:325` `{inviteUnavailable ? (` 아래 `:326`
    `<View accessibilityLiveRegion="polite" accessibilityRole="alert">` — **프롭 조합만** 있고
    그 조건에 묶인 `announceForA11y`가 이 화면에 **0건**이다(같은 화면 `:218-223`의 effect는
    `accept.isError && !inviteUnavailable` 갈래라 **이 카드가 설 때는 돌지 않는다**).
    ⚠️ **판정을 다시 돌려 확인했다**: `ROUND79_ANNOUNCE_PROPS_ADDED`·`ROUND80_ANNOUNCE_PROPS_ADDED`
    아홉 항목 중 `crossPlatform`이 **비어 있지 않은 것은 오늘도 하나**이고, 그 하나가
    `a11y-contract.test.ts:3574`(이 카드)다.
    ⚠️ **그 이유 칸은 자리 **둘**을 진다**: ⓐ 끝난 초대 카드(`inviteUnavailable`) — **안드로이드 한정**,
    ⓑ 뒤처리 실패 카드(`joinRetryNotice && joinedResult`) — **두 플랫폼 다 소리가 나지만** 낭독이
    effect가 아니라 핸들러 안에 있어 이 그물이 `announce`로 세지 않는다(사각 넷째).
    ⚠️⚠️ **그래서 이 트랙이 고칠 것은 ⓐ 하나이고, ⓑ는 오늘도 이유 칸에 남는다** — 판정 칸의
    계약이 *"조용한 자리의 조건 이름이 이유에 들어 있어야 한다"* 를 물기 때문에, 고친 뒤 이유
    칸은 **ⓑ만 말하도록 다시 써야 한다**(낡은 이유가 남으면 그 계약이 빨개진다).
    문장 재료는 `apps/mobile/src/family/invite-accept-messages.ts:69`·`:81`에 이미 상수로 있다
    (`INVITE_UNAVAILABLE_TITLE` · `INVITE_UNAVAILABLE_NEXT_STEP`) — **새 한국어 문장 0건으로 지을 수 있다.**

11. ⚠️⚠️ **후보 3의 실측 — 소스에 사는 결정형 둘의 전제가 오늘 둘 다 참이다.**
    `packages/test-utils/src/dead-export-ledger.ts`의 사각 칸을 **실행해서** 다시 쟀다
    (`tsx`로 모듈을 불러 각 사각의 `measure(baseDir)`를 돌렸다 — **테스트 러너는 쓰지 않았다**).
    적힌 값과 오늘 실측이 **일곱 자리 전부 같다**:
    `export-const-axis` **24**(하한 8) · `contract-only-data-modules` **17**(8) · `common-name` **77**(20) ·
    `comment-and-string-references` **9**(5) · `string-literal-references` **26**(10) ·
    `tsx-components` **141**(80) · `outside-two-apps` **0**(0). `DEAD_EXPORT_LEDGER` **16** · 래칫 **16**.
    - **AC절 표의 10**(*"계약 전용 데이터 모듈을 뿌리에서 가르는 판정"* · `:1160`) — 전제 참.
      `export const` **652 중 24**가 호출부 0건이고 **그중 17이 다섯 모듈**
      (`offline/offline-aware-screens.ts` 일곱 · `query/shared-cache-policy.ts` 다섯 ·
      `offline/messages.ts` 셋 · `reports/empty-period-card.ts` 하나 · `settings/more-menu.ts` 하나).
      ⚠️⚠️ **남은 일곱이 이 결정의 실제 크기다**: `analytics/events.ts:73 ANALYTICS_CATEGORY_CODES` ·
      `expenses/failed-row-prefill.ts:52 FAILED_ROW_LOCAL_ID_PARAM` ·
      `import/import-failure-messages.ts:79 IMPORT_FAILURE_KINDS` ·
      `offline/sqlite-offline-store.ts:146 OFFLINE_DB_SCHEMA_VERSION` ·
      `settings/destructive-flow-messages.ts:151 DESTRUCTIVE_FLOW_ABSENT_TARGET_BRANCHES` ·
      `admin/src/lib/load-error-copy.ts:119 LOAD_ERROR_COPY_SITES`·`:146 LOAD_ERROR_COPY_EXEMPT_SITES`.
      ⚠️ **그 일곱 중 마지막 둘은 성질이 다섯 모듈과 같다**(계약이 읽는 표) — 즉 **뿌리에서
      가르는 판정이 서면 그 둘은 면제 쪽으로 파생되고, 남는 것은 다섯이다.** **그 다섯이 이
      트랙이 실제로 판단할 전부다.**
    - **AC절 표의 9**(*"JSX 사용을 참조로 세는 판정"* · `:1236`) — 전제 참(`.tsx`의 `export function`
      **141**이 모집단 밖). ⚠️ **오늘 집지 않는다 — 그물 배정이다**(같은 파일에 축 둘을 얹지
      않는다 · 라운드 85~87·88이 같은 자리에서 받은 경고). **후보 3이 그 파일을 연다.**

12. ⚠️⚠️ **후보 5의 실측 — 접근성 표의 C-3 경과 수가 두 자리에 있고 한 자리가 세 라운드 낡았다.**
    같은 파일 안의 두 자리를 파싱해 **열거된 라운드 목록의 길이**와 **한글 수사**를 대조했다:
    - `docs/qa/accessibility-offline-checklist.md:1031`(C절 C-3 행) — *"**스물두** 라운드 연속
      미확인"* · 목록 `67…88` = **22개** · **자기 안에서는 맞다.**
    - `docs/qa/accessibility-offline-checklist.md:1048`("수동 증거" 절) — *"**2026-08-30 기준
      열아홉** 라운드 연속 미확인"* · 목록 `67…85` = **19개** · **자기 안에서는 맞고, 끝이 85다.**
    ⚠️⚠️ **갈림은 3라운드(86·87·88)이고, 이 갈림은 라운드 86부터 세 라운드째 살아 있다.**
    ⚠️ **머리말의 옛 문단들(`:199` 넷 · `:229` 다섯 · `:262` 여섯)은 낡은 것이 아니라 그 라운드의
    기록이다** — 이 판정에 넣지 않는다(모집단은 *"오늘의 경과 수를 말하는 자리"* 둘이다).
    ⚠️ **그리고 그 문서를 무는 계약은 존재 확인과 `"44px"` 하나뿐이다**(위 8) — **그래서 이
    갈림은 어떤 계약도 보지 못했다.**

13. **AC절 머리말의 결정형 재개 조건 열을 다시 셌고, 오늘의 처분은 이렇다.**

    | # | 결정형 조건 | 사는 곳 | 손 | 오늘의 처분(2026-08-31 실측) |
    | --- | --- | --- | --- | --- |
    | 1 | 근거를 값으로 적는 관례(대장) | Z-1·AB절 | 안 | **라운드 88 D가 닫았다** — 이 표에서 빠진다 |
    | 2 | 막대를 포커스 가능하게 | AA-2 | 안 | **집지 않는다 — 값 0.** 두 화면 다 표가 값을 텍스트로 준다(재실측: `clicks:206`·`:208` · `analytics:529`·`:531`의 `trend.showTable` 갈래) |
    | 3 | 기록 탭 검색의 분류 갈래 | Z절 | 안 | **집지 않는다 — 어긋남 0건 · 네 라운드 연속 같은 답** |
    | 4 | 준비템 분류 필수 입력 | Z-3 | **밖**(카탈로그 정책) | 집지 않는다. 변화 0 |
    | 5 | 서버의 중복 대기 초대 방지 | AA-4 | **밖**(초대 정책) | 집지 않는다. 변화 0 |
    | 6 | 감사 뷰의 대상 축 필터 | AA절 기각 | **밖**(서버 DTO 0건) | 집지 않는다 — ⚠️ **어느 트랙도 `apps/api/**`를 쓰기로 열지 않는다** |
    | 7 | 타일 안 배지 | AA-1 | **밖**(디자인 승인) | 집지 않는다. 변화 0 |
    | 8 | C-3 잠금 오버레이 TalkBack 투과 | 접근성 표 C-3 | **밖**(사람·기기·날짜) | **트랙 F의 소유 밖이다** — 오늘로 **스물세 라운드 연속 미확인**. ⚠️ **후보 5가 이 줄에 하는 일은 확인이 아니라 *그 수가 두 자리에서 갈리지 않게 하는 것*이다** |
    | 9 | JSX 사용을 참조로 세는 판정 | `dead-export-ledger.ts`(소스) | 안 | **집지 않는다 — 그물 배정**(후보 3이 그 파일을 연다 · 한 트랙이 한 그물에 축 둘을 얹지 않는다). 전제는 참(141) |
    | 10 | 계약 전용 데이터 모듈을 뿌리에서 가르는 판정 | `dead-export-ledger.ts`(소스) | 안 | ⚠️⚠️ **집어 든다 → 트랙 C.** 전제 참(24/652 · 그중 17이 다섯 모듈 · 남는 판단은 다섯) |
    | + | AA-3의 표기 관례 기계 | AB절 기각 → AC절 기각(`:6077`) | 안 | ⚠️⚠️ **집어 든다 → 트랙 D.** **순서 조건이 오늘 도래했다**(F가 절을 먼저 썼다) |
    | + | 재조회 실패 창에서 옛 목록을 남길지 | AC절 기각(`:6048`) | 안 | **집지 않는다 — 화면 구조 결정이고 오늘 값 0건**(라운드 88이 배선 대장 전수로 0을 냈다) |

    ⚠️ **라운드 88 F가 예고한 대로 이 표의 모집단이 늘었다** — 그 라운드의 기각 목록이 새로 적은
    결정형 둘(AA-3 기계 · 조회 실패 창)이 오늘 전수에 들어왔고, 하나가 닫혀 빠졌다.
    **열 → 열하나(닫힌 하나 제외)이고, 그중 오늘 집는 것이 둘이다.**
    ⚠️⚠️ **이 표가 라운드 88의 표보다 값이 큰 이유 하나**: **집는 수가 하나에서 둘로 늘었고, 둘 다
    *순서가 먼저 적혀 있었기 때문에* 집혔다**(9는 순서 때문에 안 집고, 10과 AA-3 기계는 순서가
    도래해서 집는다). **AC-3이 세운 그 일반형의 첫 이행이다.**

14. **라운드 88 리뷰가 남긴 사각 셋을 재실측했다 — 셋 다 오늘도 실피해 0이고 후보가 아니다.**
    - **L-1**(a11y 판정이 최내곽 갈래 하나를 **글자로만** 본다) — 그 사각의 실재는 계약이 이미
      합성 소스로 증명한다(`a11y-contract.test.ts:5611-5640`). ⚠️ **오차의 방향이 거짓 빨강(안전)**
      이고, 오늘 저장소에서 낸 피해는 **0건**(자리 스물둘 중 중첩 여덟이 전부 최내곽 갈래로 맞는다).
      ⚠️⚠️ **다만 후보 1이 이 사각의 소비자가 된다** — 새로 다는 effect의 `if` 조건은 카드의
      최내곽 갈래(`inviteUnavailable`)와 **글자가 완전히 같아야** `announce`로 떨어진다.
      **그 사실을 트랙 A의 계약 조항으로 못 박는다.**
    - **C의 사각 `helper-named-reader` 164** — 다시 재니 하한 그대로다(앵커 전수 **698** ·
      code-only **620** · comment-tolerant **70**(래칫 70) · comment-only **8**(래칫 8) ·
      unanchored **0** · 면제 8 · 그물 수 **14**). ⚠️ **그 164 안에
      `apps/admin/src/lib/analytics-trend-view.test.ts`가 있다**(라운드 88 A가 고친 파일).
      ⚠️ **오늘 집지 않는다**: 그 사각을 열면 모집단이 크게 넓어져 `comment-tolerant` 래칫 70이
      **첫날부터 전수로 다시 적혀야** 하고, 그것이 라운드 85 E·86 E·87 E·88 C가 같은 자리에서
      받은 경고다. ⚠️ **재개 조건은 그 대장이 적어 둔 그대로**(*"저장소가 소스 리더 이름을 한
      벌로 모으는 날, 또는 이 대장이 헬퍼 정의를 따라가 루트를 푸는 법을 배우는 날"*).
    - **D의 사각 셋** — `string-literal-references` **26**(참조가 전부 문자열뿐인 export **0건** →
      재개 조건 미도래) · `export-const-axis` **24/652** · `contract-only-data-modules` **17**.
      ⚠️ **뒤 둘이 정확히 후보 3의 재료다**(위 11).

15. **이월 다섯 · 성능 넷 · 그 밖의 기각은 아래 P3에 재개 조건과 함께 값으로 있다.**
    ⚠️ **성능 넷 중 ⓑ(렌더 비용)의 활성 카탈로그 수는 오늘 재지 않았다** — 그 수는 실 PostgreSQL이
    필요하고 이 정찰은 읽기만 했다. ⚠️ **라운드 88의 62를 인용하지 않는다**(옮겨 적은 수는 계약
    밖의 사본이 된다 — O-3). **"오늘 재지 않았다"가 이 자리의 값이다.**

## 상위 후보

### 1. **끝난 초대 카드가 iOS에서는 아무 소리도 내지 않는다 — 판정은 라운드 88에 값이 됐고, 화면은 오늘도 그대로다** — 모바일·가족·접근성 — S

- **사실.** `app/family/accept/[token].tsx:325`의 `inviteUnavailable` 카드(만료·취소·이미 처리된
  초대 — 조회 404/400과 수락 400 셋이 함께 보는 **막다른 길**)에는 라운드 79가 건
  `accessibilityLiveRegion="polite"` + `accessibilityRole="alert"` **프롭 조합만** 있다. 이
  저장소 자신의 분류로 그 조합은 **안드로이드 한정**이다(앞은 `@platform android` 프롭이고
  뒤에는 VoiceOver의 대응 트레이트가 없다).
- ⚠️⚠️ **초대 링크를 열었는데 아무 일도 일어나지 않은 것처럼 들린다.** VoiceOver 사용자는 카드가
  선 것을 **훑어야만** 안다. 그리고 이 화면은 **링크를 타고 들어오는 화면**이라, 그가 무엇을
  해야 하는지(*"가족 관리자에게 새 초대 링크를 요청해 주세요"*)가 소리로 닿지 않는다.
- ⚠️ **이 자리가 저장 실패 스윕(GAP-079)의 모집단에 서지 않는 이유가 값으로 적혀 있다** — 그
  모집단은 *저장 실패 문구*의 이름으로 세는데 이 카드의 문장은 **초대 상태 안내 상수**다.
  **판정 칸이 없었으면 오늘도 아무 데도 적혀 있지 않았을 자리다**(AC-4의 이행).
- **고침의 모양.** effect 한 벌 + 모듈 함수 하나. `invite-accept-messages.ts`가 이미 가진 상수
  둘로 **낭독 문장을 짓고**(화면에 새 한국어 문장 0건), 화면은 `if (inviteUnavailable)` 갈래에서
  그것을 부른다. ⚠️ **프롭은 한 바이트도 건드리지 않는다**(대장의 `after`가 그 바이트다).
- **크기 S.** 화면 한 벌(effect) · 모듈 함수 하나 · 계약 둘.

### 2. **어드민 표 열일곱 중 이름을 가진 것은 둘이고, 그 둘은 라운드 88이 만든 것이다** — 어드민·접근성 — M

- **사실.** `apps/admin/app/**`의 `<table` **열일곱** 중 `aria-label`이 있는 것은 **둘**
  (`clicks:208` · `analytics:531` — 라운드 88 A의 추이 표). 나머지 **열다섯**은 이름이 0건이다.
- ⚠️⚠️ **한 화면에 이름 없는 표가 둘 이상 서는 자리가 넷이다**(`clicks` 둘 · `analytics` 셋 ·
  `reviews` 셋 · `users-lookup` 둘). 스크린리더로 표를 훑는 운영자는 **"표"를 두세 번 듣고**
  어느 것이 무엇인지 알 길이 없다 — 라운드 88 A가 추이 표에 이름을 준 이유가 정확히 그것이었고,
  **그 이유는 형제 표들에도 똑같이 참이다.**
- ⚠️ **이름을 지을 재료가 화면에 이미 있다** — 열다섯 중 대부분이 `<section className={styles.card}>`
  안에서 바로 위의 `<h2>`를 갖는다(`clicks:118`·`:145` · `analytics:336`·`:468` · `users:307` ·
  `categories:182` · `audit-logs:346` · `links:529` · `items:613`). **`<h2>`에 `id`를 주고 표가 `aria-labelledby`로 그것을 가리키면 새 한국어
  0글자다.** ⚠️ **h2가 없거나 h2가 표를 가리키지 않는 자리**(예: `users-lookup:164`의 h2는
  *사용자 이름*이고, `reviews:441`의 차이 표는 위에 h2가 없다)만 화면이 이름을 짓고,
  **그 자리 수를 값으로 적는다.**
- ⚠️ **이 규율에는 오늘 모집단이 0건이다**(AB-1이 이름 붙인 그 병 — `analytics-trend-view.test.ts`가
  무는 것은 추이 표 둘뿐이다). **그래서 이 후보의 절반은 화면이고 절반은 모집단이다.**
- **크기 M.** 어드민 화면 여덟 · 신설 계약 하나.

### 3. **`export const` 축 652 중 24가 호출부 0건인데 그 축은 모집단 밖이다 — 들이는 판정을 라운드 88이 소스에 적어 두었다** — 계약·모바일·어드민 — M

- **사실.** `dead-export-ledger.ts:1142`의 사각 `export-const-axis`가 *"같은 뿌리·같은 조건으로
  `export const` 축을 재면 652 중 24가 호출부 0건"* 이라고 적고, 그 칸이 **재개 조건(결정형 ·
  손은 안): 계약 전용 데이터 모듈을 뿌리에서 가르는 판정이 서는 날 — 그날 이 축이 모집단으로
  들어온다** 를 함께 적는다. **오늘 그 수를 실행으로 다시 냈고 24·17 그대로다.**
- ⚠️⚠️ **오늘 그 판정의 크기를 처음 잰다**: 스물넷 중 **열일곱**은 이미 다섯 모듈에 살고
  (`CONTRACT_ONLY_DATA_MODULES` — 손으로 적은 목록이다), 남은 **일곱** 중 **둘**
  (`admin/src/lib/load-error-copy.ts`의 `LOAD_ERROR_COPY_SITES`·`LOAD_ERROR_COPY_EXEMPT_SITES`)은
  성질이 그 다섯과 같다. **즉 판정이 뿌리에서 파생하면 면제는 열아홉이 되고, 사람이 판단할
  것은 다섯이다.**
- ⚠️ **그리고 그 판정은 손으로 적어서는 안 된다**(AC-4) — 오늘 `CONTRACT_ONLY_DATA_MODULES`가
  **경로 다섯과 이유 다섯을 손으로 적은 대장**이고, 그것이 정확히 *"대장에 이름이 있다는 사실이
  그 자리는 세어졌다로 읽힌다"*(AB-4)의 모양이다. **판정은 모듈 자신이 적어 둔 사실
  (머리말의 자백 · 호출부가 계약 파일뿐인가)에서 파생해야 한다.**
- **크기 M.** 대장 하나 + 계약 하나 + 소스 주석 몇 줄.

### 4. **재개 조건에 형을 괄호로 밝히는 관례가 두 라운드를 살아남았고, 그 계약을 세울 순서가 오늘 도래했다** — 계약·문서 — M

- **사실.** AA-3이 만든 관례(*"재개 조건(사건형|결정형 · 손은 안|밖)"*)는 오늘 문서에
  **203자리 중 61(괄호 안 기준) / 84(줄 어디든 기준)** 서 있고, 손의 위치까지 적은 것은
  **11 / 14**다. ⚠️ **라운드 88은 같은 자리를 169 · 65 · 8로 적었다 — 바늘이 다르므로
  두 수를 한 낱말로 적지 않는다**(오늘의 두 바늘은 위에 적었다).
- ⚠️⚠️ **재개 조건이 오늘 도래했다.** 그 기각이 적은 조건은 *"문서 축을 무는 스윕을 **F 밖의
  트랙이 소유할 수 있게 되는 날** — 예컨대 **F가 절을 먼저 쓰고 다음 라운드가 그 계약을 세우는
  순서**"* 였고(`known-limitations.md:6077`), **라운드 88 F가 AC절을 이미 썼다.**
- ⚠️ **그래서 순환이 오늘은 끊긴다**: 이 계약을 세우는 트랙은 **AD절을 쓰지 않고**, F는 그
  계약이 이미 선 뒤에 AD절을 쓴다. ⚠️ **그리고 계약의 모양이 그 순서를 지켜야 한다** —
  **하한 래칫**(형 표기 수·손 위치 수가 줄지 않는다)이면 F가 줄을 더해도 초록이고,
  **결정형으로 표기한 줄은 손의 위치를 함께 적었을 것**이면 F가 새 결정형을 적을 때 관례가
  강제된다. **상한이나 전수 일치를 물면 F가 계약을 맞추게 된다 — 그것이 라운드 88이 이 트랙을
  집지 않은 이유다.**
- ⚠️ **AC-5의 답이 이 후보의 필요를 값으로 말한다**: 6,285줄짜리 판정 문서를 무는 단언은
  오늘 **N-4 문턱 한 줄 하나**뿐이다.
- **크기 M.** 신설 대장 하나(**저장소 그물 열다섯째**) + 계약 하나.

### 5. **C-3의 경과 수가 같은 파일 두 자리에 있고, 한 자리가 세 라운드 낡았다** — 계약·문서·접근성 — S

- **사실.** `accessibility-offline-checklist.md:1031`(C절)은 *"스물두 라운드 연속 미확인
  (67…88)"*, `:1048`("수동 증거" 절)은 *"2026-08-30 기준 열아홉 라운드 연속 미확인(67…85)"* 이다.
  **두 자리 다 자기 안에서는 맞고, 서로는 3라운드 갈렸다.**
- ⚠️⚠️ **그 갈림은 라운드 86부터 세 라운드째 살아 있고, 세 라운드 다 트랙 F가 C절만 갱신했다.**
  라운드 88 F는 그 사실을 **보고에만** 남겼다 — 문서에는 값이 되지 않았다.
- ⚠️ **어떤 계약도 이 갈림을 보지 못한다**(AC-5의 답): 그 문서를 무는 단언은 *파일이 존재한다*와
  *`"44px"`가 들어 있다* 둘뿐이다. ⚠️ **짝 문서(`runtime-verification-required.md`)는 §0 수치가
  파싱으로 다시 세어지는데**(라운드 75 C), **접근성 표에는 그 계약이 0건이다.**
- **고침의 모양.** 오늘 값으로 **통과 가능한** 계약을 세운다: ⓐ 두 자리 각각의 **한글 수사가
  자기 목록 길이와 같을 것**, ⓑ "수동 증거" 목록이 C절 목록의 **접두일 것**, ⓒ 두 목록의 끝
  라운드 차이가 **오늘의 값(3)을 넘지 않을 것**(상한 래칫). ⚠️ **오늘 셋 다 참이고, F가 C절만
  89로 올리면 ⓒ가 4가 되어 빨개진다** — 그것이 이 계약이 하는 일이다.
- **크기 S.** 계약 하나(체크표 자기집계 계열의 짝 파일 신설) · 화면 0건 · 문서 0건(갱신은 F).

## P3 (재었고 이번 라운드에 제안하지 않는다 — 전부 재개 조건과 함께)

- **AC-2의 답이 0이다 — 호출부 둘 이상의 필드 갈림 0건**(선행 확인 5). 갈리는 셋은 전부
  PATCH의 부분 갱신·선택 필드라 의도된 것이다. ⚠️ **재개 조건(사건형): 본문에 새 필드가
  붙는 날, 또는 그 스윕을 다시 돌리는 날** — ⚠️ **다시 돌릴 때는 변수로 만든 본문을 따라가는
  바늘을 함께 지고 돌려야 한다**(오늘의 사각).
- **AC-1의 일곱 — 재었고 살아 있는 결함 0건**(선행 확인 4). 한 화면만 무는 계약 83을 두 화면
  루프로 바꾸는 것은 **오늘 고칠 어긋남이 0건**이라 *판정을 맞추는 일*이 아니라 *계약의 모양을
  바꾸는 일*이 된다. ⚠️ **재개 조건(사건형): 그 일곱 중 한 자리에서 형제 화면이 실제로 갈리는
  날** — ⚠️ **그날의 하한은 일곱이 아니다**(바늘 둘의 사각을 선행 확인 4에 적었다).
- **AA-3 기계의 나머지 절반 — 소스에 사는 재개 조건의 표기까지 무는 것은 오늘 세우지 않는다.**
  `dead-export-ledger.ts`가 그 관례를 소스로 가져간 유일한 파일이고(넷), 오늘 후보 3이 그
  파일을 연다. ⚠️ **한 트랙이 한 그물에 축 둘을 얹지 않는다.** ⚠️ **재개 조건(사건형): 소스의
  재개 조건 표기가 둘째 파일에 서는 날.**
- **`dead-export-ledger.ts`의 결정형 9(JSX 사용을 참조로 세는 판정) — 전제는 참인데 집지 않는다.**
  `.tsx`의 `export function` **141**이 모집단 밖이다. ⚠️ **집지 않는 이유는 그물 배정이다**(후보 3이
  그 파일을 연다). ⚠️ **재개 조건(결정형 · 손은 저장소 안): 그 판정이 서는 날 — 그날 141이
  모집단으로 들어온다.**
- **앵커 대장의 사각 `helper-named-reader` 164 — 재었고 하한 그대로이며 오늘 집지 않는다**
  (선행 확인 14). ⚠️ **집지 않는 이유는 래칫이다**: 모집단이 넓어지면 `comment-tolerant`
  래칫 70이 첫날부터 전수로 다시 적혀야 하고, 그것이 라운드 85~88이 같은 자리에서 받은 경고다.
  ⚠️ **재개 조건(결정형 · 손은 저장소 안): 저장소가 소스 리더 이름을 한 벌로 모으는 날, 또는
  이 대장이 헬퍼 정의를 따라가 루트를 푸는 법을 배우는 날.**
- **문자열 리터럴 축(사문 대장) — 재었고 미도래.** 문자열 안에 참조가 있는 이름 **26**이지만
  **참조가 전부 문자열뿐인 export는 0건**이다. ⚠️ **재개 조건(사건형): 그 수가 0을 넘는 날 —
  그날 이 그물은 문자열도 마스킹해야 한다**(그 조건은 순서까지 적혀 있다 · AC-3의 여덟 중 하나).
- **행마다 갈리는 낭독 라벨의 모집단 — 재었고 오늘도 세우지 않는다.** ⚠️⚠️ **그리고 이 자리의
  재개 조건이 자기 모순이라는 사실이 오늘 처음 값이 된다**: 라운드 88이 적은 조건은
  *"a11y 그물을 여는 다음 트랙이 서는 날"* 인데, **a11y 그물을 여는 트랙은 언제나 자기 축이
  있으므로**(오늘은 트랙 A) *한 트랙이 한 그물에 축 둘을 얹지 않는다*는 규율이 그 조건을 매번
  막는다. ⚠️ **재개 조건을 좁혀 다시 적는다(사건형): a11y 그물을 여는 트랙의 축이 *바로 이
  모집단*인 라운드가 서는 날** — 즉 **그 스윕 자체가 한 트랙의 축이 되어야 한다.**
- **조회 실패 창의 모집단 — 라운드 88이 0으로 닫았고 오늘 다시 세지 않았다.** 어느 트랙도
  `src/offline/**`의 화면 구조를 열지 않는다. ⚠️ **재개 조건(결정형 · 손은 저장소 안):
  재조회 실패 창에서 옛 목록을 남길지 정하는 날.**
- **응답 필드 축(모바일) · `HouseholdMember.joinedAt` · 기록 탭 검색의 분류 갈래 · 막대
  포커스 가능화 — 넷 다 라운드 88의 판정 그대로이고 상태 변화 0.** 기록 탭 검색은
  `records-list-view.ts:841`의 라벨과 `:846` placeholder가 오늘도 같은 값에서 파생하고
  **어긋남 0건 · 네 라운드 연속 같은 답**이다. 막대 포커스는 두 화면 다 표가 값을 텍스트로
  주므로 **값 0**이다. ⚠️ **재개 조건 넷 다 종전 그대로.**
- **이월 다섯(U절 셋 · S-3 · `withdrawn_at`) — 재실측 없이 종전 판정 그대로.**
  ⚠️⚠️ **다만 S-3의 두 파일에 대한 *"접점 0건"* 연속이 이번 라운드에 끊긴다** — 후보 2가
  `app/items/page.tsx`·`app/links/page.tsx`를 **표 이름 축으로만** 연다(역할 게이트·폼·저장
  경로는 바이트 불변). ⚠️ **그 사실을 트랙 B의 금지 조항과 F의 이월 줄이 함께 적는다** —
  *"열었지만 S-3은 손대지 않았다"* 와 *"열지 않았다"* 는 다른 문장이다.
- **성능 넷 — ⓐ·ⓒ·ⓓ는 재었고 미도래, ⓑ는 오늘 재지 않았다.** ⓐ **첫 페인트**(⚠️ 어느 트랙도
  `useQuery` 선언을 늘리지 않는다) · ⓒ **번들**(⚠️ **새 런타임 의존성 0건 — 다섯 트랙 다 기존
  import만 쓴다**) · ⓓ **api의 루프**(⚠️ **`apps/api/**` 쓰기 0건**). ⓑ **렌더 비용**은
  활성 카탈로그 수가 실 DB를 요구해 **오늘 재지 않았다**(라운드 88의 수를 인용하지 않는다 —
  선행 확인 15). ⚠️ **어느 트랙도 준비템 탭을 열지 않는다.** ⚠️ **재개 조건 넷 다 종전 그대로.**
- **손이 밖인 다섯 — 넷은 다른 사람이 내릴 결정이고 하나는 배정이다**(선행 확인 13의 4~8).
  ⚠️ **C-3은 오늘로 스물세 라운드 연속 미확인이고, 그 줄의 손은 여전히 저장소 밖이다** —
  ⚠️⚠️ **후보 5는 그 확인을 대신하지 않는다. 하는 일은 *그 수가 두 자리에서 조용히 갈리지
  않게 하는 것* 하나다.**
- **`${seller}에서 구매하기` 둘 · 준비템 판매처 1:1 · 구매 확인 판매처 라벨 — 영구 기각 축이고
  이 줄은 제안이 아니라 스윕이 그 둘을 다시 줍지 않게 하는 표식이다**(라운드 62~88 그대로).
- **감사 뷰 CSV · 콘텐츠 리비전 편집 표면 · 어드민 손 미러 · `SYNC_STATUS_RETRY_ALL_LABEL` ·
  지출 입력 두 화면의 조립 · `PURCHASE_FOLLOWUP_MERCHANT_LABELS` 미실행 · CSV 왕복 다섯 열 손실 ·
  `refund` 생성 불가 · `link_health`의 `errors` 카운터 · 서버 알림 층 · 홈의 손 폴 다섯 ·
  공유 카드 왕복 · 어드민 카탈로그 전량 조회 · `ApplicationPrimitives.tsx` 정규식 제목 판정 ·
  미출처 틴트 둘 · `itemMatchesBand` 사문 · 첫돌 이후 마일스톤 고착 · `AuthService.refresh`의
  `user.status` · api 하네스 동시 실행 구멍 · 서버 중복 아이 가드 부재 · 발행 `before` 경합 ·
  크래시 파이프라인 부재 · 서버 stdout 두 로그 형식(O-1) · `Share.share`의 catch 없는 `void` 둘 ·
  결제 수단 기본값** — 라운드 62~88이 남긴 그대로이고 **상태 변화 0.**
- **제외 목록 준수 확인**: 준비템 목록 **가격 표시**(잠금 · ⚠️ **다섯 후보 어디에도 가격·링크
  수가 들어가지 않는다**) · 오프라인 로컬 아이 복구 · 외부 계정/키/자산 · **C-3 잠금 오버레이
  낭독**(**스물세 라운드 연속** 미확인) · **P-2 법무 대조** · P-3 테스트 건수 자동화 ·
  **표기 방언 통일** · S-4 파기 `targetId` · 40주 초과 달력 · `onBudgetRelevantChange` ·
  4가구/`viewedHouseholdId` · **지출→리포트 정확성 축** · **공유 왕복 축** · **준비템 완료율
  동기부여** · **준비템 탭 비가상화** · **홈 수치 정합** · **C/E 인용 두 방언** ·
  **`getHome` 카탈로그 전량 읽기** · **`previousMonth` 캐시 온기** · **`app/**` 전수 대장** ·
  **하네스 셋째 모집단** · **워커 시간 예산** · **리포트 첫 페인트 추가 축소** · **밴드별 카운트**
  — **전부 오늘도 제안하지 않는다.**

## 코드 건강 판정

- **핵심 루프는 오늘도 끊긴 데가 없다.** 지출 기록 → 총액 → 준비템 → 구매 링크 → 기록/상태 체크.
  ⚠️ **이번 라운드의 다섯 트랙 중 핵심 루프의 렌더를 여는 것은 0건이고**, 그 사실이 값이다 —
  오늘 나온 결함 다섯은 전부 **루프의 바깥 테두리**(가족 초대 · 운영자 도구 · 계약 · 문서)에 있다.
  ⚠️ **핵심 루프 한가운데를 두 번 재기는 했다**(AC-1의 일곱 중 다섯이 지출·기록·홈이고, AC-2의
  스물이 그 루프의 API 전부다) — **재어 보니 결함이 0건이었다.**
- **타입 검사는 두 앱 다 초록이다**(`tsc --noEmit` exit 0 · 선행 확인 2). **테스트는 돌리지
  않았다** — 이 노트는 계약의 초록을 주장하지 않는다.
- **이번 라운드의 주제는 *판정이 값이 된 뒤에도 아무도 고치지 않는다*이다.** 라운드 88은 대장에
  **판정 칸**을 세워 *"여기는 크로스플랫폼 출구가 0건이다"* 를 처음 값으로 만들었는데(AC-4),
  ⚠️⚠️ **그 판정이 가리킨 자리는 오늘도 그대로다.** **판정과 수리를 한 라운드에 섞지 않는 것이
  이 저장소의 관례라면, *그 다음 라운드가 수리를 집는 것*이 그 관례의 나머지 절반이다.**
  ⚠️ **후보 1이 그 절반이다.**
- **두 번째 주제는 *문서에는 계약이 거의 없다*이다.** AC-5의 답이 전수로 말한다 — 지켜지는
  문서 스물여섯 중 **판정 문서 둘을 무는 단언은 각각 한 줄·한 낱말**이다. ⚠️ **그래서 그 두
  문서에서 오늘 낡은 자리가 나왔다**(재개 조건의 형 표기 · C-3의 두 수). **후보 4·5가 같은
  답에서 나온 이유가 그것이다.**
- **세 번째 주제는 *순서를 적어 둔 조건은 실제로 집힌다*이다**(AC-3). 순서를 함께 적은 여덟 중
  **둘은 이미 소진됐고 하나가 오늘 도래해 트랙이 됐다.** 순서가 없는 결정형 스물넷 중 오늘
  집히는 것은 0건이다. ⚠️ **조건을 적을 때 *언제*뿐 아니라 *무엇을 먼저*까지 적는 값이 이렇게 잰다.**
- **계약 그물은 오늘 열넷이고, 이 라운드가 하나를 더한다**(트랙 D의 재개 조건 대장 =
  **열다섯째**). ⚠️ **트랙 B의 어드민 표 이름 스윕은 그 수에 들지 않는다** — 열넷 목록은 앱
  경계를 넘는 그물이고 이 스윕은 `apps/admin/app/**` 하나만 걷는다(어드민 catch 스윕·
  `admin-analytics.test.ts`와 같은 계열이다). ⚠️ **그 판단의 근거를 트랙 B의 머리말이 적는다.**
- **기각을 값으로 남긴 것은 열여섯이다**(P3 · 전부 재개 조건과 함께이고, 그중 **다섯은 조건이
  결정형이라고 이름 붙이며 손의 위치를 함께 적는다**). ⚠️⚠️ **그리고 이번에는 기각 하나가
  *조건 자체를 고친다*: 낭독 라벨 모집단의 재개 조건이 자기 모순이라 좁혀 다시 적었다.**

## 트랙 구성 (파일 단위 상호 배타)

- **A 끝난 초대 카드가 두 플랫폼 다 소리로 나간다** (#1) — **유일한 모바일 화면 트랙 · 가장 작다**
  - 소유: `apps/mobile/app/family/accept/[token].tsx`(⚠️ **effect 한 벌과 import 한 줄만** — 그 밖은
    바이트 불변) · `apps/mobile/src/family/invite-accept-messages.ts`(⚠️ **낭독 문장을 짓는 함수
    하나** — 상수 다섯은 바이트 불변) · `apps/mobile/src/family/invite-accept-messages.test.ts` ·
    `apps/mobile/src/a11y-contract.test.ts`(⚠️ **`:3574` 항목의 `crossPlatform` 이유 칸 한 자리** —
    이 파일은 이 트랙 하나만 연다)
  - 읽기: `apps/mobile/src/ui.tsx`(`announceForA11y` — 읽기만 · 바이트 불변) ·
    `apps/mobile/src/family/invite-accept-messages.test.ts` 외의 이 화면을 무는 계약
    (`src/children/household-join.test.ts` · `src/family/role-revalidation.test.ts` ·
    `src/family/invite-accept-messages.test.ts` · `src/offline/messages.test.ts` — **초록으로
    남는지는 A가 확인한다**) · `docs/qa/accessibility-offline-checklist.md`의 **C-12 행**(읽기만 —
    갱신은 F)
  - 금지: ⚠️ **서버 0건 · 새 요청 0건 · 새 화면 0건** · ⚠️⚠️ **프롭 바이트 불변** —
    `<View accessibilityLiveRegion="polite" accessibilityRole="alert">`는 대장의 `after`이고
    **한 글자도 바꾸지 않는다**(바꾸면 프롭 대장이 먼저 빨개진다) · ⚠️ **새 한국어 문장 0건**
    (낭독 문장은 `INVITE_UNAVAILABLE_*` 상수에서 **모듈이 짓는다**) ·
    ⚠️ **카드의 렌더·갈래·버튼·라벨 바이트 불변**(`:325~365`) ·
    ⚠️ **뒤처리 실패 카드(`joinRetryNotice && joinedResult`)는 손대지 않는다**(오늘 두 플랫폼 다
    소리가 나고, 그 자리의 사각은 그물의 것이지 화면의 것이 아니다) ·
    ⚠️ **`:218-223`의 기존 effect 조건 바이트 불변** ·
    ⚠️ **새 export를 `export const`로 만들지 말 것** — 트랙 C가 `export const` 축을 모집단으로
    들이므로, 부르는 자리가 있어도 **함수로** 내보내는 것이 이 트랙의 조항이다 ·
    **어드민 0건 · `packages/**` 0건 · `docs/**` 0건**
  - 계약: ⓐ **낭독** — `inviteUnavailable`이 참인 창에서 화면이 `announceForA11y`를 부를 것
    (⚠️ **effect의 `if` 조건이 카드의 최내곽 JSX 갈래와 *글자로 완전히 같을 것*** — 라운드 88
    리뷰 L-1이 이름 붙인 사각이 정확히 그것이고, 다르면 판정이 `live-region`으로 떨어진다).
    ⓑ **문구** — 낭독되는 문장이 카드가 그리는 상수들에서 파생할 것(**화면에 새 한국어 리터럴
    0건** · 눈과 귀가 다른 말을 하지 않는다). ⓒ **판정 칸** — `ROUND79_ANNOUNCE_PROPS_ADDED`의
    이 항목이 파생 판정으로 다시 돌 때 **ⓐ 자리가 `announce`가 되고**, 이유 칸은 **남은 조용한
    자리(ⓑ 뒤처리 카드)의 조건 이름을 담을 것**(⚠️ **낡은 이유가 남으면 그 계약이 빨개진다** —
    라운드 88 E가 세운 그 조항 그대로). ⓓ **부정** — 더한 effect와 import를 빼면 화면이 **종전
    바이트와 정확히 같아질 것**. ⓔ **소음 금지** — `inviteUnavailable`이 거짓인 창에서는 이
    낭독이 0건일 것. ⓕ **바이트 불변** — 프롭 조합 · 카드 문구 상수 다섯 · [앱 둘러보기] 버튼과
    그 접근성 라벨 · `:218-223`의 기존 effect.

- **B 어드민의 표가 이름을 갖는다 — 그리고 그 규율이 모집단을 얻는다** (#2) — **유일한 어드민 트랙**
  - 소유: `apps/admin/app/{users,audit-logs,reviews,categories,items,users-lookup,links,analytics,clicks}/page.tsx`
    (⚠️ **`<h2>`의 `id` 속성과 `<table>`의 `aria-labelledby`/`aria-label`만** — 그 밖은 바이트 불변) ·
    `apps/admin/src/admin-table-name.test.ts`(**신설** — ⚠️ **저장소 그물 열넷/열다섯의 하나가
    아니다**: 열넷 목록은 앱 경계를 넘는 그물이고 이 스윕은 `apps/admin/app/**` 하나만 걷는다.
    **그 사실을 이 파일의 머리말이 값으로 적는다**)
  - 읽기: `apps/admin/src/lib/analytics-trend-view.test.ts`(**추이 표 둘의 이름을 이미 무는
    계약 — 읽기만 · 바이트 불변 · 초록으로 남는지는 B가 확인한다**) ·
    `apps/admin/src/admin-load-error-copy.test.ts`·`admin-analytics.test.ts`·
    `admin-audit-logs.test.ts`·`admin-click-breakdown.test.ts`·`admin-cms-pages.test.ts`·
    `admin-users.test.ts`·`content-revision-schedule.test.ts`·`product-link-bulk.test.ts`·
    `admin-canonical-mirrors.test.ts`(**이 화면들을 무는 계약 아홉 — 읽기만 · 초록으로 남는지는
    B가 확인한다**) · `apps/admin/src/components/admin-page.module.css`(**클래스 실재 — 읽기만 ·
    새 클래스 0건**)
  - 금지: ⚠️ **서버 0건 · 새 요청 0건 · 새 컴포넌트 0건 · 새 상호작용 표면 0건**(`tabIndex`·
    `onClick`·`<button`) · ⚠️ **보이는 화면 한 픽셀 무변경**(`id`·`aria-labelledby`·`aria-label`은
    레이아웃 속성이 아니다) · ⚠️⚠️ **라운드 88 A가 만든 추이 표 둘의 `aria-label`은 바이트
    불변**(`clicks:208` · `analytics:531` — 그 카드는 그림과 표가 **끝말로 갈린** 이름 둘을 이미
    갖는다) · ⚠️ **막대 그림의 `aria-label`·`title`·식 바이트 불변** ·
    ⚠️ **DNC-009 고지 바이트 불변**(`clicks:176` · 분석 화면의 같은 문장) ·
    ⚠️ **`<td title=…>` 넷 무접촉**(`audit-logs:402`·`:422` · `clicks:190` · `analytics:511`) ·
    ⚠️ **표 머리(`<th>`)·행 내용·정렬·`toLocaleString` 자리 바이트 불변** ·
    ⚠️⚠️ **S-3은 손대지 않는다** — `items`·`links` 두 화면은 **표 이름 축으로만** 열고
    역할 게이트·[수정] 토글·폼 필드·저장 경로는 **바이트 불변**이다(⚠️ **이 트랙이 그 두 파일의
    *접점 0건* 연속을 끊는다는 사실을 F가 이월 줄에 적는다**) ·
    ⚠️ **`apps/admin/src/lib/**` 0건 변경**(새 모듈·새 export를 만들지 않는다 — 미러 스윕과
    사문 대장이 먼저 빨개진다) · **모바일 0건 · `packages/**` 0건 · `docs/**` 0건**
  - 계약: ⓐ **모집단** — `apps/admin/app/**`의 `<table` 자리를 **전수로 세고**(오늘 열일곱),
    각 자리가 **이름을 가질 것**(`aria-label` 또는 `aria-labelledby`). ⚠️ **손 목록이 아니라
    파일 전수에서 파생할 것** — 새 표가 이름 없이 붙는 날 그 자리가 빨개진다.
    ⓑ **이름의 출처** — `aria-labelledby`가 가리키는 `id`가 **같은 파일에 실재**하고 그것이
    `<h2>`일 것(⚠️ **가리키기만 하고 없는 id는 이름이 아니다**). ⓒ **화면이 짓는 이름은 값으로
    셀 것** — h2에서 파생하지 못해 화면이 문자열을 적은 자리 수를 **대장으로 들고**, 각 항목에
    **왜 h2가 그 표의 이름이 아닌지**를 이유로 적을 것(⚠️ **빈 문자열 금지**).
    ⓓ **한 카드 두 이름** — 같은 카드가 이름을 둘 이상 가지면 **끝말로 갈릴 것**
    (`막대 그래프` / `표` — 라운드 88 A가 세운 그 관례를 인용하지 발명하지 않는다).
    ⓔ **래칫** — 이름을 가진 표의 수는 줄지 않는다. ⓕ **바이트 불변** — 표 머리 넷 · DNC-009
    고지 · 오류 문구 · 막대 식 · `<td title=…>` 넷 · 추이 표 둘의 `aria-label`.
  - ⚠️ **전제 재실측 의무**: 위 열일곱·둘·열다섯은 **정찰이 grep으로 잰 하한**이다. 트랙이 다시
    세어 다른 수가 나오면 **그 수가 값이고 갈린 이유를 함께 적는다**(라운드 87 E·88 C의 형식).

- **C 사문 대장이 `export const` 축을 모집단으로 들인다 — 계약 전용 모듈을 뿌리에서 가르는 판정으로** (#3) — **사문 대장을 여는 유일한 트랙**
  - 소유: `packages/test-utils/src/dead-export-ledger.ts` · `dead-export-ledger.test.ts` ·
    (필요하면) **판단이 필요한 다섯 자리의 소스 주석만** —
    `apps/mobile/src/analytics/events.ts` · `apps/mobile/src/expenses/failed-row-prefill.ts` ·
    `apps/mobile/src/import/import-failure-messages.ts` ·
    `apps/mobile/src/offline/sqlite-offline-store.ts` ·
    `apps/mobile/src/settings/destructive-flow-messages.ts`
    (⚠️ **주석만 · export 값과 코드는 바이트 불변**)
  - 읽기: `apps/mobile/src/offline/offline-aware-screens.ts` · `src/query/shared-cache-policy.ts` ·
    `src/offline/messages.ts` · `src/reports/empty-period-card.ts` · `src/settings/more-menu.ts` ·
    `apps/admin/src/lib/load-error-copy.ts`(**판정이 파생할 여섯 모듈 — 읽기만 · 바이트 불변**) ·
    `apps/mobile/src/a11y-contract.test.ts`(**트랙 A의 소유 — 읽지도 쓰지도 않는다**)
  - 금지: ⚠️⚠️ **판정을 손으로 적지 말 것** — 오늘 `CONTRACT_ONLY_DATA_MODULES`가 경로 다섯과
    이유 다섯을 손으로 적는데, **그것이 AB-4가 이름 붙인 착시의 모양**이다. 판정은
    **모듈 자신이 적어 둔 사실**(머리말의 자백 · 호출부가 계약 파일뿐인가)에서 **파생**해야 하고
    래칫이 그 파생을 강제해야 한다 ·
    ⚠️ **항목을 지우지 말 것**(이 트랙이 하는 일은 *지우는 판단*이 아니라 **모집단을 넓히는 것**) ·
    ⚠️ **`DEAD_EXPORT_RATCHET`를 낮추지 말 것**(넓힌 모집단에서 늘어난 수는 값으로 적고, 늘어난
    이유가 *새 부채가 아니라 세는 자리가 늘었다*는 사실을 함께 적을 것 — 라운드 88 D가 세운 형식) ·
    ⚠️ **주석 마스킹·문자열 축의 판정을 건드리지 말 것**(⚠️ **한 트랙이 이 그물에 축 둘을 얹지
    않는다** — 문자열 마스킹과 JSX 판정은 각자의 재개 조건이 여는 자리다) ·
    ⚠️ **`apps/admin/app/**` 0건 · 모바일 화면(`app/**`) 0건 · `docs/**` 0건**
  - 계약: ⓐ **파생 판정** — *"이 모듈은 계약 전용 데이터다"* 가 **모듈 자신의 소스에서** 파생할 것
    (머리말이 그 사실을 적었거나, 호출부가 계약 파일뿐이거나 — 둘 중 무엇을 쓰는지는 트랙이
    값으로 고르고 그 근거를 소스에서 실제로 찾을 것 · **표식만 복사하는 것은 막힌다**).
    ⓑ **모집단** — `export const` 축이 사문 판정의 모집단에 들어올 것(오늘 652 중 24).
    ⓒ **면제의 크기** — 그 판정으로 면제되는 자리 수를 **값으로** 들고, 면제되지 않는 자리는
    **이유가 소스에 있거나 대장에 줄이 있을 것**(오늘 다섯).
    ⓓ **유령 방지** — 판정이 면제한 모듈 수·자리 수·마스킹 전후·중복이 함께 대조될 것.
    ⓔ **래칫** — 타입과 값 양쪽에 걸려, 새 `export const`가 이유 없이 죽는 날 자동으로 요구할 것.
    ⓕ **사각** — 넓힌 뒤의 사각(남은 `tsx-components` 141 · `common-name` 77 ·
    `string-literal-references` 26)을 **값과 하한으로** 다시 적을 것. ⚠️ **적힌 값과 오늘 실측이
    같은지 함께 확인할 것**(정찰은 일곱 자리 전부 같은 것을 확인했다).

- **D 재개 조건의 표기 관례를 계약이 지킨다** (#4) — **저장소 그물 열다섯째**
  - 소유: `packages/test-utils/src/resume-condition-ledger.ts`(**신설**) ·
    `resume-condition-ledger.test.ts`(**신설**)
  - 읽기: `docs/operations/known-limitations.md`(**읽기만 · 이 트랙은 문서를 한 글자도 고치지
    않는다** — 고치는 손은 F뿐이다) · `packages/test-utils/src/dead-export-ledger.ts`
    (**소스에 사는 재개 조건 넷의 본보기 — 읽기만 · 트랙 C의 소유라 손대지 말 것**) ·
    `packages/test-utils/src/comment-tolerant-anchor-ledger.ts`(**신설 그물의 형식 본보기 —
    읽기만**)
  - 금지: ⚠️⚠️ **문서를 고치지 말 것**(⚠️ 이 트랙이 문서를 고치면 *계약이 문서를 지키는 것*이
    아니라 *문서가 계약을 맞추는 것*이 된다 — 라운드 88이 이 트랙을 집지 않은 이유가 그것이고,
    오늘 집는 이유는 **F가 절을 먼저 썼기 때문**이다) ·
    ⚠️⚠️ **상한이나 전수 일치를 물지 말 것** — 래칫은 **하한**이어야 F가 AD절을 쓰며 줄을 더해도
    초록이다(전수를 물면 이 라운드의 F가 계약을 맞추게 된다) ·
    ⚠️ **`docs/5차/**`(정찰 노트)를 모집단에 넣지 말 것**(라운드별 작업 기록이지 판정 문서가
    아니다 — 넣으면 이 대장의 수가 매 라운드 흔들린다) ·
    ⚠️ **`dead-export-ledger.ts`·`comment-tolerant-anchor-ledger.ts`를 쓰지 말 것**(각각 C와
    아무 트랙의 것도 아닌 자리다) · **화면 0건 · 어드민 0건 · 모바일 0건 · `docs/**` 쓰기 0건**
  - 계약: ⓐ **모집단** — `known-limitations.md`의 *"재개 조건|재개 트리거"* 등장 자리를 **전수로**
    셀 것(⚠️ **바늘을 값으로 적을 것**: 괄호 안만 보는 바늘과 줄 전체를 보는 바늘이 **오늘 61과
    84로 갈린다** — 두 수를 한 낱말로 적지 않는다).
    ⓑ **하한 래칫** — 형을 괄호로 밝힌 자리 수와 손의 위치를 함께 적은 자리 수가 **줄지 않을 것**.
    ⓒ **결정형의 조항** — **결정형으로 표기한 자리는 손의 위치를 함께 적었을 것**(⚠️ 이것이 이
    계약의 축이다 — AA-3의 관례를 기계가 지킨다).
    ⓓ **소스 축** — 소스에 사는 재개 조건(오늘 `dead-export-ledger.ts` 넷)도 같은 조항을 받을 것.
    ⓔ **사각** — 이 그물이 못 보는 것을 **값과 하한으로** 적을 것(⚠️ 최소 셋: *산문으로만 적힌
    조건* · *두 줄로 접힌 조건* · *`docs/5차/**`와 짝 문서 둘의 조건*).
    ⓕ **자기 배제** — 대장 자신의 두 파일을 모집단에 넣지 않을 것(라운드 84 B·85 E·88 C의 규율).
  - ⚠️ **전제 재실측 의무**: 위 203·61·84·11·14는 **정찰의 두 바늘이 낸 값**이다. 트랙이 다시
    세어 다른 수가 나오면 **그 수가 값이고 갈린 이유를 함께 적는다**.

- **E 접근성 표의 C-3 경과 수가 두 자리에서 갈리지 않는다** (#5) — **가장 작다 · 체크표 자기집계 계열**
  - 소유: `packages/test-utils/src/accessibility-checklist-shape.test.ts`(**신설** — ⚠️ **새 그물이
    아니라 `runtime-checklist-shape.test.ts`의 *짝*이다**: 같은 계열의 둘째 문서를 같은 방식으로
    센다. **그 사실을 머리말이 값으로 적는다**)
  - 읽기: `docs/qa/accessibility-offline-checklist.md`(**읽기만 · 이 트랙은 문서를 한 글자도
    고치지 않는다**) · `packages/test-utils/src/runtime-checklist-shape.test.ts`(**형식 본보기 —
    읽기만 · 바이트 불변**) · `packages/test-utils/src/release-readiness.test.ts`(**같은 문서를
    무는 단언 둘 — 읽기만 · 초록으로 남는지는 E가 확인한다**)
  - 금지: ⚠️⚠️ **문서를 고치지 말 것**(갱신은 F다 — D와 같은 이유) ·
    ⚠️ **`runtime-checklist-shape.test.ts`를 고치지 말 것**(그 파일의 축은 짝 문서이고, 이 트랙의
    축은 접근성 표다 — **한 트랙이 한 그물에 축 둘을 얹지 않는다**) ·
    ⚠️⚠️ **오늘 값으로 통과 가능한 계약만 세울 것**(오늘 빨간 계약을 세우면 이 트랙이 문서를
    고쳐야 하고, 그 순간 위 금지가 깨진다) ·
    ⚠️ **머리말의 옛 라운드 문단(`:199`·`:229`·`:262`)을 모집단에 넣지 말 것**(그 수들은 낡은
    것이 아니라 **그 라운드의 기록**이다) ·
    ⚠️ **C-3의 확인 여부를 판정하지 말 것**(그 손은 저장소 밖이다 — 이 계약이 세는 것은
    **수의 정합**이지 확인이 아니다) · **화면 0건 · 어드민 0건 · 모바일 0건 · `docs/**` 쓰기 0건**
  - 계약: ⓐ **자기 정합** — 경과 수를 말하는 두 자리 각각에서 **한글 수사가 열거된 라운드 목록의
    길이와 같을 것**(오늘 C절 스물두/22 · 수동 증거 열아홉/19). ⓑ **접두** — "수동 증거" 절의
    목록이 C절 목록의 **접두일 것**(⚠️ 두 목록이 서로 다른 라운드를 말하면 그날 빨개진다).
    ⓒ **갈림의 상한 래칫** — 두 목록의 **끝 라운드 차이가 오늘의 값(3)을 넘지 않을 것**
    (⚠️⚠️ **이것이 이 트랙의 축이다**: F가 C절만 89로 올리면 차이가 4가 되어 **F가 빨개진다**).
    ⓓ **목록의 모양** — 목록이 **67부터 빠짐없이 연속**일 것(중간에 빠진 라운드가 0건).
    ⓔ **좌표** — C-3이 짝 문서의 `#26`이고 표면이 `실기기`라는 사실이 두 문서에서 **같은 값**일 것.
    ⓕ **사각** — 이 계약이 **확인 여부는 세지 않는다**는 사실과, **C-12 같은 다른 C절 줄에는 아직
    경과 수 축이 없다**는 사실을 값으로 적을 것.

- **F 문서(판정 · 짝 문서 둘)** — **마지막**
  - 소유: `docs/operations/known-limitations.md`(**AD절 신설**) ·
    `docs/qa/runtime-verification-required.md` · `docs/qa/accessibility-offline-checklist.md`
  - 본체:
    ⓐ **AD절 다섯 판정** — 오늘의 관측에서 파생할 것. 정찰이 값으로 본 씨앗은 이렇다(⚠️ **F가
    다시 세어 다르면 그 수가 값이다**): ① *판정이 값이 된 뒤에도 그것을 고치는 것은 다음 라운드의
    몫이고, 그 몫은 배정되지 않으면 오지 않는다*(AC-4 → 트랙 A) · ② *한 자리에 이름을 준 라운드의
    다음 질문은 "그 화면의 형제 자리는 전부인가"이고, 이름의 재료는 대개 화면에 이미 있다*
    (트랙 B) · ③ *순서를 함께 적은 재개 조건은 실제로 집히고, 순서가 없는 조건은 집히지 않는다*
    (AC-3 · 여덟 중 셋이 소진·도래, 순서 없는 스물넷 중 0건) · ④ *판정 문서를 무는 계약이 한 줄
    한 낱말이면 그 문서의 어느 자리가 낡아도 아무도 모른다*(AC-5 → 트랙 D·E) ·
    ⑤ *재개 조건이 "다음 트랙이 서는 날"이면 그 조건은 규율(한 그물에 축 둘 금지)에 막혀 영원히
    미도래로 남는다 — 조건은 **자기가 누구의 축이 되어야 하는지**까지 적어야 한다*(낭독 라벨
    모집단의 자기 모순 · P3).
    ⓑ **AC-1~AC-5에 대한 오늘의 답 다섯**(위 선행 확인 3의 표 · ⚠️ **각 답에 *그 수를 어떻게
    냈는지*를 함께**).
    ⓒ **되짚기** — AC-1~AC-5 각 절에 *어느 절이 답했는지만* 가리키는 한 줄을 더할 것
    (⚠️ **판정을 옮겨 적지 않는다** — O-3·X-4).
    ⓓ **결정형 재개 조건 전수를 다시 셀 것** — 하나가 닫혀 빠지고 라운드 88의 기각이 적은 둘이
    들어와 **열하나**이며, ⚠️ **오늘 집는 것이 둘**(표의 10 → 트랙 C · AA-3 기계 → 트랙 D)이고
    **둘 다 순서가 먼저 적혀 있어서 집혔다**는 사실을 함께 적을 것.
    ⓔ **N-4 미발동 한 줄**(⚠️ **수는 옮겨 적지 않는다** — 그 수는 어드민 카드가 센다 ·
    ⚠️ **이번 라운드는 그 수를 재지 않았다는 사실을 적을 것**).
    ⓕ **이월 다섯 갱신 다섯 줄** — ⚠️⚠️ **`S-3` 줄에는 *다섯 라운드 만에 그 두 파일이 열렸고,
    열린 축은 표 이름 하나이며 S-3은 손대지 않았다*는 구별을 함께 적을 것**(*"열지 않았다"* 와
    *"열었지만 그 축이 아니다"* 는 다른 문장이다).
    ⓖ **기각을 값으로 열여섯** — AC-2의 0 · AC-1의 일곱 · AA-3 기계의 나머지 절반 · 결정형 9 ·
    앵커 사각 164 · 문자열 리터럴 축 · 낭독 라벨 모집단(**조건을 좁혀 다시 적는다**) ·
    조회 실패 창 · 응답 필드 축 · `joinedAt` · 기록 탭 검색 · 막대 포커스 · 이월 다섯 ·
    성능 넷(**ⓑ는 재지 않았다**) · 손이 밖인 다섯 · `${seller}` 둘
    (**전부 재개 조건과 함께 · 결정형 다섯은 그 사실과 손의 위치를 함께**).
    ⓗ **트립와이어 88→89** — 라운드 88이 남긴 래칫·하한을 오늘 다시 잰 값의 대조표를 남길 것
    (⚠️ **정찰의 실측: 전부 움직이지 않았다**) — 사문 대장 항목 **16**/래칫 **16** · 사각 일곱
    (24·17·77·9·26·141·0) · 앵커 대장 앵커 **698**(code-only 620 · comment-tolerant **70**/래칫 70 ·
    comment-only **8**/래칫 8 · unanchored 0) · 면제 **8** · 그물 수 **14** · 프롭 대장 아홉 중
    `crossPlatform` 비어 있지 않은 것 **하나** · 짝 문서 **161행**(실기기 134 · 브라우저 17 ·
    서버 9 · 작업 1) · 접근성 표 **A-29**까지 · C절 C-3 **22** · 수동 증거 **19**.
    ⚠️⚠️ **그중 마지막 둘이 오늘의 결함이고, 트랙 E의 계약이 그 자리를 지킨다.**
    ⓘ **접근성 표**: 라운드 89분을 **A-30**으로 세울 것 — ⚠️ **실기기 하나**(트랙 A: 만료·취소된
    초대 링크를 **iOS·VoiceOver**로 열어 카드가 설 때 **훑지 않아도** 들리는가 · 안드로이드는
    종전대로 들리는가). ⚠️ **트랙 B의 어드민 항목은 종전 판정대로 행이 아니라 문단으로**
    (브라우저 화면은 그 표의 조건 밖이다 — ⚠️ **다만 그 트랙이 고친 것이 정확히 접근성 축이라는
    사실은 문단이 진다**: 이름 없는 표가 둘 이상 선 화면에서 운영자는 "표"를 두세 번 듣는다) ·
    ⚠️ **트랙 C·D·E는 소스 계약이라 두 표 어디에도 행이 서지 않는다.**
    ⚠️⚠️ **그리고 `C-12`의 상태를 갱신할 것** — 그 줄의 재개 조건은 *사건형: 이 카드를 여는
    트랙이 서는 날*이었고 **오늘 트랙 A가 그 날이다**. ⚠️ **그 줄이 함께 적기로 한 것이 있다:
    *판정과 확인 중 무엇이 먼저였는지*** — 오늘의 답은 **판정이 먼저였다**(라운드 88 E의 파생
    판정이 그 자리를 처음 값으로 만들었고, 라운드 89 A가 고쳤으며, 확인은 그 뒤에 온다).
    ⓙ **C-3은 오늘로 스물세 라운드 연속 미확인**(67~89)이고, ⚠️⚠️ **"수동 증거" 절의 같은 수를
    열아홉에서 스물셋으로 함께 올릴 것** — ⚠️ **세 라운드 낡아 있었다는 사실 자체를 값으로 적고**,
    ⚠️ **그 갈림을 이제 계약이 센다는 사실(트랙 E)을 함께 적을 것.**
    ⓚ `runtime-verification-required.md`에 라운드 89 신설분을 **#162부터** 편입하고 §0의 네 수·
    합계·§1-1 머리말 라운드 구간을 함께 갱신할 것(⚠️ 라운드 75 C의 계약이 그 값을 파싱으로 다시
    세므로 틀리면 `@wooriai/test-utils`가 먼저 빨개진다). ⚠️ **표면 배분**: 트랙 A는 **실기기**,
    트랙 B는 **브라우저**, **트랙 C·D·E는 소스 계약이라 행이 서지 않는다** —
    ⚠️ **오늘도 실기기 하나 · 브라우저 하나이고, 라운드 88과 같은 배분이다**(라운드 85·86·87의
    *실기기 셋*과 갈린 것이 **두 라운드 연속**이라는 사실을 적을 것 · 줄어든 이유는 확인을
    미뤄서가 아니라 **다섯 트랙 중 셋이 화면을 한 곳도 열지 않았기 때문**이다).
    ⓛ `launch-readiness-status.md`의 **테스트 건수 재실측**. ⚠️ **정찰은 이 수치를 적지 않는다**(O-3) ·
    ⚠️ **이 항목은 메인 세션 몫이고 게이트를 돌린 뒤에 채운다.**

- **머지 순서**: **A·B·D·E는 서로 완전 독립**이고 즉시 병렬 가능하다 —
  A=`apps/mobile/app/family/accept/[token].tsx` + `src/family/invite-accept-messages.*` +
  `src/a11y-contract.test.ts`,
  B=`apps/admin/app/**/page.tsx` 아홉 + `apps/admin/src/admin-table-name.test.ts`,
  D=`packages/test-utils/src/resume-condition-ledger.*`,
  E=`packages/test-utils/src/accessibility-checklist-shape.test.ts`.
  **파일이 한 곳도 겹치지 않는다.**
  ⚠️ **C는 A 뒤다** — 트랙 C가 `export const` 축을 모집단으로 들이므로, **A가 새 export를
  만드는 것이 C의 모집단에 들어올 수 있다.** A의 금지 조항(*새 것은 함수로 · 화면이 부른다*)이
  그것을 막지만, **순서로도 막는다**(C가 먼저 서면 A의 export가 대장의 낡은 수 위에 붙는다).
  ⚠️ **접점은 읽기 방향으로만 여섯이다**:
  ① **A → C**: A가 `invite-accept-messages.ts`에 **함수 하나**를 더하고 화면이 그것을 부른다 —
     ⚠️ **`export const`로 만들면 C의 넓힌 모집단에 사문으로 잡힌다**(그것이 A의 금지 조항이고,
     이 접점은 계약이 아니라 **금지 조항과 순서**로만 지켜진다),
  ② **B → 어드민 계약 아홉**: B가 아홉 파일을 **읽기만** 하고 초록으로 남긴다(⚠️ **B는 화면
     글자를 0건 바꾸므로 문구를 무는 단언이 흔들릴 자리가 없다** — 흔들릴 수 있는 것은 **구조를
     무는 단언**이고, `<h2>`에 속성 하나가 붙는 것이 그 단언에 걸리는지는 **B가 확인한다**),
  ③ **B → C**: B가 어드민 화면에 `id`·`aria-labelledby`만 더하고 **`apps/admin/src/lib/**`에
     새 export를 0건 만든다**(더하면 C의 사문 대장과 미러 스윕이 먼저 빨개진다),
  ④ **D → F**: D의 계약이 `known-limitations.md`를 **읽기만** 하고, F가 AD절을 쓰며 그 계약을
     만족시킨다 — ⚠️⚠️ **래칫이 하한이므로 F가 줄을 더해도 초록이고, F가 새 결정형을 적을 때만
     손의 위치를 함께 적으라는 조항이 선다.** **이것이 이 라운드가 그 기계를 집을 수 있는
     이유이고, 그 순서는 라운드 88 F가 이미 절을 썼기 때문에 성립한다**(AC-3),
  ⑤ **E → F**: E의 계약이 접근성 표를 **읽기만** 하고, F가 C절과 "수동 증거" 절을 **함께**
     올려야 초록으로 남는다 — ⚠️⚠️ **한쪽만 올리면 F가 빨개진다. 그것이 E의 값 전부다**,
  ⑥ **C → 전역**: C가 제품 소스 전역을 **읽기만** 한다(⚠️ 주석을 더하는 다섯 파일만 쓰기).
  ⚠️⚠️ **금지 조항의 교차 확인**: a11y 그물을 여는 트랙은 **A 하나**(`a11y-contract.test.ts`) ·
  사문 대장을 여는 트랙은 **C 하나**(`dead-export-ledger.*`) · **주석 관용 앵커 대장은 0건**이고,
  ⚠️ **저장소 그물 열넷의 파일을 여는 트랙은 0건이다** — **라우트 표면 · 슬라이스 가드 ·
  오프라인 대장 · 여정 스윕 · 체크표 자기집계 · 구독 대장 · `$transaction` 상한 대장 ·
  정책/무효화 대장 · 미러 스윕 · DNC 가드 대장 · DNC 범위 대장 · DNC 비밀값 스윕 · 문장 수 계측 ·
  주석 관용 앵커 대장, 열넷 다 어느 트랙도 손대지 않는다**(⚠️ **E는 체크표 자기집계의 파일을
  고치는 것이 아니라 그 *계열에 짝 파일 하나를 신설*한다** — 그 구별을 E의 머리말이 적는다).
  ⚠️ **계약 그물을 둘 이상 함께 여는 트랙은 0건이다**(D가 여는 것은 **신설 그물**이고 열넷 중
  하나가 아니다 — ⚠️ **그 사실을 D의 머리말이 적어야 한다**: 새 그물이 서는 날 그 수는
  **열다섯**이 된다. ⚠️ **B의 어드민 스윕과 E의 짝 계약은 그 수에 들지 않고, 왜 들지 않는지를
  각자의 머리말이 값으로 적는다**).
  **A를 먼저 머지한다** — 가장 작고(effect 한 벌) **사용자에게 보이는 값이 가장 빨리 닿는다**
  (⚠️ **오늘 이 저장소가 아는 결함 중 사람이 실제로 겪는 유일한 것이다**).
  그다음 **B**(어드민 · 브라우저 확인이 따라붙는다 · ⚠️ **아홉 파일을 여는 이 라운드에서 가장
  넓은 편집이고, 넓은 만큼 조항이 많다** — 표 머리·고지·`title` 넷·추이 표 둘이 전부 바이트
  불변이다). 그다음 **E**(가장 작은 계약 · 화면 0건 · ⚠️ **F를 빨갛게 만들 수 있는 유일한 계약**).
  그다음 **D**(계약 하나 · 문서 0건 · ⚠️ **순서가 이 트랙의 존재 이유다**).
  **C는 마지막이고 가장 크다** — ⚠️ **그 트랙의 절반은 코드가 아니라 *판정 결정*이고, 서두르면
  손으로 적은 대장이 하나 더 생긴다**(라운드 85 E·86 E·87 E·88 C·88 D가 같은 자리에서 받은
  경고 그대로 · ⚠️ **오늘은 그 경고에 하나가 더 붙는다: 이 트랙이 고칠 것은 다섯 자리이고, 그
  다섯을 판단하기 전에 *뿌리에서 가르는 판정*이 먼저 서야 한다** — **먼저 판정, 그다음 다섯**).
  **F는 마지막이고, 이번 F의 본체는 위 ⓐ~ⓛ이다.**

⚠️⚠️ **마지막으로, 이 정찰이 트랙에 지우는 의무 하나.** 위 다섯 후보의 전제는 전부 2026-08-31
소스에서 실측했지만(사문 대장·앵커 대장의 사각은 **실행해서** 다시 냈다), **각 트랙은 자기
전제를 다시 재고 거짓이면 정직하게 기록한다**(라운드 86 B는 전제 셋 중 둘이 거짓인 채로 집혔고,
라운드 87 B는 고치는 폭의 추정이 과대였으며, 라운드 88 E의 *"아홉 전부 초록일 가능성"* 도
거짓이었다 — **세 라운드 연속 정찰의 전제가 낡았다**). ⚠️ **특히 후보 2의 열일곱·둘·열다섯과
후보 4의 203·61·84는 정찰이 grep과 손 판정으로 낸 하한이다** — 트랙이 다시 세어 다른 수가
나오면 **그 수가 값이고, 갈린 이유를 함께 적는다**(라운드 87 E가 정찰의 열일곱을 열여섯으로,
라운드 88 C가 정찰의 쉰아홉을 일흔하나로 정정하며 세운 그 형식 그대로).
