# 라운드 93 정찰 노트 (GAP-093)

> master `f206005`(라운드 92 머지 · 적대 리뷰 커밋 `bbf1d97` 포함) 기준 · 2026-08-31 실측.
> do-not-change.md(DNC-001~020) · known-limitations **A~AG절**(AG-1~AG-5 전문 + AG절 머리말의
> **결정형 재개 조건 열여섯 전수표** · 문서 **8,777줄** · AG절 `:8140~8777`) ·
> runtime-verification-required.md(§0 · **168행** — 실기기 138 · 브라우저 20 · 서버 9 · 작업 1) ·
> accessibility-offline-checklist.md(**#108**까지 · A-33절 · C절 C-3 **스물여섯 라운드**) ·
> round56~92-scout 완료분 대조 완료.
>
> ⚠️ **이 라운드의 첫 의무는 관례가 정한 자리에 있다** — AG-1~AG-5가 각 절 끝에 남긴
> *"다음 라운드가 먼저 세어 볼 만한 것"* **다섯**을 오늘 실측으로 답하는 것, 그리고 **수를 낼 때
> 그 수를 어떻게 냈는지(모집단·바늘·사각)를 함께 적는 것**(AC-5·AD-4). 순서를 이렇게 잡았다:
> ① AG-1~AG-5의 질문 다섯을 전수로 세고(각각 **모집단을 먼저** 정하고 그다음 바늘),
> ② AG절 머리말의 **결정형 재개 조건 전수표 열여섯**을 HEAD에서 다시 세고,
> ③ 라운드 92 리뷰(GAP-092R)가 남긴 것(H-1~H-3 · M-1~M-5 · L-1~L-4)을 재실측하고,
> ④ ⚠️⚠️ **그다음 *AF-1이 낸 방법을 또 한 번 돌렸다*** — 대장을 읽는 것이 아니라 **저장소가 같은
>    물음에 이미 여러 번 고른 답을 고르지 않은 자리**를 세는 것. AG-1이 그 방법에 붙인 한 칸
>    (*계약이 있는데 모집단이 손인 곳*)을 오늘 **저장소 전수로** 돌렸고, 그 방법이 **사람이 오늘
>    겪는 자리 둘**을 냈다(후보 1·후보 4).
>
> ⚠️⚠️ **라운드 92 F의 수치 가운데 셋은 그 뒤 리뷰가 이미 바꿨고, 이 노트의 수는 전부 HEAD 실측이다.**
> a11y 목록 안 **35 → 38**(direct 36 · derived 2) · 경과 축 창 **83 → 79**(sites 383 · neither 304 ·
> 읽지 못한 수사 `['몇']`) · 키보드 스윕 사각 **다섯 → 여섯** · `unmarked-source-prose` **98 → 99**.
> ⚠️ **AG절 본문에 남은 수와 갈리는 자리는 그 사실 자체를 값으로 적었다**(아래 각 절).
>
> **관측은 하나로 모인다 — AG-1이 *"관례를 세는 계약이 있어도 모집단이 손 목록이면 관례에서 빠진
> 자리는 구조적으로 보이지 않는다"* 를 세우고 *"나머지가 몇인지는 오늘 세지 않았다"* 로 닫았는데,
> 오늘 그 나머지를 세니 손 목록이 서른둘이었고 그 안에서 사람이 겪는 자리 둘이 나왔다:**
> **⚠️⚠️ 하나는 *나가는 길이 없는 화면*이고(계약의 손 목록 열하나 밖에 서 있다), 다른 하나는
> *사용자가 지은 이름 뒤에 조사가 고정으로 붙는 문장 넷*이다(세는 계약이 0건이다).**
> 다섯 후보가 이렇게 갈린다(⚠️ **앞 둘은 사람이 오늘 겪는 자리이고, 뒤 셋은 사각의 축소다**):
> ① **동기화 상태 화면에 나가는 길이 없다** — 나가는 길 계약의 모집단이 **손 목록 열하나**라,
>    탭 밖 라우트 **31** 중 `ScreenHeader`를 쓰면서 `onBack`이 없는 **일곱**이 한 번도 세어진 적이
>    없다. 그중 `app/sync-status.tsx`는 **목록이 빌 때만 "닫기"가 서므로 대기·실패 행이 하나라도
>    있으면 화면 안의 나가는 길이 0개**이고, ⚠️⚠️ **실패 행을 고쳐 저장하면 앱이 사용자를 그
>    화면으로 보낸다**(`POST_SAVE_SYNC_STATUS_DESTINATION` · 핵심 루프 1단계 직후) (후보 1).
> ② **사용자가 지은 이름 뒤에 조사가 고정으로 붙는다** — 보간 뒤 받침 의존 조사 **33자리** 가운데
>    **넷**이 사용자 입력값 뒤에 서 있어 *"지훈를 추가했어요"* · *"김가네과 함께해요"* 가 난다.
>    ⚠️ **저장소는 이 물음에 이미 세 자리 이상에서 같은 답을 골랐다**(값의 꼬리를 고정 명사로
>    끝낸다 — `‘…’ 가구` · `… 초대` · `기록 N건`) **그리고 세는 계약은 0건이다**(후보 2).
> ③ **행마다 갈리는 낭독 라벨의 사각 ⓑ가 실재한다** — 라운드 92 B가 *"목록 밖 124는 묻지 않은
>    수이지 물을 것이 없는 수가 아니다"* 라고 값으로 적었고, 오늘 표본을 풀어 보니 **라벨 식을 지닌
>    컴포넌트 56 중 `.map(` 안에서 그려지는 것 15 · 그 안의 식 라벨 37**이 그 124 안에 있다(후보 3).
> ④ **어드민 조회 실패의 [다시 시도] 관례도 손 목록이다** — 손 목록 **열**인데 `page.tsx` 전수는
>    **열하나**이고, ⚠️ **그 목록의 주석은 *"열한 자리"* 라고 적는다**(목록은 열이다 · AG-4의 모양)
>    (후보 4).
> ⑤ **문서가 소스 좌표를 무는 반대 방향** — 좌표 전수 **929** 중 라운드 노트 902를 빼면 살아 있는
>    문서의 자리가 **27**이고, *누가 언제 옮기는지*가 적힌 것은 **하나**이며 ⚠️⚠️ **그 하나가 오늘
>    거짓이다**(후보 5).

---

## AG-1~AG-5의 질문 다섯 — 오늘의 답

⚠️⚠️ **다섯 다 답했고 넷이 발동한다**(AG-1 → 트랙 A·D · AG-4 → 트랙 B의 절반 · AG-5 → 트랙 E ·
AG-3 → 답만 내고 트랙이 되지 않는다). ⚠️ **수는 여기 옮겨 적되 *그 수를 어떻게 냈는지*(모집단 ·
바늘 · 사각)를 반드시 함께 가리킨다**(AC-5·AD-4가 세운 규율). ⚠️⚠️ **그리고 이 답들은 계약이
아니라 손으로 잰 수다** — 계약이 세는 수와 한 낱말로 적지 않는다.

| # | 자리 | 질문 | 2026-08-31 답(HEAD `f206005`) | 발동 |
| --- | --- | --- | --- | --- |
| 1 | AG-1 `:8615` | **관례를 세는 계약**이 몇이고, 그중 **모집단을 손 목록으로 적은 것**이 몇인가 | 테스트 계약 **400** 중 관례를 세는 계약 **62**(트리를 걷는 것 40 · 소스 경로를 배열로 적고 순회하는 것 32 · 겹침 10) · ⚠️⚠️ **모집단을 손 목록으로 적은 것 32**(자리 **47**) · 그중 **걷기가 한 줄도 없는 것 22** · ⚠️⚠️ **오늘 모집단 밖에 자리가 실재하는 것 둘을 이름으로 집었다**(`screen-header-back.test.ts` 손 열하나 ↔ 탭 밖 라우트 31 · `admin-load-error-copy.test.ts` 손 열 ↔ `page.tsx` 열하나) | ⚠️⚠️ **발동 → 트랙 A · 트랙 D** |
| 2 | AG-2 `:8654` | 재개 조건 중 **막는 것이 *배정*이라고 적힌 것**이 몇이고, 그중 **몇 라운드째 기다리는지가 적힌 것**이 몇인가 | 표기형 재개 조건 자리 **130**(전수 383) 중 그 항목이 *막는 것 = 배정/미배정*을 적은 것 **8** · 그중 같은 항목에 **경과가 함께 적힌 것 4** · ⚠️ **산문형 253 안의 같은 말은 이 바늘 밖이다** | ⚠️ 답했고 트랙이 되지 않았다(→ P3) |
| 3 | AG-3 `:8692` | 재개 조건 중 **리뷰가 세운 것**이 몇이고, 그 조건들이 **정찰·트랙·F 중 누구에게 처음 세어졌는가** | 저장소 전수 **599**(판정 문서 383 + 소스 216) 중 **리뷰가 세운 것 27**(문서 5 · 소스 22) — ⚠️⚠️ **판정 문서 쪽은 F 378 · 리뷰 5로 갈리고 트랙·정찰이 0건이며, 소스 쪽은 트랙 194 · 리뷰 22로 갈린다**(문서를 쓰는 손과 소스를 쓰는 손이 다르다) · ⚠️ **라운드 92 리뷰가 세운 소스 조건 셋을 오늘 이 정찰이 처음 센다**(keyboard-tap-guard 2 · resume-condition-ledger 1) — **셋 다 사건형이라 결정형 표는 늘지 않는다** | ⚠️ 답했고 트랙이 되지 않았다(→ P3) |
| 4 | AG-4 `:8735` | 소스 주석 중 **다른 자리를 인용한 것**이 몇이고, 그중 **인용당한 자리가 그 뒤에 바뀐 것**이 몇인가 | 주석이 파일을 인용한 자리 **581**(인용 쌍 **618** · 자기 인용 13 · 인용당한 고유 파일 **258**) · ⚠️⚠️ **그 주석 줄이 마지막으로 쓰인 뒤 인용당한 파일이 바뀐 쌍 319**(파일 **136**) — **바뀌었다는 것과 인용이 거짓이 됐다는 것은 다르므로 319는 상한이다** · ⚠️ **오늘 손으로 표본을 풀어 거짓을 하나 찾았다**(`merchantInputRef` 주석이 *"품목명 칸의 `itemNameInputRef`와 같은 관례"* 라며 *"포커스를 주는 데는 쓰지 않는다"* 고 적는데 `itemNameInputRef`는 정확히 포커스를 주는 데 **두 번** 쓰인다 · `new.tsx:1561`·`:2290`) | ⚠️ 답했고 **트랙 B의 절반**이 됐다 |
| 5 | AG-5 `:8774` | **문서가 소스의 좌표를 무는 반대 방향**이 몇이고, 그중 **누가 언제 옮기는지가 적힌 것**이 몇인가 | 좌표 전수 **929**(문서 38) — **라운드 노트 902 · 살아 있는 문서 27**(known-limitations **16** · runtime-verification **6** · a11y 체크표 **3** · round4 감사 **2**) · ⚠️⚠️ **이동 의무가 곁에 적힌 것은 하나뿐이고**(`known-limitations.md:2584`) ⚠️⚠️ **그 하나가 오늘 거짓이다**(무는 좌표 둘 다 밀렸다) · 손으로 스물일곱을 풀어 **오늘 거짓인 좌표 여섯**(자리 다섯) | ⚠️⚠️ **발동 → 트랙 E** |

### 답 1을 낸 방법

**모집단** = `apps/**`·`packages/**`·`scripts/**`의 테스트 파일 전수 **400**.
**바늘** = *관례를 세는 계약*은 ⓐ **소스 여럿을 한 모집단으로 삼고** ⓑ **자리마다 같은 규칙 하나를
묻는** 계약이다. 그 모집단이 어디서 오는지로 둘로 가른다 — ⓐ `readdirSync`로 트리를 **걷는** 것
**40** · ⓑ **소스 경로 문자열 셋 이상을 배열 상수로 적고 그것을 순회하는** 것 **32**(자리 47).
겹침 **10**이라 합집합은 **62**이고, **걷기가 한 줄도 없이 손 목록만인 것이 22**다.
⚠️⚠️ **그리고 그 32 가운데 *오늘 모집단 밖에 자리가 실재하는* 둘을 이름으로 집었다** —
`apps/mobile/src/screen-header-back.test.ts`의 `backWiredScreens`(**열하나** · `:31`)와
`apps/admin/src/admin-load-error-copy.test.ts`의 `RETRY_BUTTON_SCREENS`(**열** · `:425`)다.
⚠️ **사각 넷**: ⓐ **경로꼴 문자열이 셋 미만인 손 목록**(컴포넌트 이름 목록·문구 대장)은 이 바늘
밖이다 — 고치는 걸음이 손이라는 사실은 같은데 세어지지 않는다 · ⓑ **걷기가 픽스처 디렉터리인
계약도 40 안에 들어 있다**(걷는다는 사실만 보고 무엇을 걷는지는 묻지 않았다) · ⓒ **모집단이 손
목록이라는 것과 그 목록이 오늘 불완전하다는 것은 다르다** — 오늘 실피해를 확인한 것은 **둘뿐**이고
나머지 서른의 완전성은 세지 않았다 · ⓓ **테스트가 아닌 자리에서 관례를 지키는 계약**(설정·린트
규칙)은 밖이다. **예순둘도 서른둘도 하한이다.**

### 답 2를 낸 방법

**모집단** = `docs/operations/known-limitations.md`의 재개 조건 자리 **383** 가운데 **형을 괄호로
밝힌 표기형 130**(재개 조건 대장의 바늘 그대로 · 괄호 표기 **127** + 접힘·중복 표기의 차).
**바늘** = 그 조건이 사는 **항목 블록**(가장 가까운 불릿·표 행·제목부터 다음 블록 시작까지)이
*막는 것*을 **배정**으로 적었는가 — *"막는 것은 … 배정"* · *"그물 배정"* · *"아무 트랙의 것이
아니다"* · *"어느 트랙도 그 파일을 열지 않았다"* · *"축이 다르다"*.
**답은 8**이고, 그중 같은 블록에 **경과(N 라운드째/연속)가 함께 적힌 것은 4**다.
⚠️ **사각 셋**: ⓐ **산문형 253**(형 표기가 없는 조건) 안에서 같은 말을 하는 자리는 이 바늘 밖이다 ·
ⓑ **블록 경계가 불릿 단위**라 한 항목이 여러 문단이면 *배정*을 말한 문단이 잘려 나갈 수 있다 ·
ⓒ ⚠️⚠️ ***배정*이라는 낱말 없이 같은 뜻을 말한 자리**(*"이번 라운드의 축이 아니다"*)는 못 본다.
**여덟은 하한이다.**

### 답 3을 낸 방법

**모집단** = 재개 조건 자리 전수 **599** — 판정 문서 **383**(대장의 모집단)과 **소스 216**
(`apps`·`packages`·`scripts`의 `.ts`·`.tsx`·`.mjs`). ⚠️ **짝 문서 17**(runtime 6 · a11y 11)**과
라운드 노트 377은 대장이 값으로 든 모집단 밖이라 여기서도 밖이다.**
**바늘** = `git blame`으로 그 줄을 **마지막으로 쓴 커밋**의 티켓 접미를 읽어 손을 가른다 —
`GAP-\d+R`=리뷰 · `GAP-\d+F`=F · `GAP-\d+[A-E]`=트랙 · `GAP-\d+)`=정찰.
**답**: 판정 문서 **F 378 · 리뷰 5 · 트랙 0 · 정찰 0** / 소스 **트랙 194 · 리뷰 22**.
⚠️⚠️ **그 갈림 자체가 이 답의 값이다 — 판정 문서를 쓰는 손은 F 하나이고 소스에 조건을 적는 손은
트랙이며, 리뷰만 양쪽에 선다.** 그래서 AG-3이 말한 *"리뷰가 세운 조건은 다음 정찰이 처음 센다"* 는
**소스 쪽에서 훨씬 크다**(문서 5 ↔ 소스 22).
⚠️ **오늘 처음 세는 것 셋**: `apps/mobile/src/keyboard-tap-guard.test.ts:276`·`:874`(리뷰 H-3·L-3이
사각을 다섯에서 **여섯**으로 늘리며 붙인 조건)과
`packages/test-utils/src/resume-condition-ledger.ts:1250`(리뷰 M-1의 두 시점 기록). **셋 다 사건형**
이라 결정형 표는 늘지 않는다.
⚠️ **사각 셋**: ⓐ **blame은 *그 줄을 마지막으로 다시 쓴* 커밋을 가리킨다** — 리뷰가 *고친* 줄도
리뷰로 세어지므로 **오차의 방향은 리뷰 과다**다 · ⓑ **표식(`리뷰 L-6`)만 세면 또 다른 수가 나온다**
(문서 19 · 소스 15 · 표식과 blame은 서로 다른 것을 센다 — **두 수를 한 낱말로 적지 않는다**) ·
ⓒ **라운드 노트 377과 짝 문서 17은 밖이다.** **스물일곱은 하한이다.**

### 답 4를 낸 방법

**모집단** = `apps`·`packages`·`scripts`의 `.ts`·`.tsx`·`.mjs`·`.js` 가운데 **주석 줄**
(`//` · `*` · `/*`로 시작하는 줄).
**바늘** = 그 줄의 백틱 안에 **저장소에서 실제로 풀리는 파일 경로**가 있는가(앱 루트·패키지 루트·
같은 디렉터리를 후보로 푼다). **자리 581 · 인용 쌍 618 · 자기 인용 13 · 인용당한 고유 파일 258.**
그다음 `git blame`으로 그 주석 줄의 커밋 시각과 인용당한 파일의 마지막 커밋 시각을 견준다 —
**뒤쪽이 더 늦은 쌍이 319**(파일 136 · 가장 큰 자리는 `a11y-contract.test.ts` 41).
⚠️ **사각 넷**: ⓐ ⚠️⚠️ **파일이 바뀌었다는 것과 인용한 사실이 거짓이 됐다는 것은 다르다** —
**319는 상한이고 실제 낡음은 그 부분집합이다**(AG-4가 잡은 하나도 이 상한 안에 있었다) ·
ⓑ **주석이 아니라 *문자열 산문*으로 인용한 자리**(계약이 이유·사각을 문자열로 적는 그 층)는 밖이다
— **이 저장소에서 그 층은 주석 층보다 크다** · ⓒ **경로 없이 파일 이름만 적은 인용 200**은 풀리지
않아 밖이다 · ⓓ **문서→소스 방향은 이 바늘 밖이다**(그쪽은 답 5가 센다).
⚠️ **오늘 손으로 하나를 열어 거짓을 확인했다**: `app/expenses/new.tsx:576-579`의 `merchantInputRef`
주석이 *"포커스를 **주는** 데는 쓰지 않는다 — 품목명 칸의 `itemNameInputRef`와 **같은 관례**"* 라고
적는데, `itemNameInputRef`는 `:1561`과 `:2290`에서 정확히 **포커스를 주는 데** 쓰인다.
⚠️⚠️ **그 문장은 라운드 92 *리뷰*가 어제 적은 것이다** — **낡음이 아니라 태어날 때부터 거짓이었고,
AG-4의 처방(두 시점)으로는 잡히지 않는 갈래다.**

### 답 5를 낸 방법

**모집단** = `docs/**`의 `.md` 전수.
**바늘** = `<소스경로>:<줄>` 꼴 좌표 가운데 **그 파일이 오늘 실재하는 것**(문서→문서 좌표는 뺀다).
**전수 929 · 문서 38.** 갈래는 둘이다 — **라운드 노트 902**(작업 기록이라 재개 조건 대장의
`round-notes`와 같은 이유로 모집단 밖에 둘 만하다)와 **살아 있는 문서 27**:
`known-limitations.md` **16** · `runtime-verification-required.md` **6** ·
`accessibility-offline-checklist.md` **3** · `round4-production-readiness-audit.md` **2**.
그중 **±2줄 안에 *누가 언제 옮기는지*가 적힌 것은 1**(`known-limitations.md:2584`).
⚠️⚠️ **그리고 그 하나가 오늘 거짓이다** — 그 줄은 *"`monthlyUsed`는 `app/(tabs)/index.tsx:1482`에서
만들어지고 훅 호출은 `:1260`이라 **둘 중 하나를 옮겨야 한다**"* 고 적는데, 오늘 `monthlyUsed`가
서는 자리는 **`:1519`**이고 `:1260`은 예산 훅이 아니라 대기 행 주석이다.
**손으로 스물일곱을 풀어 오늘 거짓인 좌표는 여섯**(자리 다섯):
`kl:245`→`app/items/[itemTemplateId].tsx:997`(실제 **1069**) · `kl:254`→`app/(tabs)/index.tsx:2105`
(실제 **2142**) · `kl:413`→`app/settings/privacy.tsx:150`의 `loadFailedText`(**그 심볼이 0건** —
`destructive-flow-messages.test.ts:447`이 오히려 *없음*을 문다) · `kl:414`→`app/import/[importJobId].tsx:131`의
`loadFailedText`(**그 심볼이 0건** — 공용 `useLoadErrorCopy`로 옮겨 갔다) · `kl:2584`의 좌표 **둘**.
⚠️ **사각 넷**: ⓐ **좌표가 실재하는가만 기계가 보고, *그 줄이 그 사실을 말하는가*는 손으로만 풀었다**
(여섯은 손이 낸 하한이다) · ⓑ **라운드 노트 902는 모집단 밖으로 두었고, 그 판단 자체가 손이다**
(⚠️ 그중 **둘은 파일 길이를 넘는 죽은 좌표**다 — `round65-scout.md:195`·`round67-scout.md:88`이
`app/(tabs)/records.tsx:1831`·`:1884`를 가리키는데 그 파일은 오늘 **1,774줄**이다) ·
ⓒ **절 이름·`#N` 꼴로 무는 인용**(문서가 소스의 *상수 이름*을 적는 자리)은 이 바늘 밖이고 그 수는
훨씬 크다 · ⓓ **AG-5가 센 *"이 계약이 읽는 문서 셋만 세어도 서른셋"* 과 오늘의 **25**(같은 문서 셋)는
바늘이 다르다 — **두 수를 한 낱말로 적지 않는다.** **스물일곱도 여섯도 하한이다.**

---

## 선행 확인 (후보 아님)

1. **라운드 92의 다섯 트랙 + F + 적대 리뷰는 전부 머지돼 있다**(실측 · 파일 실재와 값 둘 다 확인).
   A=`apps/mobile/src/keyboard-tap-guard.test.ts`(**신설 앱 내 스윕** · 오늘 실측 **스크롤 컨테이너
   17 · 명시 11 · 키보드 못 뜸 6 · 미가드 0** · ⚠️ **리뷰 H-2가 `applyMerchantSuggestion`에
   `merchantInputRef.blur()`를 복원했고 그 배선을 이 스윕이 계약으로 문다**),
   B=`apps/mobile/src/a11y-contract.test.ts`의 행 라벨 스윕(**files 58 · labels 162 · 목록 안 38 ·
   direct 36 · derived 2 · invariant 0 · 상수 라벨 73** — ⚠️ **리뷰 H-3이 셋째 바늘을 세우며
   35·34·1을 38·36·2로 정정했고 목록 밖은 127 → 124가 됐다**),
   C=`apps/admin/src/admin-route-surface.test.ts`(**신설 앱 내 스윕** · `page.tsx` **11** ↔
   `NAV_ITEMS` **11** · 어긋남 0 · 동적/그룹/병렬/`route.ts` **0건**),
   D=`packages/test-utils/src/resume-condition-ledger.ts`의 경과 축(오늘 실행값
   **sites 383 · ownLine 4 · window 79 · neither 304** · 읽지 못한 수사 **`['몇']`** ·
   래칫 **3 · 61**),
   E=짝 계약 둘의 이동 의무(`accessibility-checklist-shape.test.ts` ·
   `runtime-checklist-shape.test.ts` · **절 이름 등호 8 · 줄 번호 배열 1 · 의무 빈 자리 0**),
   F=AG절(`:8140~8777`), R=`bbf1d97`(리뷰 열셋).
2. **테스트는 오늘 HEAD에서 초록이다**(재실행) — **mobile 246파일 5,051** · **admin 42파일 734** ·
   **test-utils 17파일 552**(그중 대장 셋 `dead-export`·`contract-net`·`comment-tolerant-anchor`가
   **152**). ⚠️ **api 850 · domain 131 · contracts 66은 다시 돌리지 않았고 상태 문서의 값이다**
   (총 **7,384**).
3. **계약 그물은 오늘도 열다섯이다** — `contract-net-ledger.test.ts` 초록 · 두 대장의
   `CONTRACT_NETS_BEFORE_THIS_ONE`이 갈리지 않는다. ⚠️ **라운드 92가 세운 신설 스윕 둘
   (`keyboard-tap-guard` · `admin-route-surface`)은 앱 하나만 걷는 스윕이라 이 수에 들지 않는다.**
4. **앵커 대장은 여덟 번째로 한 자리도 다르지 않다** — 전수 **698**(code-only 620 ·
   comment-tolerant **70**/래칫 70 · comment-only **8**/래칫 8 · unanchored **0**) · 면제 **8**.
   ⚠️ 사각 `helper-named-reader`는 오늘 **166**이고 대장이 지닌 하한은 **164**다(**세 수를 한
   낱말로 적지 않는다**).
5. **사문 대장의 절반 문턱은 여유 0 그대로다** — `.tsx`의 `export function` 오늘도 **141**
   (일곱 라운드째 같은 수) · `derived-exemptions` **44 중 22**. ⚠️⚠️ **그러므로 이 라운드의 어느
   트랙도 새 `export const`를 세우지 않는다**(필요하면 **`export function`**).
6. **DNC 계약 재확인** — 준비템 목록 가격 표시는 **사용자 결정 대기 잠금**이고 이 노트는 제안하지
   않는다. 추천 점수에 수수료율 0건(DNC-009) · 제휴 고지 문구 바이트 불변(DNC-010) · 스폰서 구분
   표시 그대로(DNC-011).
7. **외부 계정·키·자산이 필요한 자리는 후보에 넣지 않았다** — Play · 카카오 · Oracle VM ·
   Cloudflare · EAS · 실기기. 다섯 후보 전부 **저장소 안의 손**으로 닫힌다.

---

## 상위 후보

### 1. **동기화 상태 화면에는 나가는 길이 없다 — 나가는 길 계약의 모집단이 손 목록 열하나여서 스물두 라운드 동안 보이지 않았다** — 모바일·핵심 루프 1단계 직후 — S

- **사실.** `apps/mobile/app/sync-status.tsx:779`는 `<ScreenHeader eyebrow="동기화" title="동기화 상태" …/>`
  를 **`onBack` 없이** 세운다. 앱은 전역 `headerShown: false`(`app/_layout.tsx:86`)라 OS 헤더가
  없으므로, **화면 안의 나가는 길은 `ListEmptyComponent`의 `EmptyStateCard actionLabel="닫기"`
  하나뿐이고 그것은 목록이 *빌 때만* 선다**(`:807-813`).
  ⚠️⚠️ **즉 대기·실패·충돌 행이 하나라도 있으면 나가는 길이 0개다** — 그리고 이 화면은 **행이 있을 때
  보라고 만든 화면**이다.
- ⚠️⚠️ **앱이 사용자를 그 자리로 보낸다는 것이 이 후보의 값 절반이다.**
  `src/expenses/post-save-destination.ts:80`의 `POST_SAVE_SYNC_STATUS_DESTINATION`이 있고,
  `resolvePostSaveDestination`이 **실패 행 다시 쓰기**(`SYNC_FIX_ENTRY_SOURCE`) 뒤의 목적지로 그
  화면을 고른다(`:156`). **핵심 루프 1단계에서 실패한 기록을 고쳐 저장한 사람이 정확히 그 화면에
  도착하고, 그때 그 목록은 비어 있지 않다.** 진입점은 그 밖에도 셋이다 —
  기록 탭 `:228`·`:1358`, 홈의 `SyncStatusBar`(`app/(tabs)/index.tsx:2544`).
- ⚠️⚠️ **계약이 이 자리를 한 번도 읽지 않은 이유가 AG-1의 그 병이다.**
  `apps/mobile/src/screen-header-back.test.ts:31`의 `backWiredScreens`는 **손으로 적은 열하나**이고,
  그 위 주석(`:27-30`)은 *"스택으로만 도달하는 화면 중 나가는 길이 없는 곳은 이제 없다"* 고 **모집단
  전체를 두고 단언한다** — 그런데 그 계약이 세는 것은 **목록에 적힌 열하나가 관례를 지키는가**이지
  **관례를 져야 하는 자리가 이것뿐인가**가 아니다.
- **오늘의 전수.** `apps/mobile/app/**`의 라우트 파일 **36** · 탭 밖 **31** · 그중 `ScreenHeader`를
  쓰는 것 **18** · `onBack`을 배선한 것 **11**(= 손 목록과 정확히 같다) ·
  ⚠️ **`ScreenHeader`를 쓰는데 `onBack`이 없는 것 7**:
  `app/(onboarding)/{budget,child-profile,child-status,prepared-items,resume}.tsx` **다섯**(선형
  온보딩이라 뒤로가기가 없는 것이 판단이고, **일곱 중 여섯이 그 갈래다**) ·
  `app/family/accept/[token].tsx`(딥링크 착지 · 화면 안에 *"이전"* 문구가 따로 있다) ·
  ⚠️⚠️ **`app/sync-status.tsx`(그 어느 쪽도 아니다)**.
  ⚠️ **`app/onboarding/**` 다섯은 한 줄 재수출**(`export { default } from "../(onboarding)/…"`)이라
  화면이 아니고, 그 사실도 파생으로 갈린다.
- **왜 지금인가.** 라운드 55가 손 목록에 둘을 더한 뒤로 **스물두 라운드** 동안 이 목록은 자라지
  않았고, `sync-status.tsx`는 그보다 먼저(MOB-102/103) 서 있었다. **주석이 *"이제 없다"* 라고 적은
  덕에 이 자리는 *이미 해결된 것*으로 읽혔다** — AG-4가 이름 붙인 그 착시의 소스판이다.
- **고치는 값.** ⓐ `sync-status.tsx`의 `<ScreenHeader>`에 **`onBack` 한 줄**(그 화면이 이미 쓰는
  `router.back()`) — 다른 바이트는 불변이고 픽셀락 밖이다. ⓑ 계약의 모집단을 **손 목록에서 전수
  파생으로** 바꾸고 자리마다 판정 하나를 소스에서 낸다(*배선한다* · *뒤로가기가 없는 것이 판단이다
  + 이유* · *스택인데 나가는 길이 없다* = **트랙 뒤 0건**).
- **크기** S(속성 한 줄 + 스윕 하나) · **위험** 낮음(새 문구 0글자 · 새 낭독은 공용 `ScreenHeader`가
  이미 지닌 `"뒤로가기"` 하나 · 서버 0바이트).

### 2. **사용자가 지은 이름 뒤에 조사가 고정으로 붙어 문장이 깨진다 — 저장소는 그 답을 세 자리에서 골랐고 네 자리에서 고르지 않았다** — 모바일·사용자 문장 — S~M

- **사실.** 사용자가 직접 친 이름을 문장에 넣고 그 뒤에 **받침에 따라 갈려야 하는 조사**를 고정으로
  붙이는 자리가 넷이다.
  - `app/settings/children.tsx:502` `const addedNotice = \`${nickname.trim()}를 추가했어요.\``
  - `app/settings/children.tsx:505` `announceForA11y(\`${nickname.trim()}를 추가하고 선택했어요.…\`)`
  - `app/family/accept/[token].tsx:307` `const joinedText = \`${result.household.name}과 함께해요.\``
  - `app/family/accept/[token].tsx:444` 같은 문장을 화면이 다시 그린다.
  ⚠️⚠️ **아이 별명이 받침으로 끝나면 *"지훈를 추가했어요"* 가 되고, 가구 이름이 받침 없이 끝나면
  *"김가네과 함께해요"* 가 된다.** 앞의 둘은 **화면 문구와 낭독 둘 다**이고(`announceForA11y`),
  ⚠️ **아이 추가는 온보딩 직후 사람이 가장 먼저 만드는 문장이다.**
- ⚠️⚠️ **저장소는 이 물음에 이미 답을 골랐고, 그 답은 조사를 고르는 것이 아니라 *값의 꼬리를
  고정하는 것*이다** — 세 자리 이상에서 같은 답이다.
  - `src/family/household-scope.ts:238` `householdScopePhrase` — 가구 이름을 그대로 쓰지 않고
    **`‘{이름}’ 가구`** 로 끝맺는다. 그래서 `:394`의 `${phrase}를`, `:399`의 `${phrase}로`,
    `householdScopeAddChildNotice`의 `${phrase}에`가 **전부 옳다.**
  - `app/family/index.tsx:452` `pendingInviteTarget` — 끝을 **`… 초대`** 로 고정한다. 그래서
    `:459`의 `${…}를 취소할까요?`가 옳다.
  - `src/offline/messages.ts:126` `recordsCountPhrase` — **`기록 N건`** 으로 끝난다. 그래서
    `${…}은` 계열 넷이 전부 옳다.
  - 같은 모양이 더 있다: `record-row-actions.ts:142`·`notification-row-actions.ts:87`은 목록의
    **마지막 원소가 고정**(*삭제* · *지우기*)이라 `${…}를`가 옳고, `milestoneLabel`은 둘 다 받침
    ㄹ(*100일* · *첫돌*)이며, 금액은 항상 **`…원`** 으로 끝난다.
- **오늘의 전수.** 템플릿 보간 `}` 바로 뒤에 받침 의존 조사가 오는 자리 **32**(모바일·어드민·
  패키지 · 조사별 `를` 7 · `이` 6 · `은` 5 · `을` 4 · `가` 3 · `으로` 3 · `과` 2 · `로` 2)와
  괄호가 낀 꼴 **1**(`budget-edit.ts:355`) — 합 **33**. ⚠️⚠️ **그중 값의 꼬리가 고정이라 안전한 것이
  29이고, 갈리는 것이 넷이다.** **세는 계약은 0건이다.**
- **고치는 값.** ⓐ 넷의 문장을 저장소가 이미 고른 답으로 맞춘다 — **받침에서 조사를 고르는 순수
  함수 한 벌**(한글 음절은 `(코드 − 0xAC00) % 28`로 받침이 갈린다)이거나, 값의 꼬리를 고정 명사로
  끝맺는 종전 관례다. ⚠️ **정찰은 앞쪽을 권한다** — 뒤쪽은 화면 문구를 새로 짓게 되고, 앞쪽은
  *"지훈을"* · *"서아를"* 처럼 사람이 기대하는 문장을 낸다. ⓑ 전수 스윕 하나가 **33자리마다 판정
  하나**를 소스에서 내고, *갈리는데 고정으로 적은 자리*가 **트랙 뒤 0건**임을 부정 단언으로 문다.
- ⚠️ **주의(사각으로 값이 되어야 한다).** 이름이 **숫자·라틴 문자·이모지**로 끝나면 받침 판정이
  성립하지 않는다 — 그 갈래의 답(종전 조사를 그대로 둔다)을 값으로 적어야 한다.
- ⚠️ **문서 한 줄이 함께 움직인다** — `docs/qa/runtime-verification-required.md:638`(#47)이 그 토스트를
  *"…를 추가했어요."* 로 인용한다. **그 줄은 F의 것이다**(두 시점으로 옮겨 적는다).
- **크기** S~M · **위험** 낮음(서버 0바이트 · 새 화면 0건 · 픽셀락 밖).

### 3. **행마다 갈리는 낭독 라벨의 사각 ⓑ가 실재한다 — 목록 밖 124 안에 *행*이 있다** — 모바일·a11y 그물 — S~M

- **사실.** 라운드 92 B(+리뷰 H-3)가 사각 ⓐ를 **두 시점**으로 고쳐 적으며 이렇게 남겼다:
  *"남는 124의 성격은 하나가 아니라 둘이다 … ⓑ **행이면서도 이 바늘 밖인 자리가 남아 있다** …
  그 둘을 가르는 일은 이 축이 아직 하지 않았고, 그래서 124는 **묻지 않은 수**이지 **물을 것이 없는
  수**가 아니다."* ⚠️⚠️ **오늘 그 갈래를 표본으로 풀었고, 실재한다.**
- **오늘의 표본.** `apps/mobile`의 비테스트 `.tsx`에서 **식 라벨(`accessibilityLabel={`)을 지닌
  컴포넌트 56** 가운데 **호출부가 `.map(` 안인 것이 15**이고, 그 컴포넌트들이 지닌 **식 라벨 자리는
  37**이다. 오늘의 세 바늘(인라인 `.map` 콜백 · `renderItem={({item…` · `renderItem={이름}`)은
  **행 컴포넌트를 *렌더 함수에서 한 걸음*까지만** 따라가므로, **`.map` 안에서 직접 그려지는 행
  컴포넌트의 본문**은 모집단 밖이다.
  ⚠️ **그중 핵심 루프에 걸리는 자리가 셋이다** —
  `app/expenses/new.tsx:377 ExpenseQuickItemButton`(빠른 품목 타일 · 라벨 11 · **1단계**) ·
  `src/design-system/components/ModV1Primitives.tsx:150 PreparationItemCard`(준비템 카드 ·
  **3단계**) · `src/ui.tsx`/`ApplicationPrimitives.tsx`의 `CategoryChip`·`ListRow`(분류 칩과
  구매 링크 행). 그 밖에 `app/(tabs)/more.tsx:78 MoreMenuRow`(라벨 4) ·
  `app/family/index.tsx:102 FamilyInviteRow`(6) · `app/import/index.tsx:86 ImportPreviewCategoryRow`(3) ·
  `src/expenses/RecordsCalendar.tsx:170 CalendarDayCell`(2)가 있다.
- ⚠️ **이 표본은 *겹치지 않는 새 자리*만 고른 것이 아니다** — 리뷰 H-3이 데려온 셋(기록 탭 지출
  행·섹션 헤더·가져오기 검수의 잠긴 행)은 `renderItem={이름}` 바늘이 이미 잡으므로 **오늘의 38
  안**이고, 위의 15는 그 밖이다. **두 수를 한 낱말로 적지 않는다.**
- **고치는 값.** 라벨을 바꾸는 것이 아니라 **넷째 바늘 하나**다 — *`.map(` 안에서 그려지는 행꼴
  컴포넌트의 본문*을 모집단으로 데려오고, 자리마다 오늘의 판정 셋(직접 · 한 걸음 파생 · 갈리지
  않음 + 이유)을 그대로 낸다. ⚠️⚠️ **화면 0바이트가 이 트랙의 조항이다** — 갈리지 않는 자리가
  서면 그것은 이 트랙의 관측이지 이 트랙의 수정이 아니다(재개 조건이 이미 그 손을 지목한다).
- **크기** S~M · **위험** 낮음(읽기만) · ⚠️ **한 트랙이 한 그물에 축 둘을 얹지 않는다** — 이 트랙이
  a11y-contract에서 여는 축은 **행 라벨 스윕의 넷째 바늘 하나**다.

### 4. **어드민 조회 실패의 [다시 시도] 관례도 손 목록이고, 그 목록의 주석은 이미 자기 수와 갈렸다** — 어드민·계약 — S

- **사실.** `apps/admin/src/admin-load-error-copy.test.ts:424`의 주석은 *"조회 실패 배너 아래
  [다시 시도]가 서는 **열한 자리**"* 라고 적는데, 바로 아래 `RETRY_BUTTON_SCREENS`(`:425`)는
  **열 줄**이다. 그리고 어드민의 `page.tsx` 전수는 **열하나**다.
- **오늘의 전수.** `apps/admin/app/**/page.tsx` **11** 가운데 `styles.retryButton`을 지닌 것 **10**,
  없는 하나는 `app/users-lookup/page.tsx`다 — ⚠️ **그리고 그 자리는 결함이 아니다**: 검색형 화면이라
  같은 파일의 다음 `it`이 이미 *"[다시 시도]가 없는 자리는 문장만 받는다"* 로 그 넷을 이름으로 적는다
  (`loadErrorMessage(error, "사용자를 조회하지…")`). **오늘 실피해는 0건이다.**
- ⚠️⚠️ **그러므로 이 후보의 값은 *고칠 것*이 아니라 *열두 번째 화면이 서는 날*에 있다.** 손 목록이
  모집단이면 **조회 실패 배너를 [다시 시도] 없이 세운 새 화면이 조용히 통과한다** — 라운드 92 C가
  세운 `admin-route-surface.test.ts`가 `page.tsx` 전수를 이미 파생하므로, **같은 파생을 이 관례에
  쓰면 그날 빨개진다.**
- **고치는 값.** ⓐ 모집단을 `page.tsx` 전수 파생으로 바꾸고 자리마다 판정 하나(*[다시 시도]가 선다* ·
  *문장만 받는다 + 이유*)를 소스에서 낸다. ⓑ ⚠️ **낡은 수사 하나를 두 시점으로 정정한다**
  (*"열한 자리"* → 오늘의 사실 · AE-3의 관례). ⓒ **어드민 소스 0바이트**.
- **크기** S · **위험** 낮음.

### 5. **문서가 소스 좌표를 무는 반대 방향 — 이동 의무가 적힌 유일한 자리가 그 의무를 지키지 않았다** — 계약·문서 — S~M

- **사실.** AG-5가 사각 ⓑ로 *"문서가 소스의 좌표를 무는 반대 방향은 이 바늘 밖이고 그 수는 훨씬
  크며 **옮기는 손도 반대다**"* 라고 적었다. 오늘 그 방향을 세니 **929**이고, 라운드 노트 902를 빼면
  **살아 있는 문서의 자리가 27**이다. ⚠️⚠️ **이동 의무가 적힌 것은 하나(`known-limitations.md:2584`)
  이고, 그 하나가 오늘 거짓이다** — 그 줄이 *"둘 중 하나를 옮겨야 한다"* 라고 적은 두 좌표
  (`app/(tabs)/index.tsx:1482`·`:1260`)가 **둘 다 밀렸다**(`monthlyUsed`는 오늘 `:1519`다).
- ⚠️ **손으로 스물일곱을 풀어 오늘 거짓인 좌표 여섯**(자리 다섯 · 답 5 참고). 두 갈래다 —
  ⓐ **줄이 밀린 것 넷**(997→1069 · 2105→2142 · 1482→1519 · 1260) ⓑ **인용한 심볼이 사라진 것 둘**
  (`loadFailedText`가 privacy·import 두 화면에서 공용 `useLoadErrorCopy`로 옮겨 갔다 —
  ⚠️⚠️ **`destructive-flow-messages.test.ts:447`은 그 심볼이 *없음*을 계약으로 물고 있고, 문서는
  그것이 *있다*고 적는다 — 계약과 문서가 정반대를 말한다**).
- **고치는 값.** ⓐ 계약 하나가 **살아 있는 문서 넷의 좌표 자리를 전수 파생**하고, 자리마다
  ⓐ 그 파일이 실재하는가 ⓑ 그 줄이 파일 길이 안인가 ⓒ 이동 의무가 곁에 적혀 있는가를 값으로 문다
  (⚠️ **오늘의 값 27 · 27 · 1** · **셋 다 하한으로** — 등호로 물면 F가 절을 늘릴 때마다 빨개진다 ·
  AF-5·AG-5가 값으로 적은 그 비용). ⓑ ⚠️⚠️ **좌표 여섯의 *정정*은 이 트랙의 것이 아니라 F의 것이다**
  — `docs/operations/**`·`docs/qa/**`는 F가 소유하고, **옮기는 손이 반대라는 사실이 정확히 AG-5가
  적어 둔 그 갈림이다.**
- ⚠️ **새 그물을 세우지 않는다** — 이 축은 문서↔사실 정합을 이미 지고 있는
  `packages/test-utils/src/repo-self-description.test.ts`에 **축 하나**로 얹는다(그물 수는 오늘도
  **열다섯**이다).
- **크기** S~M · **위험** 낮음(읽기만 · 소스 0바이트 · 문서 0바이트).

---

## P3 (재었고 이번 라운드에 제안하지 않는다 — 전부 재개 조건과 함께)

⚠️⚠️ **AG절 머리말의 결정형 전수표는 열여섯이었고, 오늘 HEAD에서 다시 세니 열다섯이다.**
⚠️ **줄어든 하나는 새로 닫힌 것이지 사라진 것이 아니다** — **#16(라우트 표면과 내비 표의 짝)이
라운드 92 트랙 C에서 집혔고**, `admin-landmark-current.test.ts:524-530`의 그 조건이 **두 시점으로
*"그날 이후의 재개 조건은 사건형으로 바뀐다"* 를 자기 안에 적었다**(그래서 결정형 표에서 내려온다).
⚠️⚠️ **그리고 라운드 92 리뷰가 세운 조건 셋은 전부 사건형이라 열여섯째가 새로 서지 않았다** —
**AG-3이 예고한 *리뷰가 세운 조건이 다음 표에 선다*가 이번 라운드에는 일어나지 않았고, 그 0이 값이다.**

| # | 결정형 조건 | 사는 곳 | 손 | 오늘의 처분(2026-08-31 실측) |
| --- | --- | --- | --- | --- |
| 1 | 막대를 포커스 가능하게 | AA-2 | 안 | **집지 않는다 — 값 0.** 두 화면(`app/analytics/page.tsx:529` · `app/clicks/page.tsx:206`) 다 `trend.showTable` 표가 값을 텍스트로 준다(재실측 · 변화 0) |
| 2 | 기록 탭 검색의 분류 갈래 | Z절 | 안 | **집지 않는다 — 어긋남 0건 · 아홉 라운드 이어 같은 답.** `RECORDS_SEARCH_FIELDS_LABEL`이 여전히 *"품목명, 판매처, 메모"* 하나이고 placeholder가 그 값에서 파생한다(`records-list-view.ts:841`·`:847`) |
| 3 | 준비템 분류 필수 입력 | Z-3 | **밖**(카탈로그 정책) | 집지 않는다. 변화 0 |
| 4 | 서버의 중복 대기 초대 방지 | AA-4 | **밖**(초대 정책) | 집지 않는다. 변화 0 |
| 5 | 감사 뷰의 대상 축 필터 | AA절 기각 | **밖**(서버 DTO 0건) | 집지 않는다 — ⚠️ **`apps/api/**`가 두 라운드 연속 접점 0건**(라운드 91이 다섯 라운드 만에 열었고 92·93 제안 다 그 밖이다) · 서버 소스와 DTO는 여전히 0바이트다 |
| 6 | 타일 안 배지 | AA-1 | **밖**(디자인 승인) | 집지 않는다. 변화 0 |
| 7 | C-3 잠금 오버레이 TalkBack 투과 | 접근성 표 C-3 | **밖**(사람·기기·날짜) | **정찰의 소유 밖이다** — 이 라운드가 서면 **스물일곱 라운드째 미확인**(67~93) |
| 8 | JSX 사용을 참조로 세는 판정 | `dead-export-ledger.ts` | 안 | **집지 않는다 — 일곱 라운드 이어서다.** 막는 것은 **크기와 문턱의 겹침** 그대로다(`.tsx`의 `export function` 오늘도 **141** · `derived-exemptions` **44 중 22 · 여유 0**). ⚠️ 그 파일은 이번에도 아무 트랙의 것이 아니다 |
| 9 | 재조회 실패 창에서 옛 목록을 남길지 | AC절 기각 | 안 | **집지 않는다 — 화면 구조 결정이고 오늘도 접점 0건**(`src/offline/**` 무접촉) |
| 10 | 앵커 대장의 `helper-named-reader` | `comment-tolerant-anchor-ledger.ts` | 안 | **집지 않는다 — 래칫이다.** 앵커 다섯 자리(**698 · 620 · 70/70 · 8/8 · 0**)와 면제 **8**은 **여덟 번째로 한 자리도 다르지 않고**, 그 사각의 크기는 오늘도 **166**(하한 **164**) — ⚠️ **이번 라운드의 다섯 트랙 가운데 소스 리더 이름을 새로 세우는 것은 후보 2의 스윕 하나이고, 그 하나가 이 수를 움직일 수 있다** |
| 11 | 재개 조건 대장의 `prose-only` | `resume-condition-ledger.ts` | 안(**F**) | **집지 않는다.** 오늘 **208**(119 → 145 → 165 → 184 → 208 · ⚠️ **대장이 지닌 기록값은 184이고 오늘 값은 208이다 — 하한 60 설계 덕에 초록이다**). 소급 적용은 한 라운드의 F가 질 수 없다 |
| 12 | 재개 조건 대장의 `round-notes` | `resume-condition-ledger.ts` | 안 | **집지 않는다 — 관례 0건.** 오늘 **377**(노트 51벌)이고 ⚠️ **이 노트가 서면 또 늘어난다**(라운드마다 한 벌씩 자라는 사각이고, 그 사실이 이 줄의 값이다) |
| 13 | 그물 파일이 자기를 이름으로 선언하는 표식 | `contract-net-ledger.test.ts` | 안 | **집지 않는다 — 관례를 새로 정하는 일이다.** 표식 **0건** · ⚠️ **자기 배제 관례를 손으로 적은 자리는 오늘도 열넷 · 이름 넷**(`LEDGER_SELF_FILES` 4 · `SWEEP_SELF_FILES` 2 · `SELF_PATH` 4 · `SELF_FILE` 4) · **정합을 세는 자리 0건**(AG절의 열넷과 같다 — 변화 0) |
| 14 | 공유 테스트 DB를 언제 무엇으로 비울지 | `harness-catalog-cost.test.ts` | 안 | **집지 않는다 — 술어는 오늘도 참이다.** `wooriai_test`의 `item_templates` **9,439** = **생성 접두 9,377 + 그 밖 62**(겹침 **0**) · **738MB · 활성 8,797**. ⚠️⚠️ **라운드 92 F의 732MB · 9,402 · 8,765에서 셋 다 또 늘었고, 이번에 늘린 손은 *리뷰의 실행*이다**(세 시점 → **네 시점**) |
| 15 | 조건의 *도래*를 값으로 가르는 관례 | `resume-condition-ledger.ts` | 안 | **집지 않는다 — 관례를 정하는 일이다.** 기다리는 자리는 오늘도 **둘**(대장의 `typed-but-unverified`와 경과 축의 사각 ⓑ · 창 **79** 전부가 그 사각 안이다) |

⚠️⚠️ **이 표가 라운드 92의 표와 다른 점 셋.** ⓐ **열여섯이 열다섯이 됐고 줄인 것은 *집힘*이다**
(#16 · 라운드 92 트랙 C). ⓑ ⚠️ **이번 라운드도 결정형에서 집지 않는다** — 다섯 후보 가운데 결정형
조건을 소진하는 것은 0건이고, **집는 것이 0건인 라운드가 다시 시작된다**(라운드 92가 다섯 라운드
연속을 끊었으므로 오늘은 *한 라운드째*다 · **끊김과 끊긴 기간은 다른 두 칸이다**). ⓒ ⚠️⚠️ **#10은
이번 라운드에 처음으로 *트랙이 움직일 수 있는* 자리가 됐다** — 후보 2의 스윕이 소스 리더를 세우면
그 사각이 166에서 오른다. **그 사실을 미리 값으로 적어 두면 다음 F가 그 오름을 *부채*로 읽지 않는다.**

### 그 밖에 재었고 이번 라운드에 제안하지 않는 것 — 전부 재개 조건과 함께

- **AG-2의 축(막는 것이 배정인 조건 여덟)** — 재었고 트랙이 되지 않는다. ⚠️ **여덟 중 넷만 경과를
  함께 적는데, 그 넷을 세는 자는 이미 있다**(경과 축 · 라운드 92 D)**와 겹치므로 새 축이 아니라
  *두 축의 교집합*이다.** ⚠️ **재개 조건(결정형 · 손은 저장소 안): 경과 축과 *막는 것* 축을 한
  대장이 함께 질지를 정하는 날 — 그날 먼저 물을 것은 *배정을 말한 산문 253을 어떻게 셀 것인가*다.**
- **AG-3의 축(재개 조건을 세운 손)** — 재었고 트랙이 되지 않는다. ⚠️⚠️ **막는 것은 규율도 배정도
  아니라 *바늘*이다**: 오늘의 답은 `git blame`으로 냈는데 **계약이 blame을 돌리면 느리고 이력에
  기대며 rebase에 흔들린다**. 표식(`리뷰 L-N`)으로 세면 또 다른 수(문서 19 · 소스 15)가 나오고
  **그 둘 중 어느 쪽이 *손*인지는 아직 아무도 정하지 않았다.**
  ⚠️ **재개 조건(결정형 · 손은 저장소 안): 조건의 *손*을 무엇으로 셀지를 정하는 날 — 그날의 첫
  모집단은 오늘의 27이다.**
- **낭독 스캐너 줄(쿼리로 분류한 여섯 화면의 낭독)** — ⚠️ **이번 라운드에도 그 축이 아니다.**
  후보 3이 여는 것은 *행꼴 컴포넌트의 낭독 라벨*이고, 이 줄이 기다리는 것은 *쿼리로 분류한 여섯
  화면의 낭독*이다. **접점 0건으로는 다섯 라운드 연속이고, *"열었지만 그 축이 아니다"* 로는 두
  라운드 연속이다**(라운드 87이 세운 구별 · **두 수를 한 낱말로 적지 않는다**).
- **`monthly_wrapup`의 달 이동 구멍(U-3)** — ⚠️ **`src/notifications/**`가 이번에도 접점 0건이고,
  이것으로 여섯 라운드 연속이다.** 판정 종전 그대로.
- **S-3(어드민 `disabled`)** — 판정 종전 그대로, 브라우저 확인 `#130` 선행. ⚠️ **`app/items/page.tsx`
  ·`app/links/page.tsx`의 *끊긴 채로*는 오늘 세 라운드가 된다**(89·90에 이어 열렸다가 91·92·93 제안
  다 그 밖이다 — 후보 4가 여는 어드민 파일은 계약 하나뿐이고 화면은 0바이트다).
- **`withdrawn_at`** — ⚠️ **이번 라운드도 다시 재지 않았다**(파일 둘 — `data-retention-purge.job.ts` ·
  `withdrawn-row-write-paths.test.ts` · 라운드 88이 자기 정정으로 확정한 값을 여기 옮겨 적지 않는다).
  **다섯 라운드 연속 재측정 대상이 아니다.**
- **AA-R ① 연속 실패 재판정** — deps는 오늘도 `[isError]` 하나 · 자동 재시도·폴러 **0건**.
  ⚠️ 이번 라운드의 어느 후보도 그 훅도 그 훅의 호출부 화면도 열지 않는다.
- **어드민의 무명 표 하나** — 재실측 **표 18 · 이름 17**이고 스윕 밖의 하나는
  `src/components/ProductLinkBulkReplace.tsx:318`이다. ⚠️ **집지 않는 이유는 축이다** — 이번 라운드
  어드민의 축은 **조회 실패의 [다시 시도] 관례**다. ⚠️ **재개 조건(사건형 — 종전 그대로): 표 이름
  스윕의 모집단이 `app/**` 밖으로 넓어지는 날.**
- **어드민 `<section>` 34에 이름을 주지 않는다 · 어드민의 연타 가드 절반(`<button` 68 · `disabled=`
  44)** — 재었고 판정 종전 그대로. ⚠️ **어드민 `<button type=` 68은 오늘도 **전부** `type=`을 지닌다**
  (AG절이 *"빠진 자리 0건으로 닫힌 관례"* 로 적은 그 자리 · 변화 0).
- **반쪽 프롭 일곱 · `accessibilityState={{ disabled }}` 열한 자리 · `useMutation` 밖의 쓰기 35 ·
  연타 스윕의 사각 `control-verdict-is-file-scoped` · AC-1의 일곱 · AC-2의 0** — 재개 조건 전부
  사건형이고 종전 그대로. ⚠️ **후보 3이 a11y 그물을 열지만 축은 *행꼴 컴포넌트의 라벨*이고 반쪽
  프롭은 라운드 90 A의 축이다** — *한 트랙이 한 그물에 축 둘을 얹지 않는다.*
- **N-4의 두 문턱은 오늘로 16라운드 연속 미발동이고, 준비템 탭 비가상화는 이번에도 제안하지 않는다** —
  ⚠️⚠️ **오늘도 두 수가 나왔고 둘이 서로 다른 사실이다**: 제품이 보는 카탈로그는 **62**
  (`wooriai_dev` **24MB** · 활성 62)이고, 하네스가 보는 카탈로그는 **8,797**(`wooriai_test` 활성 ·
  전체 **9,439** · **738MB**)다. ⚠️ **문턱 200을 넘는 것은 뒤쪽 하나뿐이고 그 수는 부채가 아니라
  하네스의 누적이다.**
- **AG절 본문과 갈리는 수 넷을 값으로 남긴다**(재론이 아니라 *어느 시점의 수인지*를 가르기 위해서다):
  a11y 목록 안 **35 → 38**, 경과 창 **83 → 79** · neither **300 → 304** · 읽지 못한 수사 **0 → 1**(`몇`),
  키보드 스윕 사각 **다섯 → 여섯**, `unmarked-source-prose` **98 → 99**. ⚠️ **넷 다 라운드 92
  *리뷰*가 움직였고, F의 절이 닫힌 뒤라 AG절 본문에는 옛 수가 남아 있다** — **AG-3이 이름 붙인
  그 순서의 사실이 수치에서도 그대로 보인다.**

---

## 코드 건강 판정

- **테스트는 초록이다**(오늘 재실행) — mobile **246파일 5,051** · admin **42파일 734** ·
  test-utils **17파일 552**. api 850 · domain 131 · contracts 66은 상태 문서의 값이고 다시 돌리지
  않았다(총 **7,384**).
- **계약 그물 열다섯 · 앵커 대장 여덟 번째로 동일 · 사문 대장 44/22 · 재개 조건 대장 383/127/35/36/1**
  — **다섯 대장이 전부 오늘도 자기 하한 위에 있다.**
- ⚠️ **가장 큰 구조적 사각 셋은 그대로다**: 재개 조건의 `prose-only` **208**(가장 크고 라운드마다
  자란다) · 라운드 노트 **377** · 소스의 표기 없는 산문 조건 **99**. **셋 다 하한 설계 덕에 초록이고,
  셋 다 *줄어드는 방향*으로는 아무 계약도 밀지 않는다.**
- ⚠️⚠️ **이번 라운드가 처음 세는 건강 신호 하나**: **관례를 세는 계약 62 가운데 모집단이 손 목록인
  것이 32**다. **계약의 절반 남짓이 *목록에 적힌 자리가 관례를 지키는가*만 묻고 *관례를 져야 하는
  자리가 이것뿐인가*는 묻지 못한다** — 오늘 그중 둘에서 실피해가 나왔고(후보 1·4), 나머지 서른의
  완전성은 **아무도 세지 않았다.**
- **핵심 루프는 오늘도 끊기지 않는다**(지출 기록 → 총액 → 준비템 → 구매 링크 → 구매 후 기록).
  ⚠️ **다만 1단계 직후의 한 갈래에 *나가는 길*이 없다**(후보 1) — 이것이 이 라운드가 고치는 유일한
  루프 상의 결함이다.

---

## 트랙 구성 (파일 단위 상호 배타)

⚠️⚠️ **다섯 트랙 어디에도 같은 파일이 두 번 서지 않는다. 읽기는 겹쳐도 좋고, 쓰기는 겹치지 않는다.**
⚠️ **한 트랙이 한 그물에 축 둘을 얹지 않는다.** ⚠️ **새 계약 그물 0건 — 오늘도 열다섯이다.**
⚠️ **모든 트랙 공통 금지**: 새 `export const` 0건(**`export function`을 쓴다** — 사문 대장의 절반
문턱 여유가 0이다) · 새 계약의 `indexOf`/`slice`에는 **존재 가드 필수** · 등호 단언 대신 **하한·래칫**
(등호를 고르면 **누가 언제 옮기는지를 그 자리에 적는다** · AF-5·AG-5) · **DNC-009·010·011 바이트
불변** · **외부 계정·키·자산 0건**.

- **A 동기화 상태 화면에 나가는 길이 선다** (#1) — **유일한 모바일 화면 트랙 · 핵심 루프 1단계 직후**
  - 소유: `apps/mobile/app/sync-status.tsx`(⚠️ **`<ScreenHeader>`(`:779`)에 `onBack` 한 줄** —
    그 밖은 바이트 불변) · `apps/mobile/src/screen-header-back.test.ts`(**손 목록 → 전수 파생**)
  - 읽기: `apps/mobile/app/**` 라우트 전수(**읽기만**) · `apps/mobile/src/ui.tsx`(`ScreenHeader`의
    `onBack` 슬롯과 `"뒤로가기"` 라벨 — **읽기만 · 바이트 불변**) ·
    `apps/mobile/src/expenses/post-save-destination.ts`(**`/sync-status`가 저장 후 목적지라는 근거 —
    읽기만**) · `apps/mobile/app/_layout.tsx`(**`headerShown: false` — 읽기만**) ·
    이 화면을 소스로 읽는 계약들(`src/offline/{messages,ui-wiring,pending-row-actions,
    item-status-outbox,store-open-gate,permission-denied,server-error-escape}.test.ts` ·
    `src/expenses/{failed-row-prefill,post-save-destination}.test.ts` ·
    `src/family/record-permissions.test.ts` · `src/a11y-contract.test.ts` — **초록으로 남는지는 A가
    확인한다**)
  - 금지: ⚠️⚠️ **새 문구 0글자**(`onBack`이 그리는 것은 공용 `ScreenHeader`의 `‹`와 `"뒤로가기"`
    하나이고 **새 한국어 리터럴을 만들지 않는다**) · ⚠️⚠️ **`EmptyStateCard`의 `"닫기"`를 지우지 말 것**
    (빈 목록의 그 길은 종전 그대로다 — 없애면 이 트랙이 고친 것이 아니라 옮긴 것이 된다) ·
    ⚠️ **온보딩 다섯과 딥링크 착지에 `onBack`을 붙이지 말 것**(뒤로가기가 없는 것이 판단인 자리이고,
    이 트랙이 하는 일은 **그 판단을 이유와 함께 값으로 적는 것**이다) · ⚠️ **`ScreenHeader`의 구현을
    고치지 말 것**(`src/ui.tsx`는 읽기만) · ⚠️ **`app/onboarding/**`의 한 줄 재수출 다섯을 화면으로
    세지 말 것**(파생으로 갈라야 한다) · **어드민 0건 · `apps/api/**` 0건 · `packages/**` 0건 ·
    `docs/**` 0건**
  - 계약: ⓐ **모집단** — `apps/mobile/app/**`의 라우트 파일을 **걷기로** 파생할 것(오늘 **36** ·
    탭 밖 **31**). ⚠️ **손 목록 금지**(고치려는 병이 정확히 손 목록이다) · ⚠️ **한 줄 재수출 라우트는
    파생으로 갈라낼 것**(오늘 다섯).
    ⓑ **판정 셋** — 탭 밖 자리마다 **셋 중 하나**를 소스에서 낼 것: *`onBack`을 배선한다*(오늘 **11**) ·
    *뒤로가기가 없는 것이 판단이다 + 40자 넘는 이유*(오늘 **여섯** — 온보딩 다섯 + 딥링크 착지 하나) ·
    *스택인데 나가는 길이 없다*(**트랙 뒤 0건**을 부정 단언으로 못 박는다).
    ⓒ **오늘 고치는 그 하나** — `app/sync-status.tsx`가 판정 첫째로 떨어짐을 **파일 이름으로** 못 박을 것.
    ⓓ **바이트 불변** — 새 속성 하나를 벗기면 종전 바이트와 정확히 같을 것(**부정 단언** ·
    라운드 91 A·92 A의 ⓔ 형식을 인용) · **새 한국어 리터럴 0건 · 새 낭독 0건**(라벨은 공용 슬롯이
    이미 지닌 하나다).
    ⓔ **래칫** — 배선한 자리 수는 **줄지 않는다**(하한) · *나가는 길이 없다*는 **0을 상한으로** 문다.
    ⓕ **사각** — 값과 하한으로 **넷 이상**: ⓐ **`ScreenHeader`를 쓰지 않는 탭 밖 화면 열셋은 이 바늘
    밖이다**(자기 헤더를 그리거나 헤더가 없는 자리 · 오늘 그중 넷이 `router.back()`을 다른 자리에서
    쓴다 — **그 수를 값으로 적을 것**) · ⓑ **안드로이드 하드웨어 뒤로가기와 iOS 스와이프는 이 계약이
    보지 않는다** — 이 자가 세는 것은 **화면 안의 길**이다 · ⓒ **소스 대조이지 런타임이 아니다**
    (실제로 눌러 나가지는지는 **실기기 항목**의 몫이다) · ⓓ **판정 둘째의 *이유*는 손이 적는다**
    (파생이 아니라 사람의 문장이고, 빈 알리바이를 길이로만 막는다).
    ⚠️ **재개 조건을 자기 축과 함께 적을 것**(AD-5) · ⚠️⚠️ **그리고 이 계약의 옛 주석
    (*"나가는 길이 없는 곳은 이제 없다"*)을 지우지 말고 두 시점으로 적을 것**(AE-3 · **그 문장이
    이 자리를 스물두 라운드 동안 *이미 해결된 것*으로 읽히게 했다**는 사실이 이 트랙의 값 절반이다).

- **B 사용자가 지은 이름 뒤의 조사가 값에서 갈린다** (#2 · AG-4의 절반) — **유일한 문구 트랙**
  - 소유: `apps/mobile/app/settings/children.tsx`(⚠️ **`:502`·`:505` 두 문장** — 그 밖은 바이트 불변) ·
    `apps/mobile/app/family/accept/[token].tsx`(⚠️ **`:307`·`:444` 두 문장**) ·
    `apps/mobile/src/text/korean-particles.ts`(**신설 순수 모듈** — ⚠️ **`export function`만** ·
    화면을 import하지 않는다) ·
    `apps/mobile/src/korean-particle-guard.test.ts`(**신설 앱 내 스윕** — ⚠️ **저장소 그물 열다섯의
    하나가 아니다: `apps/mobile/**` 하나만 걷는다. 그 사실을 머리말이 값으로 적는다**) ·
    `apps/mobile/app/expenses/new.tsx`의 **`:576-579` 주석 한 문단**(⚠️⚠️ **AG-4의 답이 오늘 찾은
    거짓 인용을 두 시점으로 정정한다** — *"포커스를 주는 데는 쓰지 않는다 · `itemNameInputRef`와
    같은 관례"* 는 오늘 거짓이다 · **그 파일의 다른 바이트는 전부 불변**)
  - 읽기: `apps/mobile/src/family/household-scope.ts`(**저장소가 고른 답의 본보기 — 읽기만**) ·
    `apps/mobile/src/offline/messages.ts`(`recordsCountPhrase` — **읽기만**) ·
    `apps/mobile/app/family/index.tsx`(`pendingInviteTarget` — **읽기만 · 바이트 불변**) ·
    `apps/mobile/src/expenses/record-row-actions.ts` · `apps/mobile/src/notifications/notification-row-actions.ts`
    (**마지막 원소가 고정인 관례 — 읽기만**) · 이 두 화면을 무는 계약
    (`src/children/manage-children-flow.test.ts` · `src/journey/children-management-journey.test.ts` ·
    `src/family/invite-accept-messages.test.ts` — **초록으로 남는지는 B가 확인한다**)
  - 금지: ⚠️⚠️ **문장을 새로 짓지 말 것** — 바꾸는 것은 **조사 한 글자**이고 나머지 낱말·어순·
    마침표는 바이트 불변이다 · ⚠️⚠️ **안전한 스물아홉 자리를 건드리지 말 것**(값의 꼬리가 고정이라
    오늘 옳다 — 손대면 이 트랙이 *계약이 화면을 지킨다*가 아니라 *화면이 계약을 맞춘다*가 된다) ·
    ⚠️ **`householdScopePhrase`·`pendingInviteTarget`의 고정 어미를 바꾸지 말 것**(그 셋이 이 트랙의
    **근거**다) · ⚠️ **`app/expenses/new.tsx`에서 주석 한 문단 말고는 한 바이트도 고치지 말 것**
    (그 파일은 라운드 92 A·R이 막 지나간 자리다) · ⚠️ **낭독 라벨의 *식*을 바꾸지 말 것**(후보 3의
    모집단이다) · ⚠️ **어드민 0건 · `apps/api/**` 0건 · `packages/**` 0건 · `docs/**` 0건**
  - 계약: ⓐ **모집단** — `apps/mobile/{app,src}/**`의 비테스트 `.ts`·`.tsx` 전수에서 **템플릿 보간
    바로 뒤에 받침 의존 조사가 오는 자리**를 파생으로 셀 것(오늘 **33** · 괄호·따옴표가 낀 꼴 포함).
    ⚠️ **주석을 마스킹한 뒤에 셀 것** · ⚠️ **손 목록 금지.**
    ⓑ **판정 셋** — 자리마다 **셋 중 하나**를 소스에서 낼 것: *값의 꼬리가 고정이라 갈리지 않는다*
    (고정 어미를 값으로 적는다 — 오늘 **29**) · *값에서 조사를 고른다*(**오늘 0 → 트랙 뒤 넷**) ·
    *갈리는데 고정으로 적었다*(**트랙 뒤 0건**을 부정 단언으로 못 박는다).
    ⓒ **순수 함수의 값** — 받침 판정을 **표로 못 박을 것**: 받침 있는 음절(*지훈* · *하율*) · 없는
    음절(*서아* · *가네*) · ⚠️ **한글이 아닌 끝**(숫자·라틴·이모지)에서 **종전 조사를 그대로 둔다**는
    답을 값으로 적을 것.
    ⓓ **바이트 불변** — 넷 말고는 모든 문구가 바이트 불변임을 **부정 단언**으로 못 박을 것 ·
    **새 한국어 낱말 0건**(바뀌는 것은 조사 한 글자다).
    ⓔ **래칫** — *갈리는데 고정*은 **0을 상한으로** · 모집단 수는 **줄지 않는다**(하한).
    ⓕ **사각** — 값과 하한으로 **넷 이상**: ⓐ **보간이 아니라 상수로 이어 붙인 자리**(`a + "를"`)는
    이 바늘 밖이다 · ⓑ **값의 꼬리가 고정인지는 *한 걸음*만 따라간다**(함수가 만든 문자열의 끝을
    두 걸음 이상 좇지 않으며, **오차의 방향이 거짓 빨강(안전)임을 값으로 적을 것**) ·
    ⓒ **한글이 아닌 끝의 답은 관례이지 문법이 아니다**(재개 조건 사건형: 숫자·라틴으로 끝나는 이름이
    처음 보고되는 날) · ⓓ **소스 대조이지 런타임이 아니다** — TalkBack이 실제로 그 조사를 어떻게
    읽는지는 **실기기 항목**의 몫이다.
    ⚠️ **재개 조건을 자기 축과 함께 적을 것**(AD-5) · ⚠️⚠️ **그리고 이 스윕이 소스를 읽는 헬퍼를
    세우면 앵커 대장의 사각 `helper-named-reader`가 166에서 오른다는 사실을 머리말에 값으로 적을 것**
    (**부채가 아니라 스윕이 하나 늘었다는 뜻이다** · 결정형 10).

- **C 행꼴 컴포넌트의 낭독 라벨이 모집단으로 들어온다** (#3) — **유일한 a11y 그물 트랙**
  - 소유: `apps/mobile/src/a11y-contract.test.ts`(⚠️⚠️ **행 라벨 스윕의 넷째 바늘 하나 — 이 파일에서
    이 트랙이 여는 축은 그것 하나다**)
  - 읽기: `apps/mobile/{app,src}/**`의 비테스트 `.tsx` 전수(**읽기만 · 화면 0바이트**) ·
    `apps/mobile/src/keyboard-tap-guard.test.ts`(**전수 파생과 사각 여섯의 본보기 — 읽기만**)
  - 금지: ⚠️⚠️ **화면 0바이트** — 새 바늘이 데려온 자리가 *갈리지 않는다*로 떨어지면 그것은 **관측**
    이고, 라벨을 고치는 것은 이 트랙의 일이 아니다(재개 조건이 그 손을 이미 지목한다) ·
    ⚠️⚠️ **반쪽 프롭 스윕(라운드 90 A) · 프롭 대장 판정 칸(라운드 88 E) · 모듈 층 스윕(라운드 87) ·
    GAP-065 #6·#7 블록 · 라운드 92 B가 세운 바늘 셋의 판정 로직을 건드리지 말 것**
    (*한 트랙이 한 그물에 축 둘을 얹지 않는다* — 넷째 바늘은 **더하는 것**이지 셋을 고치는 것이 아니다) ·
    ⚠️ **`ROW_LABEL_UNTOUCHED_BYTES` 셋을 지우지 말 것** · ⚠️ **상수 라벨 73은 이 모집단이 아니다** ·
    **어드민 0건 · `apps/api/**` 0건 · `packages/**` 0건 · `docs/**` 0건**
  - 계약: ⓐ **넷째 바늘** — *`.map(` 안에서 그려지는 **행꼴 컴포넌트**의 본문*을 모집단으로 데려올 것
    (오늘 컴포넌트 **15** · 그 안의 식 라벨 **37**). ⚠️ **컴포넌트를 손으로 적지 말 것** — 선언과
    호출부를 **소스에서 파생**하고, *행꼴*의 판정(이름·호출부가 `.map(` 안)을 값으로 적을 것.
    ⓑ **판정 셋은 그대로** — 새 자리마다 *행 직접* · *한 걸음 파생* · *갈리지 않음 + 이유*를 낼 것.
    ⚠️⚠️ **셋째가 서면 `ROW_INVARIANT_REASONS`에 이유를 적는다**(빈 이유 금지 · 40자 하한).
    ⓒ **유령 방지** — 넷째 바늘이 데려온 자리가 **오늘의 38과 겹치지 않음**을 값으로 보일 것
    (겹치면 그 자리는 이미 세어지고 있었다 — **두 수를 한 낱말로 적지 않는다**).
    ⓓ **래칫** — 목록 안 자리 수는 **줄지 않는다**(하한 · 오늘 38 + 새 자리) · *갈리지 않음*은
    **상한**을 그대로 진다.
    ⓔ **바이트 불변** — 이 트랙이 화면을 **0바이트** 고쳤음을 부정 단언으로 적을 것.
    ⓕ **사각** — ⚠️⚠️ **오늘의 사각 ⓐ를 세 번째 시점으로 다시 쓸 것**(124가 얼마로 줄었는지와,
    **남는 수의 성격이 무엇인지**를 값으로). 그 밖에 최소 셋: ⓐ **`.map(` 밖에서 그려지는 행꼴
    컴포넌트**(부모가 배열을 펼치지 않고 한 번만 그리는 자리)는 여전히 밖이다 ·
    ⓑ **두 걸음 이상의 파생**(행 컴포넌트가 다시 부르는 하위 컴포넌트)은 밖이고 **오차 방향은 거짓
    빨강(안전)**이다 · ⓒ **소스 대조이지 런타임이 아니다.**
    ⚠️ **재개 조건을 자기 축과 함께 적을 것**(AD-5).

- **D 어드민 [다시 시도] 관례의 모집단이 전수가 된다** (#4) — **유일한 어드민 트랙**
  - 소유: `apps/admin/src/admin-load-error-copy.test.ts`(⚠️ **`RETRY_BUTTON_SCREENS` 손 목록 →
    `page.tsx` 전수 파생** + ⚠️ **`:424` 주석의 낡은 수사를 두 시점으로 정정**)
  - 읽기: `apps/admin/app/**/page.tsx` **열하나**(**읽기만 · 바이트 불변**) ·
    `apps/admin/src/admin-route-surface.test.ts`(⚠️⚠️ **같은 파생의 본보기 — 읽기만 · 바이트 불변** ·
    **라운드 92 C가 세운 그 계약을 이 트랙이 고치지 않는다**) ·
    `apps/admin/src/lib/load-error-copy.ts`(**문장의 단일 소스 — 읽기만**)
  - 금지: ⚠️⚠️ **어드민 소스 0바이트**(화면·컴포넌트·`lib` 전부) — 이 트랙이 만드는 것은 **세는 자**
    하나다 · ⚠️ **`users-lookup`을 결함으로 세지 말 것**(검색형이라 문장만 받는 것이 판정이고, 그
    이유를 **값으로** 적는다) · ⚠️ **`discardedSites` 등 이 파일의 다른 손 목록을 함께 열지 말 것**
    (한 트랙이 한 축을 진다 — 남는 것은 재개 조건으로) · ⚠️ **`admin-route-surface.test.ts`의 파생을
    복사하지 말고 *같은 사실을 각자 파생*할 것**(사본을 만들면 두 자리가 갈린다) ·
    **모바일 0건 · `apps/api/**` 0건 · `packages/**` 0건 · `docs/**` 0건**
  - 계약: ⓐ **모집단** — `apps/admin/app/**/page.tsx` 전수를 **걷기로** 파생할 것(오늘 **11**).
    ⚠️ **손 목록 금지.**
    ⓑ **판정 둘** — 자리마다 *[다시 시도]가 선다*(`styles.retryButton` + `loadError.canRetry ? (`
    + `{loadError.message}` — 오늘 **10**)와 *문장만 받는다 + 40자 넘는 이유*(오늘 **하나** ·
    `users-lookup`)를 소스에서 낼 것. ⚠️⚠️ **셋째(*배너는 있는데 판정에서 파생하지 않는다*)가
    0건임을 부정 단언으로 못 박을 것.**
    ⓒ **두 시점** — `:424` 주석의 *"열한 자리"* 를 지우지 말고 **언제 무엇이 참이었는지**로 적을 것
    (AE-3 · ⚠️ **그 수가 목록 열과 갈려 있었다는 사실 자체가 이 트랙의 값 절반이다**).
    ⓓ **래칫** — [다시 시도]가 선 자리 수는 **줄지 않는다**(하한) · 셋째는 **0을 상한으로**.
    ⓔ **바이트 불변** — 어드민 소스가 **0바이트**임을 값으로 적을 것.
    ⓕ **사각** — 값과 하한으로 **넷 이상**: ⓐ **`page.tsx`가 아닌 진입**(`error.tsx`·`not-found.tsx`)은
    이 바늘 밖이다(라운드 92 C가 이름 붙인 그 경계를 **인용**할 것 — 발명이 아니다) ·
    ⓑ **한 화면 안의 두 번째 배너**(대시보드의 워커 줄)는 자리 단위로 세지 않는다 ·
    ⓒ **소스 대조이지 브라우저가 아니다**(#130 계열) · ⓓ **문장이 옳은지는 이 자가 묻지 않는다**
    (문구 축은 이 파일의 다른 절이 이미 진다).
    ⚠️ **재개 조건을 자기 축과 함께 적을 것**(AD-5).

- **E 문서가 소스 좌표를 무는 자리를 세는 자가 선다** (#5 · AG-5 발동) — **유일한 계약·문서 트랙**
  - 소유: `packages/test-utils/src/repo-self-description.test.ts`(⚠️⚠️ **새 축 하나 — 문서→소스
    좌표. 이 파일에서 이 트랙이 여는 축은 그것 하나다** · ⚠️ **기존 넷(`근거:` 파생 · DNC 사본 ·
    진입 문서 셋 · 폐기 팔레트)은 바이트 불변**)
  - 읽기: `docs/operations/known-limitations.md` · `docs/qa/runtime-verification-required.md` ·
    `docs/qa/accessibility-offline-checklist.md` · `docs/operations/round4-production-readiness-audit.md`
    (**읽기만 · 0바이트** — ⚠️⚠️ **넷 다 F의 것이다**) ·
    `packages/test-utils/src/{accessibility-checklist-shape,runtime-checklist-shape}.test.ts`
    (**반대 방향(소스→문서)의 본보기 — 읽기만 · 바이트 불변** · **라운드 92 E의 것이다**)
  - 금지: ⚠️⚠️ **문서를 한 바이트도 고치지 말 것** — **좌표 여섯의 정정은 F의 걸음이다**(옮기는 손이
    반대라는 사실이 AG-5의 판정이고, 이 트랙이 문서를 고치면 그 판정이 무너진다) ·
    ⚠️⚠️ **등호로 묻지 말 것** — 자리 수도 의무 수도 **하한**이다(등호면 F가 절을 늘릴 때마다 빨개지고,
    그것이 AF-5가 값으로 적은 비용이다) · ⚠️ **라운드 노트 902를 모집단에 넣지 말 것**(넣으면 이
    계약의 수가 라운드마다 통째로 흔들린다 — **값으로 적고 밖에 둔다**) · ⚠️ **새 그물을 세우지 말 것**
    (그물은 오늘도 열다섯이다 — **그 사실을 머리말이 값으로 적는다**) ·
    ⚠️ **`OWNED_DOCS` 손 목록을 이 트랙이 전수로 바꾸지 말 것**(그것은 또 다른 축이다 — 재개 조건으로) ·
    **모바일 0건 · 어드민 0건 · `apps/api/**` 0건**
  - 계약: ⓐ **모집단** — 위 문서 넷에서 `<소스경로>:<줄>` 꼴 좌표를 **전수로 파생**할 것
    (오늘 **27** — known-limitations 16 · runtime 6 · a11y 3 · round4 감사 2). ⚠️ **손 목록 금지** ·
    ⚠️ **문서→문서 좌표는 갈라낼 것.**
    ⓑ **판정 셋** — 자리마다 ⓐ **그 파일이 실재하는가**(오늘 27/27) ⓑ **그 줄이 파일 길이 안인가**
    (오늘 27/27) ⓒ **이동 의무가 곁에 적혀 있는가**(오늘 **1**)를 값으로 낼 것.
    ⓒ **유령 방지** — 모집단이 0건이 아니고 문서 넷을 실제로 읽었음을 값으로 보일 것.
    ⓓ **래칫** — **셋 다 하한**: 자리 수는 줄지 않고(문서가 자란다), 실재·범위는 **오늘의 27**을
    바닥으로, 이동 의무는 **1**을 바닥으로 문다. ⚠️⚠️ **하한을 고른 비용을 그 자리에 적을 것** —
    *사라지는 좌표는 못 본다*(AG-5의 일반형).
    ⓔ **문서 0바이트** — 이 트랙이 문서를 **읽기만** 했음을 값으로 적을 것(**부정 단언**).
    ⓕ **사각** — 값과 하한으로 **넷 이상**: ⓐ ⚠️⚠️ **좌표가 실재한다는 것과 *그 줄이 그 사실을
    말한다*는 것은 다르다** — 오늘 **손으로 여섯이 거짓임을 확인했고 그 여섯을 자리 이름으로 적을 것**
    (재개 조건 결정형: 그 여섯을 F가 정정하는 날 이 사각이 얼마로 줄었는지를 다시 잰다) ·
    ⓑ **라운드 노트 902는 밖이다**(그중 **둘은 파일 길이를 넘는 죽은 좌표**이고 그 수를 값으로 적을 것) ·
    ⓒ **절 이름·`#N` 꼴 인용은 이 바늘 밖이고 그 수는 훨씬 크다** ·
    ⓓ **이 자는 앞쪽(의무의 실재)만 센다 — 뒤쪽(의무가 지켜졌는가)은 라운드마다 F의 걸음이 답한다**
    (⚠️⚠️ **그리고 오늘 그 답은 *지켜지지 않았다*이고, 그것이 이 축이 서는 이유다**).
    ⚠️ **재개 조건을 자기 축과 함께 적을 것**(AD-5).

- **F 판정 문서** (GAP-093F)
  - 소유: `docs/operations/known-limitations.md`(**AH절 신설** — 판정 다섯 + AG의 답 다섯 + AH-1~AH-5의
    *"다음 라운드가 먼저 세어 볼 만한 것"* + **결정형 재개 조건 전수표**) ·
    `docs/qa/runtime-verification-required.md`(**#169~ 신설 행** — 후보 1의 나가는 길과 후보 2의
    조사가 실기기에서 어떻게 보이는지 · ⚠️ **#47의 인용 *"…를 추가했어요."* 를 두 시점으로 정정**) ·
    `docs/qa/accessibility-offline-checklist.md`(**#109~ 신설 행** · C-3 **스물일곱 라운드**) ·
    `docs/5차/launch-readiness-status.md`(총 테스트·PR 갱신)
  - 읽기: 다섯 트랙의 소유 파일 전부(**읽기만**)
  - 금지: ⚠️⚠️ **소스 0바이트**(F는 문서만 쓴다) · ⚠️ **판정을 두 자리에 옮겨 적지 말 것**(O-3·X-4 —
    수와 판정은 그 판정이 사는 절 하나에만) · ⚠️ **AG절 본문의 옛 수를 지우지 말 것**(두 시점으로)
  - 해야 할 일(값): ⓐ ⚠️⚠️ **AG-5가 낸 좌표 여섯을 정정할 것** — `kl:245`(997→**1069**) ·
    `kl:254`(2105→**2142**) · `kl:413`(`privacy.tsx:150`의 `loadFailedText` — **그 심볼이 오늘
    0건이고 계약이 *없음*을 문다**) · `kl:414`(`import/[importJobId].tsx:131`의 같은 심볼) ·
    `kl:2584`의 좌표 **둘**(1482→**1519** · 1260). ⚠️⚠️ **그중 마지막 자리는 *이동 의무가 적힌 유일한
    자리*이고 그 의무가 지켜지지 않았다는 사실을 판정으로 적을 것** — **AG-5의 뒤쪽 물음에 이 라운드가
    내는 답은 *지켜지지 않았다*이다.** ⓑ **정정할 때 옛 좌표를 지우지 말고 두 시점으로 적을 것**
    (AE-3 · 그래야 트랙 E의 사각이 다음 라운드에 얼마로 줄었는지 셀 수 있다). ⓒ **라운드 92 리뷰가
    움직인 수 넷**(a11y 38 · 창 79 · 사각 여섯 · unmarked 99)을 AH절이 **두 시점으로** 받을 것.

---

> ⚠️ **이 노트의 수는 전부 하한이다** — 각 트랙은 자기 축의 전제를 **다시 재고**, 정찰과 갈리면
> **갈렸다는 사실 자체를 값으로** 적는다(라운드 92 A·B·D·E가 넷 다 그렇게 했다). ⚠️⚠️ **그리고 이
> 노트 자신이 재개 조건 대장의 사각 `round-notes`를 한 벌만큼 키운다**(오늘 377 · 노트 51벌) —
> **정찰을 쓰는 걸음이 곧 그 사각을 키우는 걸음이라는 사실을 여기서 미리 값으로 적어 둔다.**
