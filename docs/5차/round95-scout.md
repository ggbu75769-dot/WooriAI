# 라운드 95 정찰 노트 (GAP-095)

> master `96231d1`(라운드 94 머지 · PR #99 · 적대 리뷰 커밋 `64646dc`와 상태 커밋 `87c706b` 포함)
> 기준 · 2026-09-01 실측.
> do-not-change.md(DNC-001~020) · known-limitations **A~AI절**(AI-1~AI-5 전문 + AI절 머리말의
> **결정형 재개 조건 통합표 스물셋** · 문서 **10,012줄** · AI절 `:9500~10012` · 재실측 표 머리
> `:9768`) · runtime-verification-required.md(**#171**까지) ·
> accessibility-offline-checklist.md(**#171**까지 · C절 C-3 **스물여덟 라운드**) ·
> round56~94-scout 완료분 대조 완료.
>
> ⚠️ **이 라운드의 첫 의무는 관례가 정한 자리에 있다** — AI-1~AI-5가 각 절 끝에 남긴
> *"다음 라운드가 먼저 세어 볼 만한 것"* **다섯**을 오늘 실측으로 답하는 것, 그리고 **수를 낼 때
> 그 수를 어떻게 냈는지(모집단·바늘·사각)를 함께 적는 것**(AC-5·AD-4·AH-5·AI-1). 순서를 이렇게
> 잡았다:
> ① AI-1~AI-5의 질문 다섯을 전수로 세고(각각 **모집단을 먼저** 정하고 그다음 바늘),
> ② AI절 머리말의 **결정형 통합표 스물셋**을 HEAD에서 다시 세고 **그 표가 닫힌 뒤에 선 결정형
>    셋**(AI절 기각 목록)을 함께 세고, ⚠️ **라운드 94 리뷰(`64646dc`)가 세운 것도 함께 확인했다**,
> ③ ⚠️⚠️ **그다음 *제품*을 걸었다** — 라운드 87~94가 문구·조사·접미사·키보드 탭·나가는 길로
>    문구·접근성 층을 상당히 닫았으므로, 이번 정찰은 **그 다음 층**(상태 전이 · 이중 표시 ·
>    페이지네이션 경계 · 시기 전환)을 걸었고 **거기서 실결함 하나가 나왔다**,
> ④ AI-4의 처방(*문구를 고치는 트랙이 그 바이트를 무는 계약 전부를 가진다*)을 **배정 단계에서**
>    다시 썼다.
>
> ⚠️⚠️ **라운드 94 F의 수치 가운데 여럿은 그 뒤 리뷰가 이미 바꿨고, 이 노트의 수는 전부 HEAD
> 실측이다.** 재실측 표 행 **37 → 40** · 바늘 **13 → 39 + 명시적 무바늘 1** · 수만 있는 행
> **24 → 0** · 좌표꼴 **2 → 9** · 붐비는 칸 **2 → 18** · 공유 DB **9,476 → 9,545** ·
> `unmarked-source-prose` **129 → 61** · 노트 **52 → 53벌**.
>
> **관측은 하나로 모인다 — AI-1이 *"바늘이 낱말 하나면 재현되고 문장이면 재현되지 않는다"* 를
> 세우고 *"나머지 노트의 수는 오늘 세지 않았다"* 로 닫았는데, 오늘 그 나머지를 세니 표본 다섯
> 가운데 낱말 하나였던 둘이 **둘 다 정확히 재현됐고**(테스트 파일 전수 401 → **401** · 걷는 계약
> `readdirSync` 43 → **43** · 덤으로 `helper-named-reader`의 넓은 바늘 189 → **189**), 문장이었던
> 셋은 **셋 다 갈렸다**(손 목록 61/33 → 56/26 · 사각 전수 66/11 → 62/14 · 자기 배제 관례 15 → 17).**
> ⚠️⚠️ **그리고 오늘 실피해가 나온 자리는 계약이 아니라 *화면*이었다 — 저장소가 이미 규율로 적어
> 둔 문장**(*"선택 여부를 문구로 다시 말하지 않는다 — 그건 상태가 진다"* ·
> `apps/mobile/src/a11y-contract.test.ts:2257`)**을 세 자리가 어기고 있고, 그중 하나가 핵심 루프
> 1단계다.** 그 규율을 세운 계약의 모집단은 **한 자리**(온보딩 카드)다.
> 다섯 후보가 이렇게 갈린다(⚠️ **앞 하나는 사람이 오늘 겪는 자리이고, 뒤 넷은 사각의 축소다**):
> ① **선택 상태를 라벨과 상태로 두 번 말하는 자리 셋** — 규율은 이미 저장소 안에 있고 모집단이
>    한 자리다. TalkBack이 *"…, 선택됨, 선택됨"* 으로 읽는다 (후보 1).
> ② **사각의 크기 0이 *실제 0*인지 *셀 수 없어 0*인지를 가르는 필드가 한 파일에만 있다** —
>    AI-3의 질문이 그대로 축이 된다. 어드민 두 계약의 0 여섯이 그 갈래를 지지 않는다 (후보 2).
> ③ **재개 조건 대장의 사각 열하나 가운데 일곱의 *적힌 값*이 오늘의 자와 갈린다** — 하한 설계라
>    조용하고, **갈림을 세는 자는 0건**이다. 본보기(사문 대장의 등호 여섯)는 저장소 안에 있다 (후보 3).
> ④ **공유 테스트 DB #14 — 세는 자를 세울 수 있다.** 오늘 일곱째 시점에 **9,545**이고 오름을
>    만든 손은 소스에서 셀 수 있다(`itemTemplate.create` 자리 **17** · 정리 걸음 **넷**) (후보 4).
> ⑤ **AI-1의 갈래(낱말 하나 ↔ 문장)를 판정 문서의 표에 대는 자** — 오늘 그 갈래를 세는 자는
>    0건이고, 트랙 E의 바늘 판정 넷 옆에 여섯째로 선다 (후보 5).

---

## AI-1~AI-5의 질문 다섯 — 오늘의 답

⚠️⚠️ **다섯 다 답했고 넷이 발동한다**(AI-1 → 트랙 E · AI-2 → **트랙 배정의 근거** · AI-3 →
트랙 B · AI-4 → **트랙 A의 배정 근거** · AI-5 → 답만 내고 트랙이 되지 않는다).
⚠️ **수는 여기 옮겨 적되 *그 수를 어떻게 냈는지*(모집단 · 바늘 · 사각)를 반드시 함께 가리킨다.**
⚠️⚠️ **그리고 이 답들은 계약이 아니라 손으로 잰 수다** — 계약이 세는 수와 한 낱말로 적지 않는다.

| # | 자리 | 질문 | 2026-09-01 답(HEAD `96231d1`) | 발동 |
| --- | --- | --- | --- | --- |
| 1 | AI-1 `:9869` | 정찰 노트가 낸 수 가운데 **바늘이 낱말 하나인 것**이 몇이고 **문장인 것**이 몇인가 | `round94-scout.md`의 「답 N을 낸 방법」 다섯 절이 명시한 바늘 **아홉**(답1 셋 · 답2 하나 · 답3 하나 · 답4 셋 · 답5 하나) · ⚠️⚠️ **낱말 하나(정규식/식별자 하나로 재현되는 것) 둘 · 문장(조건 둘 이상을 사람이 이어야 하는 것) 일곱** · ⚠️⚠️ **오늘 재현 실험: 낱말 하나 둘이 *둘 다 정확히* 재현됐고**(테스트 파일 전수 **401** · 걷는 계약 `readdirSync` **43**) **문장 셋이 *셋 다* 갈렸다**(손 목록 61/33 → **56/26** · 사각 전수 66/11 → **62/14** · 자기 배제 관례 15 → **17**) · ⚠️ 덤으로 정규식 하나 바늘도 재현됐다(`NAMED_SOURCE_READER` 넓은 바늘 파일 **189**) | ⚠️⚠️ **발동 → 트랙 E** |
| 2 | AI-2 `:9905` | 결정형 전수표의 스물셋을 **닫히는 범위**로 갈라 *한 파일* 몇 · *한 앱* 몇 · *저장소 전체* 몇인가 | ⚠️⚠️ **한 파일 8 · 한 앱 8 · 저장소 전체 7**(검산 23) · ⚠️⚠️ **집힌 셋(#16·#18·#19)이 *전부* 한 파일이다** · ⚠️ **여덟·아홉 라운드째 안 집히는 것(#8·#10·#11·#12·#13)은 다섯 다 저장소 전체다** · ⚠️ **#22 하나는 결정 자체가 범위를 정한다**(F의 표에 적으면 한 파일 · 계약 안에 적으면 저장소 전체) | ⚠️⚠️ **발동 → 배정 규율의 근거**(아래 「트랙 구성」머리말) |
| 3 | AI-3 `:9947` | 저장소의 사각 가운데 **크기가 0인 것**이 몇이고, 그 0이 *실제로 0*인 것과 *셀 수 없어서 0*인 것이 각각 몇인가 | 크기가 **0으로 적힌** 사각 자리 **15 · 파일 5**(자리 단위 · 증인 안의 0 셋은 따로) · ⚠️⚠️ **열다섯 다 `measure`를 지녀 *걷어서 낸 0*이다 — 실제로 0** · ⚠️⚠️ **셀 수 없어서 0인 자리는 `today`를 아예 두지 않고 `uncountable`을 진 둘**(`apps/mobile/src/screen-header-back.test.ts:349`·`:398`)**이고 그 안의 증인 0이 셋**(`:375`·`:382`·`:427`) · ⚠️⚠️ **그 갈래를 적는 낱말(`zeroMeans`)은 오늘 저장소에 *한 파일에서 여섯 번*뿐이다 — 나머지 열다섯 0에는 *이 0이 무엇의 0인가*가 적혀 있지 않다** | ⚠️⚠️ **발동 → 트랙 B** |
| 4 | AI-4 `:10001` | 제품 파일 하나를 고칠 때 **함께 열어야 하는 계약이 평균 몇 개**인가 | ⚠️⚠️ **평균 3.33 · 중앙값 1 · 최대 29**(물린 제품 파일 **181** · 계약 파일 **187** · 쌍 **602** · 두 계약 이상이 무는 제품 파일 **89**) · ⚠️⚠️ **평균과 중앙값이 갈린다 — 절반(92)은 계약 하나만 물리고 꼬리 열둘이 열하나 이상을 문다** · ⚠️⚠️ **가장 비싼 자리가 정확히 핵심 루프다**: `app/expenses/new.tsx` **29** · `app/(tabs)/index.tsx` **29** · `app/items/[itemTemplateId].tsx` **22** · `app/(tabs)/records.tsx` **20** | ⚠️⚠️ **발동 → 트랙 A의 배정 근거** |
| 5 | AI-5 `:10009` | 재실측 표의 **수만 있는 행이 24에서 얼마로 줄었고** 그중 **좌표꼴로 가리킨 행이 몇으로 늘었는가** | 행 **40**(37에서 자랐다) · 수가 있는 행 **40** · 바늘이 곁에 적힌 행 **39** · 명시적 무바늘 **1**(*문턱·래칫 상수*) · ⚠️⚠️ **수만 있는 행 24 → 0** · ⚠️⚠️ **좌표꼴 2 → 9**(정찰의 손 바늘로는 1 → 9) · 붐비는 칸 2 → **18** · 모집단 밖 옛 표 **다섯 · 행 129** · ⚠️ **F의 수와 트랙 E의 자가 오늘도 갈리지 않는다**(리뷰 M-4가 다섯째 판정을 붙인 뒤의 값 그대로) | ⚠️ 답했고 트랙이 되지 않는다(→ P3 · 뒤쪽을 무는 자는 여전히 0건) |

### 답 1을 낸 방법

**모집단** = `docs/5차/round94-scout.md`의 「답 N을 낸 방법」 **다섯 절**이 `**바늘** =` 또는
`**바늘 셋**` 꼴로 **명시한** 바늘 전수. **아홉**이다(답1 셋 — 손 목록 · 걷는 계약 · 주석의 단언
판정 · 답2 하나 · 답3 하나 · 답4 셋 · 답5 하나).
**바늘** = 그 바늘이 ⓐ **정규식/식별자 하나의 검색으로 재현되는가**(→ *낱말 하나*) ⓑ **조건 둘
이상(ⓐⓑⓒ)을 사람이 이어 붙여야 하는가**(→ *문장*). **답은 둘 대 일곱.**
**낱말 하나 둘**: `readdirSync`(걷는 계약) · *"다른 트랙 소유"·"다른 트랙의 파일"* 낱말 검색
(답4의 바늘 ② — 그 노트 자신이 *"바늘 ②는 낱말 검색이라"* 고 적었다).
⚠️⚠️ **오늘의 재현 실험 — 다섯 자리를 다시 댔다.**

| 자리 | 라운드 94의 수 | 오늘 다시 댄 수 | 바늘의 꼴 | 재현 |
| --- | --- | --- | --- | --- |
| 테스트 파일 전수 | 401 | **401** | 낱말 하나(`.test.ts`/`.test.tsx`) | ⚠️⚠️ **정확** |
| 걷는 계약 | 43 | **43** | 낱말 하나(`readdirSync`) | ⚠️⚠️ **정확** |
| `helper-named-reader` 넓은 바늘(파일) | 189 | **189** | 정규식 하나(`NAMED_SOURCE_READER`) | ⚠️⚠️ **정확** |
| 손 목록(자리 / 파일) | 61 / 33 | **56 / 26** | 문장(배열 상수 + 경로꼴 셋 이상 + 순회) | ⚠️ **갈렸다** |
| 사각 전수(자리 / 파일) | 66 / 11 | **62 / 14**(느슨) · **45 / 7**(엄격) | 문장(`id` + 서술 + 재개 조건) | ⚠️ **갈렸다** |
| 자기 배제 관례 자리 | 15(좁은) / 17(넓은) | **17**(이름 여섯) / **18**(이름에 `SELF`) | 문장(이름 목록) | ⚠️ **갈렸다** |

⚠️⚠️ **AI-1의 일반형이 오늘 두 번째로 확인됐고, 이번에는 표본이 여섯이다** — 낱말/정규식 하나였던
셋이 **셋 다** 한 자리도 다르지 않았고, 문장이었던 셋이 **셋 다** 갈렸다.
⚠️ **그리고 갈린 셋은 어느 쪽이 틀린 것이 아니다** — 사각 전수는 내 파서의 객체 크기 상한(8KB)을
풀자 55 → 62가 됐고 최상위 키만 보게 좁히자 45가 됐다. **한 바늘 안에서도 문장은 갈린다.**
⚠️ **사각 넷**: ⓐ **모집단이 정찰 노트 한 벌이다** — 라운드 93 이전의 노트는 밖이다 ·
ⓑ **바늘을 명시하지 않고 낸 수는 이 판정 밖이다**(후보 절·트립와이어 인용의 수 · 오차 방향은 과소) ·
ⓒ **낱말/문장의 판정 자체가 사람의 읽기다** — 오늘의 둘/일곱은 손이 낸 수이고 계약이 세는 수가
아니다 · ⓓ **재현 실험은 여섯 자리뿐이다** — 나머지 셋(답2의 391 · 답4의 1·5·1 · 답5의 37)은
오늘 다시 대지 않았다. **둘도 일곱도 하한이다.**

### 답 2를 낸 방법

**모집단** = AI절 머리말의 결정형 통합표 **스물셋 전수**(`:9639~9662`).
**바늘** = 그 조건을 이행할 때 **쓰기가 닿는 파일 목록**을 조건마다 적고, ⓐ 한 파일(짝 계약 포함)
ⓑ 한 앱/한 패키지 안의 여러 파일 ⓒ 두 앱·패키지 이상에 걸침 으로 가른다.

| 갈래 | 수 | 조건 번호 |
| --- | --- | --- |
| **한 파일** | **8** | #7(짝 문서 한 벌) · #15 · **#16**✔ · **#18**✔ · **#19**✔ · #21 · #22(앞쪽을 고르면) · #23 |
| **한 앱** | **8** | #1(admin 화면 둘 + 계약) · #2(mobile) · #4(api) · #6(mobile) · #9(mobile) · #14(api) · #17(admin + 하네스 의존) · #20(mobile 세 벌) |
| **저장소 전체** | **7** | #3 · #5(api DTO + admin) · #8 · #10 · #11 · #12 · #13 |

⚠️⚠️ **AI-2가 예고한 상관이 오늘 값으로 선다** — 이번 라운드에 **집힌 셋(#16·#18·#19)이 전부
*한 파일*** 이고, **여덟·아홉 라운드째 안 집히는 다섯(#8·#10·#11·#12·#13)이 전부 *저장소 전체***
다. ⚠️ **그러므로 *집히는 속도를 정하는 것은 손이 아니라 범위*라는 AI-2의 일반형은 오늘 반증되지
않았고, 이 노트는 그 갈래를 배정 규율로 쓴다**(아래 「트랙 구성」).
⚠️ **사각 셋**: ⓐ **범위는 *오늘의 이행 계획*에 달렸다** — 같은 조건도 좁게 이행하면 한 파일이고
넓게 이행하면 저장소 전체다(#22가 그 실물이다) · ⓑ **손이 밖인 다섯(#3·#4·#5·#6·#7)은 범위를
*가정*해 세었다** — 그 결정이 실제로 언제 어떻게 오는지는 저장소가 정하지 않는다 ·
ⓒ **짝 계약을 한 파일로 세었다**(대장과 그 `.test.ts`는 한 짝이다 — 두 파일로 세면 *한 파일*이 0이
된다). **8·8·7은 하한이 아니라 오늘의 갈래다.**

### 답 3을 낸 방법

**모집단** = `apps/**`·`packages/**`·`scripts/**`의 `.ts`·`.tsx`·`.mjs` 전수에서, 객체 리터럴이
ⓐ `id:`/`key:` 를 지니고 ⓑ 서술(`reason`·`what`·`statement`)을 지니고 ⓒ 재개 조건을 지닌 항목.
**오늘 62자리 · 파일 14**(⚠️ AH-3의 66/11과 바늘이 다르다 — 위 「답 1」의 표).
**바늘(크기가 0인가)** = 그 항목의 **최상위** `today:`/`value:` 가 리터럴 `0`인가.
**답은 15 · 파일 5.**

| 자리 | 크기 | 자 |
| --- | --- | --- |
| `apps/admin/src/admin-landmark-current.test.ts` `:469`·`:484`·`:503`·`:550` | 0 넷 | ⚠️ 넷 다 `measure` 있음 |
| `apps/admin/src/admin-route-surface.test.ts` `:400`·`:412`·`:443` | 0 셋 | ⚠️ 셋 다 `measure` 있음 |
| `apps/admin/src/admin-load-error-copy.test.ts` `:1018`·`:1039`·`:1072`·`:1797`·`:1873` | 0 다섯 | 다섯 다 `measure` 있음 |
| `apps/mobile/src/screen-header-back.test.ts:480` (`extension-narrowed-to-tsx`) | 0 하나 | `measure` 있음 |
| `packages/test-utils/src/dead-export-ledger.ts` `:1999`·`:2135` | 0 둘 | 둘 다 `measure` 있음 |

⚠️⚠️ **그러므로 *크기가 0으로 적힌* 열다섯은 전부 걷어서 낸 0이다 — 실제로 0.**
⚠️⚠️ **셀 수 없어서 0인 자리는 크기를 아예 두지 않는다.** `screen-header-back.test.ts`의
`hardware-back-and-swipe-are-not-seen`(`:349`)과 `source-not-runtime`(`:398`)이 `measure`도
`today`도 두지 않고 `uncountable`(*세려 한 것* · *왜 소스에 없는가* · **`zeroMeans`** · *증인*)을
지며, 그 증인 안에서 0을 낸 자리가 **셋**(`:375` `BackHandler` · `:382` `gestureEnabled` ·
`:427` 렌더 하네스). ⚠️ **그리고 그 파일 자신이 두 0을 한 낱말로 적지 않는다** —
`extension-narrowed-to-tsx`의 이유가 *"위 `hardware-back-…`의 0과 **한 낱말로 적지 않는다**"* 라고
소스에 적혀 있다.
⚠️⚠️ **오늘의 값은 여기 있다 — 그 갈래를 적는 낱말 `zeroMeans`는 저장소 전체에서 *한 파일에
여섯 번*뿐이고, 나머지 열다섯 0에는 *이 0이 무엇의 0인가*가 한 글자도 적혀 있지 않다.**
그 열다섯 가운데 `source-not-runtime`이라는 **같은 이름**을 쓰는 자리가 어드민에 둘 있고
(`admin-landmark-current.test.ts:484` · `admin-route-surface.test.ts:431`), 그 둘은 모바일의 같은
이름과 **성질이 다르다**(모바일 쪽은 *셀 수 없어 0*이고 어드민 쪽은 *걷어서 낸 0*이다).
⚠️ **사각 넷**: ⓐ **`measure`가 오늘 0을 내는 자리는 세지 않았다** — 이 답은 *적힌 0*의 수이고,
자를 다 돌려 0이 나오는 자리는 더 많을 수 있다(오차 방향은 과소) · ⓑ **문자열 배열로 적은 옛 산문
사각은 이 바늘 밖이다**(AH-3이 이름 붙인 그 자리 · `a11y-contract.test.ts`의 셋) ·
ⓒ **증인 안의 0(셋)과 사각 자신의 0(열다섯)을 한 낱말로 적지 않는다** ·
ⓓ **소스 대조이지 실행이 아니다.** **15도 2도 하한이다.**

### 답 4를 낸 방법

**모집단** = `apps/**`·`packages/**`의 `.ts`·`.tsx` 가운데 테스트가 아닌 파일(**제품 파일 507**)과
테스트 파일(**계약 408**).
**바늘** = 계약 C가 ⓐ **제품 파일 P의 경로를 문자열로 지니고**(그 파일을 읽는다는 뜻이다 — 앱·패키지
루트 일곱을 차례로 대어 실재를 확인한다) ⓑ **P 안에 실제로 있는 한국어 리터럴(길이 6 이상)을
바이트로 지니면** 쌍 (P, C)가 선다.
**답: 계약 187 · 물린 제품 파일 181 · 쌍 602 · 평균 3.33 · 중앙값 1 · 최대 29.**

| 계약 수 | 1 | 2 | 3 | 4~5 | 6~10 | 11 이상 |
| --- | --- | --- | --- | --- | --- | --- |
| 제품 파일 | **92** | 33 | 8 | 17 | 19 | **12** |

⚠️⚠️ **평균이 아니라 분포가 답이다.** 절반(92/181)은 계약 하나만 물리므로 문구 하나를 고치는 값이
싸고, **꼬리 열둘이 열하나 이상을 문다.** ⚠️⚠️ **그 꼬리가 정확히 핵심 루프다** —
`app/expenses/new.tsx`(1단계 · **29**) · `app/(tabs)/index.tsx`(2단계 · **29**) ·
`app/items/[itemTemplateId].tsx`(3·4단계 · **22**) · `app/(tabs)/records.tsx`(2단계 · **20**) ·
`app/(tabs)/reports.tsx`(**18**) · `app/(tabs)/items.tsx`(**17**).
⚠️⚠️ **그러므로 AI-4가 든 라운드 94 표본(제품 둘 ↔ 계약 다섯 · 핀 아홉)은 *중앙값 쪽* 표본이었다** —
`more.tsx`는 오늘 **9**이고 `budget-edit.ts`는 **2**이며 `korean-particles.ts`는 **1**이다.
**핵심 루프의 화면을 여는 라운드는 그 세 배를 각오해야 한다는 것이 이 수의 값이고, 이번 라운드의
후보 1이 그 자리를 연다**(그래서 아래 트랙 A가 *바꾸는 바이트*를 한 조각으로 좁혔다).
⚠️ **사각 넷**: ⓐ **경로 문자열로 읽지 않고 `import`로 읽는 계약은 이 바늘 밖이다**(오차 방향은
과소 — `month-jump.test.ts`가 그 실물이다) · ⓑ **한국어 리터럴이 길이 6 이상이라 짧은 문구는
밖이다** · ⓒ **쌍이 섰다고 그 계약이 *그 바이트*를 무는 것은 아니다** — 이 자는 *같은 파일을 읽고
같은 문구를 지녔다*까지만 본다(AH-2의 391과 한 낱말로 적지 않는다) · ⓓ **소스 대조이지 실행이
아니다.** **3.33도 602도 하한이다.**

### 답 5를 낸 방법

**모집단** = `docs/operations/known-limitations.md`의 **마지막 판정 절**(오늘 `## AI.` · `:9500`)
뒤에 서는 재실측 표(`| 자리 | … |` · 머리 `:9768`) 행 전수 **40**. ⚠️ **절 이름을 손으로 적지
않았다** — 트랙 E의 계약이 쓰는 `JUDGMENT_SECTION_PATTERN`을 그대로 대어 마지막 절을 찾았다.
**바늘** = 그 행 안에 ⓐ 백틱에 싸인 소스 파일 이름 ⓑ *모집단*·*바늘* 이라는 낱말 ⓒ *걷는다*·
*파생한다* ⓓ 백틱에 싸인 줄 조각 가운데 하나라도 있는가. ⚠️ **다섯째 판정이 먼저 선다** —
행이 스스로 `**바늘**: 없다` 라고 적었으면 그 행은 넷 중 어느 것도 아니다(리뷰 M-4).
**답: 수가 있는 행 40 · 바늘이 적힌 행 39 · 명시적 무바늘 1 · 수만 있는 행 0 · 좌표꼴 9.**
⚠️⚠️ **AI-5가 물은 두 수가 오늘 답을 받았다 — 수만 있는 행은 24에서 *0*이 됐고 좌표꼴은 2에서
*9*가 됐다.** F가 스물넷에 바늘을 붙이는 숙제를 **한 라운드에 다 했다**는 뜻이고, 그 자리마다
붙은 것이 바늘 하나라는 사실은 트랙 E의 자가 오늘도 그대로 확인한다.
⚠️ **다만 갈래 하나가 오늘 커졌다** — **붐비는 칸(한 값 칸에 수가 여덟 이상)이 2에서 18**이 됐다.
사문 대장의 사각 여덟(수 16) · 나가는 길(16) · 앵커 대장(15) · 공유 DB(13) 같은 행이고,
⚠️⚠️ **그 열여덟 행은 바늘이 적혀 있어도 *어느 수에 어느 바늘인지*가 적혀 있지 않다** — 이것이
AI-5가 닫지 않고 넘긴 뒤쪽(재현)의 새 얼굴이다.
⚠️ **사각 넷**: ⓐ **바늘이 적혔다와 그 바늘로 재면 같은 수가 나온다는 다르다** — 오늘 그 뒤쪽을
실제로 두 자리에서 대 봤고(`helper-named-reader` **189** ↔ 칸의 **167** · 노트 벌 수 **53** ↔ 내
`round*.md` 바늘 **50**), 둘 다 갈렸다 · ⓑ **모집단 밖 옛 표 다섯 · 행 129는 밖이다** ·
ⓒ **한 행에 수가 여럿이면 한 칸으로 센다** · ⓓ **짝 문서 둘은 이 표 밖이다.**
**40도 39도 9도 하한이다.**

---

## 선행 확인 (후보 아님)

- **테스트는 오늘 세 스위트를 다시 돌렸고 전부 초록이다** — mobile **247파일 5,127** ·
  admin **42파일 751** · test-utils **17파일 582**. api 850 · domain 131 · contracts 66은 상태
  문서의 값이고 다시 돌리지 않았다(총 **7,507** — 상태 문서와 한 자리도 다르지 않다).
- **라운드 94의 커밋 열**(`1a5d18b..96231d1`)**을 전수로 읽었다.** 트랙 A(`1bae485` 받침 갈림) ·
  B(`557a454` 사각 자 파생) · C(`da11b5b` 어드민 손 목록 둘) · D(`bf3713a` 소스 바늘 세 갈래) ·
  E(`ab4b702` 재실측 표의 수·바늘) · F(`7dd560b` AI절) · **리뷰**(`64646dc` 열하나) ·
  상태(`87c706b`) · 정찰(`69a7ab8`). **20파일 · +4,918 / −245.**
- ⚠️⚠️ **라운드 94 리뷰(`64646dc`)가 세운 결정형은 0건이다.** 세운 것은 **사건형 하나**
  (NFC 분해형 태명이 홈 카운터 문구에 나타나는 날 · `korean-particles.ts`)와 **사각 둘**
  (L-1 *파일을 통째로 빼는 배제* · L-3 *별칭으로 들여온 이름을 못 보는 바늘*)이다.
  **그러므로 통합표는 오늘도 스물셋이고, 그 뒤에 선 결정형은 F의 기각 목록에서 온 셋뿐이다.**
- **핵심 루프는 오늘도 끊기지 않는다**(지출 기록 → 총액 → 준비템 → 구매 링크 → 구매 후 기록).
  ⚠️ **오늘 정찰이 그 다음 층을 걸었고, 상태 전이·페이지네이션·이중 표시 층은 이미 닫혀 있다**:
  ⓐ 페이지 경계 — `collectExpensePages`가 커서를 끝까지 돌고 **부분 결과를 성공으로 위장하지
  않는다**(상한 초과·커서 정체·`nextCursor` 부재를 전부 오류로 던진다) · ⓑ 이중 표시 —
  `records.tsx:1659`가 로딩·오류·필터 0건·빈 달을 **네 갈래로** 가르고 *"0건 · 0원"* 을 로딩·오류
  중에는 그리지 않는다 · ⓒ 상태 전이 — `resolveScreenPhase`(에러 → 로딩 → 정상)가 **다섯 화면**에
  섰다 · ⓓ 오프라인 복귀 — `src/offline/**`가 41파일이고 `session-expiry` · `store-open-gate` ·
  `delta-sync` · `outbox-merge`가 각자 계약을 진다.
  ⚠️ **그래서 이번 라운드의 제품 후보는 흐름이 아니라 *낭독*에서 나왔다**(후보 1).
- ⚠️ **다만 위 ⓒ에는 값 하나가 붙는다** — `useQuery`를 쓰는 화면은 **18**인데 `resolveScreenPhase`를
  쓰는 화면은 **다섯**이다(`(tabs)/index` · `(tabs)/items` · `family/index` ·
  `items/[itemTemplateId]` · `expenses/[expenseId]`). ⚠️ **남는 열셋이 틀렸다는 뜻이 아니다** —
  `records.tsx`는 자기 사다리를 네 갈래로 갖고 있고 오늘 실피해 0건이다(TanStack v5에서
  `isError`와 `isLoading`은 동시에 참이 될 수 없어 순서가 갈려도 결과가 같다).
  **그러나 그 사실을 세는 자는 0건이고, 그것이 이 줄의 값이다**(→ P3).
- ⚠️ **곁딸린 관측 하나 — `apps/mobile/src/a11y-contract.test.ts`에 NUL 바이트가 한 자리 있다**
  (오프셋 466229 · `` `${site.file}\x00${site.body}` ``의 구분자를 이스케이프가 아니라 **실제
  바이트**로 적었다). ripgrep은 읽지만 **GNU grep은 이 파일을 이진으로 보고 내용을 감춘다.**
  저장소에서 가장 큰 낭독 계약이 셸 스윕에 통째로 보이지 않는다는 뜻이라 값으로 적어 두되,
  ⚠️ **계약은 전부 `readFileSync`로 읽으므로 실피해는 0건이다** — 트랙으로 만들지 않는다(→ P3).

---

## 상위 후보

### 1. **선택 상태를 라벨과 상태로 두 번 말하는 자리가 셋이고, 그 규율은 이미 저장소 안에 있으며 그 규율을 세운 계약의 모집단은 한 자리다** — 모바일·사용자 낭독 — S~M

- **사실.** `apps/mobile/src/a11y-contract.test.ts:2257`이 이렇게 적고 있다:
  *"선택 여부를 문구로 다시 말하지 않는다 — 그건 상태가 진다(같은 사실을 두 번 읽지 않는다)"* ·
  `expect(tag, "상태를 말로 다시 적지 않는다").not.toContain("선택됨")`.
  ⚠️⚠️ **그 자의 모집단은 `cardPressableTag()` 한 자리**(온보딩 `child-status.tsx`의 단계 카드)
  **이고, 저장소에는 그 규율을 어기는 자리가 셋 있다.**
- **오늘의 전수.** `accessibilityState={{ … }}` 자리 **57**(테스트 밖) — `disabled` **18** ·
  `selected` **17** · `expanded` **10** · `checked` **9** · `busy` **3**.
  ⚠️ **그중 라벨이 같은 사실을 말로 다시 적는 자리 셋 · 전부 `selected` 갈래**:

| # | 자리 | 오늘 낭독되는 것 | 상태 프롭 |
| --- | --- | --- | --- |
| ① | `apps/mobile/app/expenses/new.tsx:392` (`ExpenseQuickItemButton`) | `` `${label}${hint ? ". "+hint : ""}${selected ? ". 선택됨" : ""}` `` | `accessibilityState={{ selected }}` (`:394`) |
| ② | `apps/mobile/src/month-jump.ts:308` → `apps/mobile/src/MonthJumpSheet.tsx:148` | `"2026년 7월, 선택됨"` | `accessibilityState={{ selected: cell.isSelected }}` |
| ③ | `apps/mobile/src/expenses/date-picker-month.ts:338` → `apps/mobile/src/expenses/ExpenseDatePicker.tsx:204` | `"8월 12일, 선택됨"` | `accessibilityState={{ selected }}` |

- ⚠️⚠️ **셋 다 핵심 루프 위에 있다** — ①은 **1단계**(지출 기록의 *"분류별 빠른 품목"* 타일) ·
  ②는 **2단계**(기록·리포트 탭의 달 이동 시트) · ③은 **1단계**(지출 기록의 날짜 픽커).
  RN의 `accessibilityState.selected`는 안드로이드에서 `setSelected(true)`로, iOS에서
  `UIAccessibilityTraitSelected`로 내려가 **OS가 스스로 *"선택됨"* 을 읽는다** — 그 사실을 이
  저장소가 이미 알고 적어 두었다(`app/(tabs)/records.tsx:473`: *"TalkBack은 '리스트, 탭, 선택됨'처럼
  읽는다"*). 그래서 오늘 이 셋은 **"…, 선택됨, 선택됨"** 으로 들린다.
- ⚠️⚠️ **그리고 형제가 옳게 서 있어 대조가 된다** — 같은 파일 `:334`의 `ExpenseCategoryIconButton`은
  라벨을 `category.label` **값에서 파생**하고 상태는 `accessibilityState`에만 맡긴다.
  **한 화면 안에서 두 타일이 서로 다른 관례를 쓰고, 한쪽만 두 번 말한다.**
- ⚠️ **다른 넷 갈래는 오늘 0건이다** — `비활성`·`체크됨`·`펼침/접힘`·`로딩 중` 이 낭독 라벨에 든
  자리는 저장소 전체에서 **0건**이다(주석에만 있다). **즉 이 병은 오늘 `selected` 하나에만 있다.**
- **바꾸는 것.** ⚠️⚠️ **바이트 셋 + 상태 프롭 둘.** ①은 라벨 템플릿의 `${selected ? ". 선택됨" : ""}`
  **한 조각**을 걷는다. ②·③은 순수 함수의 `parts.push("선택됨")` **한 줄씩**을 걷는다 —
  ⚠️ **그 자리가 `else if`라 걷으면 *못 고르는 이유*가 대신 들어온다**(선택됐는데 못 고르는 칸에서
  오늘은 이유가 조용했다 · **개선이지 손실이 아니다**). ⚠️⚠️ **그리고 그 둘의 *비선택 가지*
  (`MonthJumpSheet.tsx:140`의 `<View accessible>` · `ExpenseDatePicker.tsx:195`의 `<View accessible>`)
  는 상태 프롭을 지지 않으므로**, 그 가지에 `accessibilityState={{ selected: …, disabled: true }}`를
  함께 세워야 *선택됐는데 못 고르는* 갈래가 소리를 잃지 않는다.
- **그 바이트를 무는 계약(AI-4의 처방 · 같은 트랙이 소유).** ⚠️ **셋**이다 —
  `apps/mobile/src/month-jump.test.ts:131`(`toBe("2026년 7월, 선택됨")`) ·
  `apps/mobile/src/expenses/date-picker-month.test.ts:232`·`:243`(`toBe("8월 12일, 선택됨")`) ·
  `apps/mobile/src/a11y-contract.test.ts:2257`(**규율을 세운 자 — 모집단을 한 자리에서 전수로**).
  ⚠️ `apps/mobile/src/settings-flow.test.ts:94`의 `not.toContain('"선택됨"')`는 **다른 화면**의
  다른 축이다(설정 요약의 무정보 문구) — **읽기만 하고 한 바이트도 고치지 않는다.**
  ⚠️ `apps/mobile/src/expenses/entry-screen-visual-restore.test.ts:55`는 슬라이스의 **끝**을
  `function ExpenseQuickItemButton`으로 잡으므로 ①의 바이트는 그 창 밖이다(읽기만).
- **왜 지금인가.** ⚠️⚠️ **AI-3이 이름 붙인 병(*"자가 주석보다 좁다"*)의 세 번째 실물이고, 이번에는
  주석이 아니라 *규율*이 자보다 넓다** — 규율은 부류 전체를 두고 말하는데 자는 한 자리만 본다.
  라운드 94 트랙 C가 어드민에서(`handListNeedle`), 트랙 B가 모바일에서(사각 ⓒ의 증인) 각자 그
  한 겹을 닫았고, **오늘 남은 것은 그 병이 *화면에* 실제 손해를 내고 있는 자리다.**
- **크기.** 제품 파일 다섯(바이트 셋 + 프롭 둘) · 계약 셋 · **어드민 0 · api 0 · packages 0.**
- ⚠️ **DNC 확인**: DNC-018(해요체 톤) — **새 문장 0건**이고 낱말 하나를 걷는다 ·
  DNC-002(핵심 루프) — 흐리지 않고 1·2단계의 낭독을 고친다 · DNC-009·010·011 바이트 불변.

### 2. **사각의 크기 0이 *실제 0*인지 *셀 수 없어 0*인지를 가르는 필드가 한 파일에만 있다 — AI-3의 질문이 그대로 축이 된다** — 어드민·계약 — S

- **사실.** AI-3이 *"0은 없음이 아니라 셀 수 없음의 값이다"* 를 세우고 *"그 둘을 가르는 자는 오늘
  0건이다"* 로 닫았다. 오늘 세니 **크기가 0으로 적힌 사각 열다섯 가운데 그 갈래를 지닌 것은 0**이고,
  갈래를 적는 낱말(`zeroMeans`)은 **`apps/mobile/src/screen-header-back.test.ts` 한 파일에 여섯 번**
  뿐이다.
- **오늘의 전수(어드민 몫).** `apps/admin/src/admin-landmark-current.test.ts`의 0 **넷**
  (`:469`·`:484` `source-not-runtime`·`:503`·`:550`) · `apps/admin/src/admin-route-surface.test.ts`의
  0 **셋**(`:400` `page-only-needle`·`:412` `intentional-absence`·`:443` `source-not-runtime`).
  ⚠️⚠️ **일곱 다 `measure`를 지녀 걷어서 낸 0인데, *그 0이 무엇의 0인가*는 어디에도 적혀 있지 않다.**
- ⚠️⚠️ **그리고 이름이 겹쳐 병이 보인다** — 어드민의 `source-not-runtime` 둘과 모바일의
  `source-not-runtime`(`screen-header-back.test.ts:398`)이 **같은 이름인데 성질이 반대다**:
  모바일 쪽은 *셀 수 없어서 0*(자를 두지 않고 증인을 진다)이고 어드민 쪽은 *걷어서 낸 0*(자가 있다)이다.
  **다음 라운드가 이 이름을 성격으로 읽으면 정확히 AH-3이 예고한 오독이 난다.**
- ⚠️ **본보기가 저장소 안에 있다** — `UncountableEvidence`(*세려 한 것* · *왜 소스에 없는가* ·
  *0의 뜻* · *증인 걷기*)의 꼴을 **인용**하되 복사하지 않는다(라운드 94 트랙 B·C가 쓴 관례).
- **크기.** 어드민 계약 둘 · **제품 0바이트.**

### 3. **재개 조건 대장의 사각 열하나 가운데 일곱의 *적힌 값*이 오늘의 자와 갈리고, 그 갈림을 세는 자가 0건이다** — 대장·계약 — S~M

- **사실.** 오늘 세 대장의 사각을 **실제로 돌려** 적힌 값과 맞댔다.

| 대장 | 사각 | 적힌 값 ↔ 오늘의 자 | 갈림 |
| --- | --- | --- | --- |
| `dead-export-ledger.ts` | 여덟(자 여섯) | 22↔22 · 226↔226 · 20↔20 · 55↔55 · 105↔105 · 141↔141 | ⚠️⚠️ **0건 — 여섯이 다 일치** |
| `comment-tolerant-anchor-ledger.ts` | 일곱 | ⚠️ **자가 하나도 없다**(`measure` 0건) | 잴 수 없다 |
| `resume-condition-ledger.ts` | 여덟 + 경과 셋 = **열하나** | prose-only 184↔**246** · folded-notation 2↔**8** · round-notes 377↔**440** · sibling-documents 15↔**18** · one-line-two-sites 21↔**28** · elapsed-truth 70↔**102** · elapsed-outside-population 89↔**97** · (일치 넷: quoted 55 · source-notation 11 · unmarked-source-prose 61 · prose-elapsed 12) | ⚠️⚠️ **일곱** |

- ⚠️⚠️ **이것이 AI-3이 이름 붙인 병의 세 번째 얼굴이다** — AH-3은 *"크기만 적으면 다음 라운드가
  그 크기를 성격으로 읽는다"*, AI-3은 *"셀 수 없는 사각에 크기를 두면 상수를 함수로 포장한 것"*
  이었고, **오늘 보이는 것은 *크기가 적혀 있는데 그 크기가 오늘의 값이 아니다*이다.**
  ⚠️ **하한 설계라 계약은 초록이고**(prose-only 하한 60 · round-notes 하한 100), **그래서 이 갈림은
  아무 소리도 내지 않는다.**
- ⚠️⚠️ **처방이 이미 저장소 안에 있다** — 사문 대장은 같은 자리를 **등호로** 문다
  (`expect(tsxExportFunctionCount()).toBe(141)` 꼴 · 여섯 자리). ⚠️ **그러나 이 대장에 등호를 그대로
  옮기면 안 된다** — 그 수들은 **라운드마다 자란다**(이 노트가 서면 `round-notes`가 또 오른다).
  **그래서 고를 길은 셋이고, 그중 하나를 고르는 것이 이 후보의 결정이다**: ⓐ 등호 ⓑ 하한만 두고
  적힌 값을 지운다 ⓒ **적힌 값을 *두 시점*으로 두고 *갈림의 크기*를 값으로 세운다.**
  ⚠️ **하한·래칫 우선 규율에 맞는 것은 ⓒ다.**
- ⚠️ **곁딸린 값 하나** — `unmarked-source-prose`는 **129 → 61**로 오늘 처음 *줄었고*, 줄어든 자리는
  사라진 것이 아니라 필드형이라는 제 이름을 얻었다(라운드 94 트랙 D). **그 줄어듦은 옳고, 위 일곱은
  옳지 않다 — 두 갈래를 한 낱말로 적지 않는다.**
- **크기.** 대장 한 파일 + 그 짝 계약(**AI-2의 갈래로 *한 파일***) · **제품 0바이트.**

### 4. **공유 테스트 DB #14 — 오름을 만든 손은 셀 수 있고, 그 자는 소스 대조로 한 파일에서 닫힌다** — api·계약 — S

- **사실(일곱째 시점).** `wooriai_test`의 `item_templates` **9,476 → 9,545**(활성 **8,829 → 8,888** ·
  **744MB → 753MB**) · `wooriai_dev` **62**(활성 62 · **24MB**) 그대로.
  ⚠️⚠️ **이번 정찰도 api 스위트를 돌리지 않았다** — 그러니 **+69**를 만든 손은 라운드 94의 리뷰
  또는 F의 실행이고, **그 사실은 오늘도 어디에도 값으로 적혀 있지 않다.**
  ⚠️ **오름이 빨라졌다**: 다섯째→여섯째 **+37**, 여섯째→일곱째 **+69**.
- ⚠️⚠️ **그런데 그 손은 소스에서 셀 수 있다.** `apps/api/test/**`에서
  `prisma.itemTemplate.create(` 자리 **17 · 파일 10**이고, **정리를 함께 지닌 자리는 넷**
  (`onboarding.e2e.test.ts:448`·`:1044` · `admin-affiliate-click-breakdown.e2e.test.ts:60`·`:172`의
  `itemTemplate.deleteMany`). ⇒ **정리 없이 누적을 만드는 자리 열셋**(하한).
- ⚠️⚠️ **그리고 그 자리는 이미 계약 하나가 반쪽만 물고 있다** —
  `apps/api/test/harness-catalog-cost.test.ts`의 계약 ⓔ(*누적의 값*)가 **셋업이 정리를 0건 한다**를
  전이적 import 폐포(`SETUP_CLOSURE`)에 부정 단언으로 걸었다. **지우는 쪽은 세는데 *만드는 쪽*은
  세지 않는다** — 같은 축의 한쪽 얼굴이다. ⚠️ **그러니 이것은 새 축이 아니라 ⓔ의 비대칭을 닫는
  일이고, 새 그물도 서지 않는다**(그 파일은 그물 열다섯 밖이다 — `SCAN_ROOT_COUNT = 1`).
- ⚠️ **집는 것은 결정 #14가 아니다** — *어느 표를 언제 비울 것인가*는 여전히 열려 있고, 이 후보가
  세우는 것은 **그 결정이 기다리는 첫 모집단**(누적을 만드는 열셋)뿐이다.
- ⚠️⚠️ **부수 효과 하나가 값이다** — `apps/api/**`가 **네 라운드 연속 접점 0건**이었고(91이 다섯
  라운드 만에 열었다) 이 후보가 그 0을 깬다. **DB를 쓰지 않으므로 PostgreSQL 없이도 초록이다.**
- **크기.** api 계약 한 파일 · **api 소스 0바이트 · 모바일 0 · 어드민 0.**

### 5. **AI-1의 갈래(바늘이 낱말 하나인가 문장인가)를 판정 문서의 표에 대는 자가 0건이다** — 계약·문서 — S~M

- **사실.** AI-1이 *"바늘이 낱말 하나면 재현되고 문장이면 재현되지 않는다"* 를 일반형으로 세웠고,
  오늘 표본 여섯이 그것을 다시 확인했다(위 답 1). ⚠️⚠️ **그런데 그 갈래를 세는 자는 0건이다** —
  트랙 E의 자는 바늘이 **있는가**(넷 중 하나 + 다섯째)만 보고 **어떤 꼴인가**는 보지 않는다.
- **오늘의 전수.** 재실측 표 행 **40** · 바늘이 적힌 행 **39** · 그중 좌표꼴 **9**.
  ⚠️ **좌표꼴 아홉은 이미 *기계가 따라갈 수 있는* 바늘이므로 낱말 쪽에 가깝고, 남는 서른은 오늘
  갈래가 없다.** ⚠️⚠️ **그리고 붐비는 칸 열여덟에서는 갈래가 더 급하다** — 한 칸에 수가 여덟 넘게
  들어 있는데 바늘은 하나뿐이라, *어느 수에 어느 바늘인지*가 애초에 적히지 않는다.
- ⚠️ **이 축이 묻는 것은 재현이 아니라 *재현 가능성의 대리 지표*다** — 뒤쪽(실제로 다시 대어 보는
  것)은 AI-5의 기각 목록이 결정형으로 들고 있고 **이 라운드도 집지 않는다**(크기).
- **크기.** 대장 한 파일(`repo-self-description.test.ts`)에 **판정 하나 추가** · **문서 0바이트.**

---

## 결정형 재개 조건 — AI절 통합표 스물셋의 오늘 처분

⚠️⚠️ **AI절 머리말의 통합표는 스물셋이었고, 오늘 HEAD에서 다시 세니 스물셋 그대로다** —
집힌 셋(#16·#18·#19)은 두 시점으로 내려가 있고 **남은 조건은 스물**이다.
⚠️ **오늘 발동한 것은 0건이다** — 스물 가운데 어느 것도 술어가 바뀌지 않았다.
⚠️⚠️ **이번 라운드의 후보 넷은 결정형을 집는 것이 아니라 *AI절의 질문이 연 새 축*이다**(후보
2 → AI-3의 답 · 후보 3 → 세 대장을 돌려 본 실측 · 후보 5 → AI-1의 답 · 후보 4는 #14의 **모집단만**
세우고 결정은 열어 둔다).

| # | 결정형 조건 | 닫히는 범위 | 손 | 오늘의 처분(2026-09-01 실측 · HEAD `96231d1`) |
| --- | --- | --- | --- | --- |
| 1 | 막대를 포커스 가능하게 | 한 앱(admin) | 안 | **집지 않는다 — 값 0.** 두 화면 다 `trend.showTable` 표가 값을 텍스트로 준다(재실측 · 변화 0) |
| 2 | 기록 탭 검색의 분류 갈래 | 한 앱(mobile) | 안 | **집지 않는다 — 어긋남 0건 · 열한 라운드 이어 같은 답.** `RECORDS_SEARCH_FIELDS_LABEL`이 여전히 하나이고 `RECORDS_SEARCH_PLACEHOLDER`가 `` `${…}로 검색` ``으로 그 값에서 파생한다(`records-list-view.test.ts:1195`·`:1202`) |
| 3 | 준비템 분류 필수 입력 | 저장소 전체 | **밖** | 집지 않는다. 변화 0 |
| 4 | 서버의 중복 대기 초대 방지 | 한 앱(api) | **밖** | 집지 않는다. 변화 0 |
| 5 | 감사 뷰의 대상 축 필터 | 저장소 전체 | **밖** | 집지 않는다 — ⚠️⚠️ **`apps/api/**`가 네 라운드 연속 접점 0건이다**(라운드 94 diff에서 `apps/api` 0파일) · 서버 소스와 DTO는 여전히 0바이트다. ⚠️ **이번 라운드의 후보 4가 그 0을 깨지만 여는 것은 `apps/api/test/**` 계약 하나이고 서버 소스는 0바이트다** |
| 6 | 타일 안 배지 | 한 앱(mobile) | **밖** | 집지 않는다. 변화 0 |
| 7 | C-3 잠금 오버레이 TalkBack 투과 | 한 파일(짝 문서) | **밖** | **정찰의 소유 밖이다** — 이 라운드가 서면 **스물아홉 라운드째 미확인**(67~95) |
| 8 | JSX 사용을 참조로 세는 판정 | 저장소 전체 | 안 | **집지 않는다 — 아홉 라운드째다.** 막는 것은 **크기와 문턱의 겹침** 그대로다(`.tsx`의 `export function` 오늘도 **141** — 대장의 자가 등호로 문다 · `derived-exemptions` **44 중 22 · 여유 0**). ⚠️ **그 파일은 이번에도 아무 트랙의 것이 아니다(다섯 라운드 연속)** |
| 9 | 재조회 실패 창에서 옛 목록을 남길지 | 한 앱(mobile) | 안 | **집지 않는다 — 화면 구조 결정이고 오늘도 접점 0건**(라운드 94 diff에서 `apps/mobile/src/offline` 0파일 · 이번 라운드 후보 어디에도 없다) |
| 10 | 앵커 대장의 `helper-named-reader` | 저장소 전체 | 안 | **집지 않는다 — 래칫이다.** 앵커 전수 **699** · code-only **621** · comment-tolerant **70**/래칫 70 · comment-only **8**/래칫 8 · unanchored **0** · 면제 **8** — ⚠️⚠️ **여덟 자리가 한 자리도 다르지 않다(두 라운드 연속 조용).** ⚠️ **세 수를 한 낱말로 적지 않는다**: 대장의 자 **167** · 넓은 바늘 파일 **189**(오늘 정확히 재현) · 출현 **827** |
| 11 | 재개 조건 대장의 `prose-only` | 저장소 전체 | 안(**F**) | **집지 않는다.** 오늘 대장을 돌려 **246**(119 → 145 → 165 → 184 → 208 → 230 → 246)이고 하한 60 위 초록이다. ⚠️⚠️ **적힌 값은 184이고 오늘의 자는 246이다 — 그 갈림을 세는 자가 0건이라는 사실이 후보 3의 축이다** |
| 12 | 재개 조건 대장의 `round-notes` | 저장소 전체 | 안 | **집지 않는다 — 관례 0건.** 오늘 **440**(319 → 377 → 410 → 440)이고 노트는 **53벌**이다(`docs/5차/**`의 `.md` 전수 — ⚠️ **낱말 하나 바늘이라 정확히 재현됐다** · 내 `round*.md` 바늘로는 50이고 **두 수를 한 낱말로 적지 않는다**). ⚠️ **이 노트가 서면 54벌이 된다** |
| 13 | 그물 파일이 자기를 이름으로 선언하는 표식 | 저장소 전체 | 안 | **집지 않는다 — 관례를 새로 정하는 일이다.** 표식 **0건** · ⚠️ **자기 배제 관례는 오늘 이름 여섯의 선언 자리 17**(`LEDGER_SELF_FILES` 4 · `SELF_PATH` 5 · `SELF_FILE` 4 · `SWEEP_SELF_FILES` 2 · `SELF_CODE` 1 · `COORDINATE_SELF_PATH` 1)**이고 넓은 바늘(이름에 `SELF`)로는 18**이다(늘어난 하나는 `SELF_UPDATE_MESSAGE`로 자기 배제가 아니다) · **정합을 세는 자리 0건** |
| 14 | 공유 테스트 DB를 언제 무엇으로 비울지 | 한 앱(api) | 안 | ⚠️⚠️ **집지 않는다 — 그러나 오늘 그 결정이 기다리는 모집단을 세었다.** 일곱째 시점: `wooriai_test` **9,545**(활성 **8,888** · **753MB**) ↔ `wooriai_dev` **62**(활성 62 · 24MB). **+69**(직전은 +37). ⚠️⚠️ **오름을 만든 손은 소스에서 셀 수 있다** — `apps/api/test/**`의 `itemTemplate.create` **17 · 파일 10** · 정리 걸음 **넷** ⇒ **정리 없이 누적을 만드는 자리 열셋**(후보 4가 이 수를 자로 세운다 · 결정 자체는 열려 있다) |
| 15 | 조건의 *도래*를 값으로 가르는 관례 | 한 파일 | 안 | **집지 않는다 — 관례를 정하는 일이다.** 기다리는 자리는 오늘도 **둘** |
| 16 | 어드민 계약의 손 목록 둘 | 한 파일 | 안 | **집혔다(라운드 94 트랙 C) — 두 시점으로 내려가 있다.** 오늘 재확인: 그 절에 손 목록 **0건** · `handListNeedle`이 꼴 바늘로 서 있다 |
| 17 | 어드민에 컴포넌트 렌더 하네스를 세울지 | 한 앱(admin) | 안 | **집지 않는다 — 크기다.** 사각 `source-not-render`가 오늘도 **0**을 들고 어드민에 렌더 하네스 표식 **0건**이다. ⚠️ 트랙 B가 같은 앱의 **다른 두 파일**을 열지만 축은 *0의 뜻*이지 하네스가 아니다 |
| 18 | 사각의 자를 `measure: () => number` 꼴로 | 한 파일 | 안 | **집혔다(라운드 94 트랙 B) — 두 시점으로 내려가 있다.** 오늘 재확인: 그 파일의 사각 **여섯** · 파생 자 **넷** · 자 없음 **둘**(`uncountable`) · 그 자리에 선 새 재개 조건은 **사건형**이다 |
| 19 | `⚠️`와 `재개` 사이의 강조 표식 | 한 파일 | 안 | **집혔다(라운드 94 트랙 D) — 두 시점으로 내려가 있다.** 오늘 재확인: `unmarked-source-prose` **61**(129에서 줄었고 줄어든 자리는 필드형이라는 이름을 얻었다) |
| 20 | 열 자리의 *열거*를 자가 내게 할지 | 한 앱(mobile) | 안 | **집지 않는다 — 축이다.** 그 열거가 사는 세 벌 가운데 둘이 `apps/mobile/src/a11y-contract.test.ts` 안이고, 이번 라운드에 트랙 A가 그 파일에서 여는 축은 **하나**(상태 이중 낭독)다 — *한 트랙이 한 그물에 축 둘을 얹지 않는다* |
| 21 | 문턱·래칫 상수 38의 갈래 셋에 모집단을 | 한 파일 | 안 | **집지 않는다 — 재현 불가로 남은 유일한 칸 그대로다.** ⚠️ **오늘 정찰의 넓은 바늘로는 76**(이름에 `FLOOR`/`CEILING`/`THRESHOLD`/`RATCHET`)**이고 줄머리 상수만 보면 84**다 — F의 38·53과 **세 수를 한 낱말로 적지 않는다.** 갈래 셋(하네스 4 · 제품 8 · 소스 텍스트 26)의 모집단은 어디에도 없고, 트랙 E의 `RETEST_UNREPRODUCIBLE_CELL`이 그 행이 사라지는 것만 막는다 |
| 22 | 트립와이어의 수마다 바늘을 어디에 적을지 | ⚠️ **결정이 범위를 정한다** | 안(**F**) | **절반이 집혔다(라운드 94 E + F) — 나머지 절반은 열려 있다.** ⚠️⚠️ **오늘 그 비용이 값으로 보인다**: F가 앞쪽(표에 적기)을 골라 **수만 있는 행이 0**이 됐지만, **붐비는 칸이 2 → 18**이 됐다 — *한 칸에 수가 여덟 넘게 든 행에 바늘이 하나*라 어느 수에 어느 바늘인지가 애초에 적히지 않는다. ⚠️ **뒤쪽(계약 안에 적기)을 고르면 범위가 *저장소 전체*가 된다** |
| 23 | `COORDINATE_LIVE_DOCS` 손 목록을 전수로 | 한 파일 | 안 | **집지 않는다 — 소유가 갈리는 결정이다.** 오늘도 밖에 `docs/5차/design-restore-spec.md`가 있고(계약의 주석은 **3건**, 내 넓은 좌표 바늘로는 **4건** — 두 수를 한 낱말로 적지 않는다), 그 사실을 그 주석이 스스로 값으로 적는다 |

### 그 표에 서지 않은 결정형 셋 (AI절 F 커밋의 기각 목록에서 왔다 — 오늘 처음 표로 세운다)

⚠️⚠️ **셋 다 F 자신이 세웠고, 라운드 94 *리뷰*가 세운 결정형은 0건이다**(사건형 하나 · 사각 둘).
⚠️ **AI-2가 예고한 *"리뷰가 세운 조건이 다음 표에 선다"* 는 이번 라운드에 **일어나지 않았다** —
그 자리를 F가 채웠다.

| # | 결정형 조건 | 사는 곳 | 닫히는 범위 | 세운 손 | 오늘의 처분 |
| --- | --- | --- | --- | --- | --- |
| 24 | **손 목록 옆 주석의 *단언 꼴*을 관례로 정할지** | AI절 기각 목록 | 저장소 전체 | 라운드 94 **F** | **집지 않는다 — 관례이고 바늘이 사람의 문장 읽기다.** 첫 모집단은 열일곱과 그중 자가 없는 열하나 그대로다. ⚠️ **그런데 그 병의 한 겹 안쪽이 오늘 *화면에* 나타났다** — 후보 1의 규율은 부류 전체를 두고 말하는데 자의 모집단은 한 자리다 |
| 25 | **사각의 자 꼴을 저장소 관례로 정할지**(`measure`를 전체로 넓힐지) | AI절 기각 목록 | 저장소 전체 | 라운드 94 **F** | **집지 않는다 — 크기와 소유다.** 오늘 사각 자리 **62 · 파일 14**(내 바늘)이고 열네 파일이 여섯 트랙의 것이다. ⚠️⚠️ **다만 오늘 그 조건 옆에 *다른 질문*이 섰다 — 자를 두는 것보다 먼저 *0의 뜻*을 적어야 한다는 것이 AI-3의 답이고, 후보 2가 그쪽을 어드민 두 파일에서 연다** |
| 26 | **표의 수를 *다시 대어 보는* 자가 설지** | AI절 기각 목록 | 저장소 전체 | 라운드 94 **F** | **집지 않는다 — 크기다.** ⚠️⚠️ **다만 오늘 정찰이 그 자를 두 자리에서 손으로 대 봤고 둘 다 갈렸다**(`helper-named-reader` 189↔167 · 노트 벌 수 53↔50). **뒤쪽은 여전히 0건이고, 후보 5는 앞쪽의 *갈래*만 센다** |

---

## P3 (재었고 이번 라운드에 제안하지 않는다 — 전부 재개 조건과 함께)

- **낭독 라벨 갈래 잔여 일곱 — 재었고 이번 라운드에도 축이 아니다.** ⚠️ 여섯은 이름이 행꼴이 아니고
  (`ExpenseCategoryIconButton` · `PrimaryButton` · `SecondaryButton` · `TextButton` ·
  `ChildDateField`의 둘) 하나는 라벨이 격자 자체를 읽는다(`PreparationListParity.tsx`의 `ItemGrid`).
  ⚠️⚠️ **그중 핵심 루프 1단계인 `ExpenseCategoryIconButton`을 오늘 실측했고, 제품 수리 후보가
  아니다**: `apps/mobile/app/expenses/new.tsx:333`의 라벨이 `accessibilityLabel={category.label}` —
  **행에서 파생한 값이지 고정 문구가 아니고**, `:334`가 `accessibilityState={{ selected }}`를 지며,
  아이콘은 `accessible={false}`로 트리에서 빠진다. **오늘 낭독은 *"기저귀, 버튼, 선택됨"* 으로
  옳다.** ⚠️⚠️ **그런데 같은 화면의 형제 타일(`ExpenseQuickItemButton` · `:392`)이 상태를 라벨에
  또 적는다** — 그 자리가 후보 1이고, **이 축(이름 판정 문턱)을 넓히지 않고도 제품이 고쳐진다.**
  ⚠️ **재개 조건(사건형 — 종전 그대로): 이름 판정 문턱(`ROW_SHAPED_NAME_WORDS`)을 넓히는 것이
  한 트랙의 축이 되는 날.**
- **사문 대장의 결정형 8 — 전제는 참이고 아홉 라운드째 집지 않는다.** `.tsx`의 `export function`
  오늘도 **141**(대장의 자가 등호로 문다) · `derived-exemptions` **44 중 22 · 여유 0** ·
  `common-name` 226 · `comment-and-string-references` 20 · `string-keyed-dynamic-access` 55 ·
  `jsx-apostrophe-string-masking` 105. ⚠️⚠️ **여섯 자리가 적힌 값과 오늘의 자에서 한 자리도 다르지
  않다 — 이 대장이 후보 3의 본보기인 이유가 그 수다.** ⚠️⚠️ **그 파일이 아무 트랙의 것도 아닌
  다섯 번째 라운드다.** ⚠️ **재개 조건(결정형 · 손은 저장소 안 — 종전 그대로): JSX 사용을 참조로
  세는 판정이 서는 날.**
- **AI-5의 뒤쪽(바늘이 적혔다 ↔ 그 바늘로 재면 같은 수가 나온다) — 재었고 집지 않는다.**
  오늘 두 자리를 실제로 다시 대 봤고 **둘 다 갈렸다.** ⚠️ **집지 않는 이유는 크기다** — 마흔 행의
  자를 다 대려면 대장 여섯을 돌리고 문서 다섯을 걸어야 한다. ⚠️ **재개 조건(결정형 · 손은 저장소
  안 — 종전 그대로): 표의 수를 다시 대어 보는 자가 서는 날.**
- **AH-1의 축(손 목록 옆의 단언) — 재었고 트랙이 되지 않는다.** ⚠️ 오늘 손 목록은 내 바늘로
  **자리 56 · 파일 26**이고(라운드 94의 61/33과 갈린다 — 문장 바늘이다), 걷는 계약은 **43**이며
  겹침 **13**이다. ⚠️ **재개 조건(결정형 · 손은 저장소 안 — 종전 그대로).**
- **`useQuery` 화면 열여덟 가운데 `resolveScreenPhase`를 쓰는 것은 다섯이다 — 오늘 처음 값으로
  적고 집지 않는다.** ⚠️ **남는 열셋이 틀렸다는 뜻이 아니다** — `records.tsx`는 자기 사다리를
  네 갈래(로딩 · 오류 · 필터 0건 · 빈 달)로 갖고 있고 오늘 실피해 **0건**이다. ⚠️ **집지 않는 이유는
  *규율이 아직 관례가 아니기 때문*이다** — 다섯 화면의 주석은 *"MOB-130: 에러 → 로딩 → 정상 순서는
  resolveScreenPhase가 정한다"* 인데 그 문장이 부류 전체를 두고 말하지는 않는다.
  ⚠️ **재개 조건(결정형 · 손은 저장소 안): 화면의 상태 사다리를 한 곳에서 정하는 것이 관례가 되는
  날 — 그날 먼저 물을 것은 *열셋을 옮길 것인가 다섯의 주석을 좁힐 것인가*이고, 첫 모집단은 오늘의
  열여덟과 그중 다섯이다.**
- **`apps/mobile/src/a11y-contract.test.ts`의 NUL 바이트 한 자리 — 오늘 처음 값으로 적고 집지
  않는다.** GNU grep이 이 파일을 이진으로 보아 내용을 감추지만 ripgrep과 `readFileSync`는 읽으므로
  **실피해 0건**이다. ⚠️ **재개 조건(사건형): 셸 `grep`에 기대는 스윕·게이트가 서는 날 — 그날 이
  파일이 통째로 빠진다.**
- **C-3 잠금 오버레이 TalkBack 투과** — ⚠️ **정찰의 소유 밖이고, 이 라운드가 서면 스물아홉 라운드째
  미확인이다**(67~95). 기다리는 것은 수가 아니라 사람·기기·날짜 배정이다.
- **준비템 가격 표시 잠금 — ⚠️⚠️ 재론하지 않는다.** 판정 종전 그대로이고 이번 라운드의 어느 트랙도
  그 표면을 열지 않는다.
- **`monthly_wrapup`의 달 이동 구멍(U-3)** — ⚠️ `src/notifications/**`가 이번에도 접점 0건이고
  **여덟 라운드 연속**이다. 판정 종전 그대로.
- **S-3(어드민 `disabled`)** — 판정 종전 그대로, 브라우저 확인 `#130` 선행. ⚠️ `app/items/page.tsx`·
  `app/links/page.tsx`의 *끊긴 채로*는 오늘 **다섯 라운드**가 된다(후보 2가 여는 어드민 파일은 계약
  둘뿐이고 화면은 0바이트다).
- **`withdrawn_at`** — ⚠️ 이번 라운드도 다시 재지 않았다(일곱 라운드 연속 재측정 대상이 아니다).
- **AA-R ① 연속 실패 재판정 · 반쪽 프롭 일곱 · `accessibilityState={{ disabled }}` 열여덟 자리 ·
  `useMutation` 밖의 쓰기 35 · AC-1의 일곱 · AC-2의 0** — 재개 조건 전부 사건형이고 종전 그대로다.
  ⚠️ **다만 `accessibilityState={{ disabled }}`의 수를 오늘 다시 재어 18로 적는다**(종전 열하나와
  바늘이 다르다 — 나는 테스트 밖 전수를 세었다 · **두 수를 한 낱말로 적지 않는다**).
- **AG-2·AG-3의 축 둘 · 자기 배제 관례 · 문턱이 재는 세계** — 재었고 판정 종전 그대로다.
- **AI절 본문과 갈리는 수를 값으로 남긴다**(재론이 아니라 *어느 시점의 수인지*를 가르기 위해서다):
  공유 DB **9,476 → 9,545**, 재실측 표 붐비는 칸 **2 → 18**, 노트 **52 → 53벌**,
  `helper-named-reader` 넓은 바늘 **189**(변화 0), 문턱·래칫 넓은 바늘 **53 → 76/84**(내 바늘).

---

## 코드 건강 판정

- **테스트는 초록이다**(오늘 재실행) — mobile **247파일 5,127** · admin **42파일 751** ·
  test-utils **17파일 582**. api 850 · domain 131 · contracts 66은 상태 문서의 값이다(총 **7,507**).
- **계약 그물 열다섯 · 사문 대장 44/22/6 · 앵커 대장 699/621/70/8/0 · 재실측 표 40행/바늘 39 ·
  범위 밖 0** — **대장들이 전부 오늘도 자기 하한 위에 있다.**
- ⚠️ **가장 큰 구조적 사각 셋은 그대로이고 둘이 자랐다**: 재개 조건의 `prose-only`(**246**) ·
  라운드 노트(**440** · 53벌) · 소스의 표기 없는 산문(**61** — 라운드 94 D가 이름을 고쳐 129에서
  내려왔다).
- ⚠️⚠️ **이번 라운드가 처음 세는 건강 신호 셋.**
  ⓐ **제품 파일 하나를 고칠 때 함께 열어야 하는 계약이 평균 3.33이고 핵심 루프 네 화면에서
  20~29다** — AI-4의 질문에 대한 답이자, **핵심 루프의 화면을 여는 라운드의 실제 비용**이다.
  ⓑ **크기가 0으로 적힌 사각 열다섯 가운데 *그 0이 무엇의 0인가*가 적힌 것은 0이다** —
  갈래를 적는 낱말은 한 파일에만 있고, **어드민에는 모바일과 같은 이름의 사각이 반대 성질로 둘 있다.**
  ⓒ ⚠️⚠️ **재개 조건 대장의 사각 열하나 가운데 일곱의 *적힌 값*이 오늘의 자와 갈린다** — 하한
  설계라 초록이고 **갈림을 세는 자가 0건**이다. **사문 대장은 같은 자리를 여섯 다 등호로 물어
  갈림이 0이다 — 두 대장이 같은 병에 서로 다른 답을 냈고, 그 대조가 이 신호의 값이다.**
- **핵심 루프는 오늘도 끊기지 않는다.** ⚠️ **이번 라운드가 고치는 것은 흐름이 아니라 *1·2단계의
  낭독*이다**(후보 1) — **화면이 이미 말하고 있는 사실을 OS가 한 번 더 말하는 자리 셋**이고,
  그 규율은 저장소가 이미 문장으로 적어 두었다.

---

## 트랙 구성 (파일 단위 상호 배타)

⚠️⚠️ **여섯 트랙 어디에도 같은 파일이 두 번 서지 않는다. 읽기는 겹쳐도 좋고, 쓰기는 겹치지 않는다.**
⚠️ **한 트랙이 한 그물에 축 둘을 얹지 않는다.** ⚠️ **새 계약 그물 0건 — 오늘도 열다섯이다.**
⚠️⚠️ **이번 라운드는 배정 규율에 AI-2의 답을 더한다**: **한 트랙이 여는 축은 *한 파일 또는 한 앱*
안에서 닫혀야 한다.** 오늘의 실측이 그 규율의 근거다 — 집힌 셋은 전부 한 파일이었고, 저장소 전체에
걸치는 다섯은 여덟·아홉 라운드째 그대로다. **그래서 후보 2는 저장소 열다섯 가운데 어드민 일곱만,
후보 4는 결정 #14가 아니라 그 모집단만 연다.**
⚠️⚠️ **그리고 AI-4의 처방을 배정 단계에서 다시 쓴다**: **제품 문구·라벨을 고치는 트랙은 그 바이트를
무는 계약 파일 전부를 함께 소유한다.** 오늘 그 수를 손이 아니라 **소스로** 세었다(후보 1이 여는
제품 파일 다섯 ↔ 그 바이트를 무는 계약 **셋**). ⚠️ **트랙 A가 소스를 다시 걸어 넷 이상이면 그
사실 자체를 값으로 적는다** — 라운드 94에서 정찰의 넷이 트랙의 다섯이 됐고, **손으로 센 배정은 늘
하한이다.**
⚠️ **모든 트랙 공통 금지**: 새 `export const` 0건(**`export function`을 쓴다** — 사문 대장의 절반
문턱 여유가 0이다) · 새 계약의 `indexOf`/`slice`에는 **존재 가드 필수**(빈 문자열·`-1` 갈래를
값으로) · 등호 단언 대신 **하한·래칫**(등호를 고르면 **누가 언제 옮기는지를 그 자리에 적는다**) ·
**DNC-009·010·011 바이트 불변** · **준비템 가격 표시 잠금 — 재론 0건** ·
**외부 계정·키·자산 0건** · ⚠️ **재개 조건을 자기 축과 함께 적을 것**(AD-5) ·
⚠️ **형은 괄호로 밝힐 것**(`재개 조건(사건형)` · `재개 조건(결정형 · 손은 저장소 안|밖)`) ·
⚠️⚠️ **정찰과 갈리면 갈렸다는 사실 자체를 값으로 적을 것**(AI-1의 일반형).

- **A 선택 상태를 라벨과 상태로 두 번 말하는 자리 셋이 한 번만 말한다** (#1) —
  **유일한 제품 트랙 · 이 라운드의 유일한 화면 수정**
  - 소유(제품 다섯): `apps/mobile/app/expenses/new.tsx`(⚠️ **`:392`의 라벨 템플릿에서
    `${selected ? ". 선택됨" : ""}` **한 조각**만** — 그 밖은 바이트 불변) ·
    `apps/mobile/src/month-jump.ts`(⚠️ **`:307~309`의 `parts.push("선택됨")` 갈래 하나**) ·
    `apps/mobile/src/expenses/date-picker-month.ts`(⚠️ **`:337~339`의 같은 갈래 하나**) ·
    `apps/mobile/src/MonthJumpSheet.tsx`(⚠️ **`:140`의 비선택 가지에 상태 프롭 하나** — 그 밖은
    바이트 불변) · `apps/mobile/src/expenses/ExpenseDatePicker.tsx`(⚠️ **`:195`의 비선택 가지에
    상태 프롭 하나**)
  - 소유(계약 셋 — **AI-4의 처방으로 배정 단계에서 준다**):
    `apps/mobile/src/month-jump.test.ts`(`:131`의 기대 문자열) ·
    `apps/mobile/src/expenses/date-picker-month.test.ts`(`:232`의 `it` 이름과 `:243`의 기대 문자열) ·
    ⚠️⚠️ `apps/mobile/src/a11y-contract.test.ts`(**`:2257`의 규율 하나 — 모집단을 한 자리에서
    전수로. 이 파일에서 이 트랙이 여는 축은 그것 하나다**)
  - 읽기: `apps/mobile/src/settings-flow.test.ts`(`:94`가 무는 것은 **설정 요약의 무정보 문구**이지
    이 축이 아님을 확인 — **읽기만 · 바이트 불변**) ·
    `apps/mobile/src/expenses/entry-screen-visual-restore.test.ts`(`:54~55`의 슬라이스가 이 바이트
    **밖**임을 확인 — 읽기만) · `apps/mobile/app/(onboarding)/child-status.tsx`(**규율이 이미
    옳게 선 본보기 — 읽기만 · 바이트 불변**) · `apps/mobile/app/(tabs)/records.tsx`(`:473`의 주석이
    *OS가 상태를 읽는다*를 적은 자리 — **인용하되 고치지 않는다**) ·
    `apps/mobile/src/expenses/records-list-view.test.ts` · `apps/mobile/src/reports/month-end-review-flow.test.ts`
    (둘 다 이 바이트를 물지 않음을 확인 — 읽기만)
  - 금지: ⚠️⚠️ **문장을 새로 짓지 말 것** — 걷는 것은 **상태 낱말 하나와 그 앞의 구분자**이고
    나머지 낱말·어순·마침표는 바이트 불변이다(새 한국어 낱말 **0건**) ·
    ⚠️⚠️ **`hint`·*못 누르는 이유*를 건드리지 말 것** — 그 둘은 상태가 지지 못하는 사실이라 라벨에
    남는 것이 옳다 · ⚠️ **`accessibilityRole`·`onPress`·스타일·픽셀을 한 바이트도 고치지 말 것** ·
    ⚠️ **`a11y-contract.test.ts`에서 이 축 말고는 한 바이트도 고치지 말 것**(그 파일의 다른 축은
    라운드 90 A·92 B·93 C·94 A의 것이다) · ⚠️ **`ExpenseCategoryIconButton`을 건드리지 말 것**
    (오늘 옳고, 그 이름 판정 문턱은 P3다) · **어드민 0건 · `apps/api/**` 0건 · `packages/**` 0건 ·
    `docs/**` 0건**
  - 계약: ⓐ **모집단** — `apps/mobile`의 테스트 밖 전수에서 `accessibilityState={{ … }}` 자리를
    **걷기로** 파생할 것(오늘 **57** — `disabled` 18 · `selected` 17 · `expanded` 10 · `checked` 9 ·
    `busy` 3). ⚠️ **손 목록 금지.**
    ⓑ **판정 셋** — 자리마다 소스에서 하나씩 낼 것: *상태를 상태로만 말한다* ·
    *상태를 라벨에도 적었다*(**오늘 3 → 트랙 뒤 0**을 **상한**으로) ·
    *상태 낱말이 라벨에 없는데 상태 프롭도 없다*(오늘 0). ⚠️⚠️ **어느 판정에도 안 떨어지는 자리가
    0건임을 부정 단언으로 못 박을 것.**
    ⓒ **상태 낱말의 모집단** — `선택됨` 하나가 아니라 **다섯 갈래**(선택 · 비활성 · 체크 · 펼침/접힘 ·
    로딩)를 값으로 지고, ⚠️ **오늘 넷이 0건임을 값으로 적을 것**(0을 적지 않으면 다음 라운드가 이
    축을 *선택 전용*으로 읽는다).
    ⓓ **비선택 가지** — `MonthJumpSheet`·`ExpenseDatePicker`의 `<View accessible>` 가지가
    **상태 프롭을 지니게** 하고, ⚠️⚠️ **선택됐는데 못 고르는 갈래에서 소리를 잃지 않음**을
    순수 함수의 값으로 문을 것(*"선택됐고 못 고른다"* 가 상태 둘로 선다).
    ⓔ **두 표면** — 순수 함수가 내는 문자열과 화면이 세우는 프롭을 **나란히 놓아** 물 것
    (보이는 줄과 낭독이 같은 사실을 **한 번씩** 말한다).
    ⓕ **바이트 불변** — 다섯 자리 말고는 모든 문구·프롭이 바이트 불변임을 **부정 단언**으로 못 박을 것.
    ⓖ **핀 이동(AI-4의 이행)** — 계약 셋의 옛 바이트가 **코드 어디에도 없음**을 부정 단언으로 문고
    (두 시점 주석 속은 예외), **자리 · 옛 바이트 · 오늘의 바이트 셋을 값으로** 적을 것.
    ⚠️⚠️ **그리고 이 걸음이 *통합이 아니라 트랙의 커밋*에 실린다는 사실을 머리말이 값으로 적을 것.**
    ⓗ **래칫** — *상태를 라벨에도 적었다*는 **0을 상한**으로 · 모집단 수(57)는 **줄지 않는다**(하한).
    ⓘ **사각** — 값과 하한으로 **넷 이상**: ⓐ **소스 대조이지 런타임이 아니다** — TalkBack이 실제로
    무엇을 읽는지는 **실기기 항목**이다(그 항목을 F가 세운다) · ⓑ **`accessibilityState`를 아예 쓰지
    않고 라벨로만 상태를 말하는 자리는 이 바늘 밖이다**(오늘 그런 자리의 크기를 값으로 적을 것 —
    ⚠️ **0이면 0을 적을 것**) · ⓒ **상태 낱말의 목록이 관례이지 문법이 아니다**(다른 말로 상태를
    적은 자리를 못 본다 · 오차 방향은 조용한 쪽) · ⓓ **플랫폼이 상태를 읽는다는 전제는 소스가
    아니라 RN·OS의 계약이다** — 그 근거를 자리와 함께 적을 것(`records.tsx:473`의 주석을 **인용**).
    ⚠️ **전제 하한**: 오늘 `accessibilityState` 자리 **57 이상** · 위반 **셋 이하**(0을 상한으로
    내린다) · `선택됨`이 라벨에 든 제품 자리 **셋**.

- **B 어드민 사각의 0 일곱이 *무엇의 0인지*를 말한다** (#2 · AI-3의 답) — **유일한 어드민 트랙**
  - 소유: `apps/admin/src/admin-landmark-current.test.ts`(⚠️ **0 넷 —
    `:469`·`:484`·`:503`·`:550`**) · `apps/admin/src/admin-route-surface.test.ts`(⚠️ **0 셋 —
    `:400`·`:412`·`:443`**)
  - 읽기: `apps/mobile/src/screen-header-back.test.ts`(⚠️⚠️ **`UncountableEvidence`와 `zeroMeans`의
    본보기 — 읽기만 · 바이트 불변** · **그 파일은 어느 트랙의 것도 아니다**) ·
    `apps/admin/app/**/page.tsx` · `apps/admin/src/components/**`(**읽기만 · 0바이트**) ·
    `apps/admin/src/admin-load-error-copy.test.ts`(⚠️ **같은 앱의 0 다섯이 그 파일에 있지만 그 파일은
    라운드 94 트랙 C의 것이다 — 읽기만 · 바이트 불변**)
  - 금지: ⚠️⚠️ **어드민 소스 0바이트**(화면·컴포넌트·`lib` 전부) · ⚠️⚠️ **사각의 `measure`·`today`·
    `floor`를 한 글자도 바꾸지 말 것** — 이 트랙이 더하는 것은 **0의 뜻**이지 수가 아니다 ·
    ⚠️ **`admin-load-error-copy.test.ts`를 고치지 말 것**(그 파일의 0 다섯은 **다음 라운드의 것**이고,
    그 사실을 값으로 적을 것) · ⚠️ **모바일의 본보기를 복사하지 말 것**(인용하되 같은 사실을 각자
    파생) · ⚠️ **렌더 하네스를 세우지 말 것**(결정형 17) ·
    **모바일 0건 · `apps/api/**` 0건 · `packages/**` 0건 · `docs/**` 0건**
  - 계약: ⓐ **모집단** — 이 두 파일의 사각 항목을 **파일 자신의 소스에서** 전수로 파생하고
    그중 `today: 0`인 자리를 가를 것(오늘 **일곱**). ⚠️ **손 목록 금지.**
    ⓑ **판정 셋** — 0인 자리마다 ⓐ **걷어서 낸 0**(자가 오늘 실제로 돌아 0을 낸다) ⓑ **셀 수 없어
    0**(자를 둘 수 없고 증거를 진다) ⓒ **아직 갈래가 없다**(**트랙 뒤 0**을 **상한**으로)를 값으로 낼 것.
    ⓒ **0의 뜻** — 걷어서 낸 0에는 *무엇이 없어서 0인가*를, 셀 수 없는 0에는 *왜 소스에 없는가*를
    한 문장씩 지게 할 것. ⚠️⚠️ **그리고 이름이 겹치는 자리를 값으로 집을 것** —
    `source-not-runtime`이 이 두 파일과 모바일에 **셋** 있고 **성질이 갈린다**(어드민 둘은 걷어서
    낸 0 · 모바일 하나는 셀 수 없어 0). **같은 이름을 성격으로 읽지 말라는 문장을 값으로 남길 것.**
    ⓓ **유령 방지** — 0이라고 적힌 자리의 `measure`가 **오늘 실제로 불려 0을 낸다**는 것을 값으로
    보일 것(상수를 감싼 함수가 0건임을 부정 단언으로).
    ⓔ **두 시점** — 옛 값을 지우지 말고 *언제 무엇이 참이었는지*로 적을 것(AE-3).
    ⓕ **래칫** — 0인 자리의 수는 **각자 자기 방향** · *갈래가 없다*는 **0을 상한**으로.
    ⓖ **사각** — 값과 하한으로 **넷 이상**: ⓐ **저장소의 나머지 0 여덟은 이 트랙 밖이다**
    (`admin-load-error-copy.test.ts` 다섯 · `screen-header-back.test.ts` 하나 ·
    `dead-export-ledger.ts` 둘 — **그 수를 값으로 적을 것**) · ⓑ **0이 아닌 크기의 갈래는 묻지
    않는다** · ⓒ **소스 대조이지 브라우저가 아니다**(#130 계열) · ⓓ **0의 뜻이 옳은지는 사람의
    문장이고 이 자가 묻는 것은 *적혀 있는가* 하나다**(AI-5의 앞쪽/뒤쪽과 같은 갈림).
    ⚠️ **전제 하한**: 이 두 파일의 사각 자리 **열 이상** · 그중 `today: 0` **일곱** ·
    `zeroMeans` 낱말이 이 두 파일에 **오늘 0건**.

- **C 재개 조건 대장의 사각에서 *적힌 값*과 *오늘의 자*가 갈리는 것을 세는 자가 선다** (#3) —
  **유일한 대장 트랙 ①**
  - 소유: `packages/test-utils/src/resume-condition-ledger.ts`(⚠️⚠️ **사각의 *기록 축* 하나 —
    이 파일에서 이 트랙이 여는 축은 그것 하나다**) ·
    `packages/test-utils/src/resume-condition-ledger.test.ts`
  - 읽기: `packages/test-utils/src/dead-export-ledger.ts`·`dead-export-ledger.test.ts`
    (⚠️⚠️ **여섯 자리를 등호로 문 본보기 — 읽기만 · 바이트 불변**) ·
    `packages/test-utils/src/comment-tolerant-anchor-ledger.ts`(⚠️ **자가 하나도 없는 대조군 —
    읽기만 · 바이트 불변**) · `docs/operations/known-limitations.md`(**읽기만 · 0바이트**)
  - 금지: ⚠️⚠️ **소스 축·문서 축·경과 축의 *바늘*을 한 글자도 바꾸지 말 것** — 이 트랙이 여는 것은
    **사각이 자기 값을 어떻게 기록하는가**이지 그 값을 내는 자가 아니다(*한 트랙이 한 그물에 축 둘을
    얹지 않는다*) · ⚠️⚠️ **등호를 세우지 말 것** — 이 수들은 라운드마다 자라므로 등호는 F가 정직하게
    문서를 쓰는 걸음마다 빨개진다(**빨간 계약은 아무것도 지키지 못한다**) ·
    ⚠️ **사각 열하나의 이름·서술·재개 조건을 지우지 말 것**(옛 값은 **두 시점**으로) ·
    ⚠️⚠️ **문서를 한 바이트도 고치지 말 것**(F의 것이다) ·
    **모바일 0건 · 어드민 0건 · `apps/api/**` 0건**
  - 계약: ⓐ **모집단** — 이 대장의 사각 전수(`LEDGER_BLIND_SPOTS` 여덟 + `ELAPSED_BLIND_SPOTS` 셋 =
    **열하나**)를 **자기 소스에서** 파생할 것. ⚠️ **손 목록 금지.**
    ⓑ **판정 셋** — 자리마다 ⓐ **자가 있는가** ⓑ **적힌 값이 있는가** ⓒ **그 둘이 오늘 같은가**를
    값으로 낼 것(오늘 **자 열하나 · 적힌 값 열하나 · 같은 것 넷 · 갈린 것 일곱**).
    ⓒ **갈림의 크기** — 갈린 자리마다 **적힌 값 · 오늘의 자 · 차이**를 값으로 적을 것
    (prose-only +62 · folded-notation +6 · round-notes +63 · sibling-documents +3 ·
    one-line-two-sites +7 · elapsed-truth +32 · elapsed-outside-population +8).
    ⚠️⚠️ **차이가 *음수*인 자리가 0건임을 부정 단언으로 못 박을 것** — 이 사각들은 자라는 쪽이고,
    줄어드는 날은 이름이 틀렸던 날이다(라운드 94 D의 `unmarked-source-prose` 129 → 61이 그 실물이다).
    ⓓ **래칫** — *갈린 자리의 수*는 **줄어야 좋은 수라 하한이 아니다** — **값으로만** 적고
    ⚠️ **적힌 값 ≤ 오늘의 자**를 **모든 자리에서** 문을 것(오르는 쪽 래칫).
    ⓔ **본보기의 인용** — 사문 대장이 같은 자리를 **등호로** 물어 갈림이 **0**이라는 사실을 값으로
    들 것(⚠️ **로직을 복사하지 말고 그 대장을 읽어 파생할 것**). ⚠️⚠️ **그리고 *왜 이 대장은 등호를
    고르지 않는가*(수가 라운드마다 자란다)를 값으로 적을 것 — 고르지 않은 길도 값이다.**
    ⓕ **두 시점** — 적힌 값을 **지우지 말고** 갱신하되 옛 값을 남길 것(AE-3).
    ⓖ **사각** — 값과 하한으로 **넷 이상**: ⓐ **자가 없는 대장은 이 자 밖이다**
    (`comment-tolerant-anchor-ledger.ts`의 사각 **일곱**에 `measure`가 0건이라 *갈림*이라는 질문이
    성립하지 않는다 — **그 수를 값으로 적을 것**) · ⓑ **이 자는 *갈렸다*까지만 보고 *어느 쪽이 옳은가*를
    묻지 않는다** · ⓒ **적힌 값이 사각의 성격까지 낡았는지는 못 본다**(AH-3의 병) ·
    ⓓ **이 대장 하나만 본다** — 저장소의 다른 대장으로 넓히는 것은 **저장소 전체**라 이 라운드의
    범위가 아니다(AI-2 · **재개 조건과 함께 적을 것**).
    ⚠️ **전제 하한**: 사각 **열하나 이상** · 자를 지닌 자리 **열하나** · 갈린 자리 **일곱 이하**
    (0을 향한다) · `dead-export-ledger`의 갈림 **0**.

- **D 공유 테스트 DB의 누적을 *만드는 손*을 세는 자가 선다** (#4 · 결정 #14의 모집단) —
  **유일한 api 트랙 · `apps/api/**` 접점 0건이 다섯 라운드 만에 깨진다**
  - 소유: `apps/api/test/harness-catalog-cost.test.ts`(⚠️⚠️ **계약 ⓔ(*누적의 값*)의 비대칭 하나 —
    이 파일에서 이 트랙이 여는 축은 그것 하나다**)
  - 읽기: `apps/api/test/**` 전수(**읽기만 · 0바이트**) · `apps/api/test/helpers/test-db.ts` ·
    `apps/api/test/global-setup.ts` · `apps/api/test/helpers/shared-db-lock.ts`
    (**셋업 폐포 — 읽기만 · 바이트 불변**)
  - 금지: ⚠️⚠️ **api 소스 0바이트**(`apps/api/src/**`) · ⚠️⚠️ **테스트를 고치지 말 것** —
    이 트랙은 `apps/api/test/**`를 **읽기만** 한다(정리 걸음을 *더하는 것*은 결정 #14이고 이 트랙의
    축이 아니다) · ⚠️ **DB에 붙지 말 것** — 이 계약은 **소스 대조**이고 PostgreSQL 없이 초록이어야
    한다(그 파일의 관례) · ⚠️ **`SCAN_ROOT_COUNT`·모집단 바늘(`tab=now`)·`SETUP_CLOSURE`를 건드리지
    말 것**(라운드 91 C와 94 리뷰의 것이다) · ⚠️ **새 그물을 세우지 말 것**(열다섯 그대로) ·
    **모바일 0건 · 어드민 0건 · `packages/**` 0건 · `docs/**` 0건**
  - 계약: ⓐ **모집단** — `apps/api/test/**`를 **걷기로** 파생해 `itemTemplate.create(`/
    `createMany(` 자리를 전수로 낼 것(오늘 **17 · 파일 10**). ⚠️ **손 목록 금지 · 자기 배제**
    (이 파일이 바늘을 값으로 적으므로 조각으로 이을 것 — 그 파일의 오늘 관례를 인용).
    ⓑ **판정 둘** — 자리마다 *같은 파일에 정리 걸음(`itemTemplate.delete`/`deleteMany`)이 있다*
    (오늘 **넷**) / *없다*(오늘 **열셋**)를 소스에서 낼 것.
    ⚠️⚠️ **판정에서 파생하지 않는 자리가 0건임을 부정 단언으로 못 박을 것.**
    ⓒ **비대칭의 값** — 계약 ⓔ가 **지우는 쪽 0건**을 부정 단언으로 물고 있다는 사실과, 오늘
    **만드는 쪽 열일곱**이 세어졌다는 사실을 **나란히** 놓을 것. ⚠️⚠️ **그리고 *누가 올렸는지를
    세는 자가 오늘 처음 선다*는 문장을 값으로 남길 것**(AI절 #14가 여섯 라운드 기다린 그 자리다).
    ⓓ **래칫** — *정리 없이 만드는 자리*는 **늘지 않는다**(오늘 열셋을 **상한**으로) ·
    모집단 열일곱은 **줄지 않는다**(하한).
    ⓔ **결정은 열어 둘 것** — ⚠️⚠️ **이 트랙은 결정 #14를 집지 않는다.** *어느 표를 언제 비울
    것인가*는 그대로 두고, ⚠️ **재개 조건(결정형 · 손은 저장소 안)을 이 자리에 다시 적을 것**:
    *정리 걸음을 관례로 세우는 날 — 그날 첫 모집단은 오늘의 열셋이다.*
    ⓕ **사각** — 값과 하한으로 **넷 이상**: ⓐ **`itemTemplate` 밖의 표는 이 바늘 밖이다**
    (라운드 91 C가 적어 둔 여섯 표 가운데 하나만 본다 — **그 사실을 값으로**) ·
    ⓑ **시드(`prisma/seed.ts`)가 만드는 행은 이 바늘 밖이다** — 그쪽은 멱등이라 누적이 아니다 ·
    ⓒ **정리가 *같은 파일*에 있는지만 본다** — 다른 파일이나 훅이 지우면 이 자가 못 본다(오차
    방향은 **거짓 빨강 = 안전**) · ⓓ **소스 대조이지 DB가 아니다** — 오늘의 9,545는 **정찰이 손으로
    잰 수**이고 이 계약이 내는 수가 아니다(**두 수를 한 낱말로 적지 않는다**).
    ⚠️ **전제 하한**: `itemTemplate.create` 자리 **열일곱 이상** · 정리 걸음 **넷** ·
    api 스위트를 **돌리지 않는다**(이 계약은 DB를 쓰지 않는다).

- **E 판정 문서의 바늘을 *낱말 하나*와 *문장*으로 가르는 자가 선다** (#5 · AI-1의 답) —
  **유일한 대장 트랙 ②**
  - 소유: `packages/test-utils/src/repo-self-description.test.ts`(⚠️⚠️ **재실측 표 축의 판정
    하나 추가 — 이 파일에서 이 트랙이 여는 축은 그것 하나다** · ⚠️ **라운드 93·94 E가 세운 좌표
    축과 기존 다섯은 바이트 불변**)
  - 읽기: `docs/operations/known-limitations.md`(**읽기만 · 0바이트**) ·
    `docs/5차/round94-scout.md`·`round95-scout.md`(⚠️ **정찰 노트의 바늘 선언이 같은 갈래의 실물 —
    읽기만 · 바이트 불변**)
  - 금지: ⚠️⚠️ **문서를 한 바이트도 고치지 말 것** — 갈래를 붙이는 손은 **F**다(옮기는 손과 세는
    손이 반대라는 AG-5의 판정) · ⚠️⚠️ **등호로 묻지 말 것** — 행 수도 갈래 수도 **하한**이다 ·
    ⚠️ **바늘 판정 넷과 다섯째(명시적 무바늘)의 정규식 바이트를 한 글자도 넓히거나 좁히지 말 것**
    (라운드 94 E와 리뷰 M-4의 것이다 — **새 판정은 그 옆에 여섯째로 선다**) ·
    ⚠️ **좌표 축(`COORDINATE_LIVE_DOCS` 계열)의 손 목록을 건드리지 말 것**(결정형 23) ·
    ⚠️ **새 그물을 세우지 말 것**(열다섯 그대로) · ⚠️ **AI절 말고 K~AH절의 옛 표를 모집단에 넣지
    말 것** · **모바일 0건 · 어드민 0건 · `apps/api/**` 0건**
  - 계약: ⓐ **모집단** — 마지막 판정 절의 재실측 표 행 가운데 **바늘이 적힌 행**(오늘 **39**)을
    파생할 것. ⚠️ **절 이름을 손으로 적지 말 것**(오늘의 관례 그대로) · ⚠️ **표를 못 찾으면 0이
    아니라 빨개질 것**(유령 방지).
    ⓑ **여섯째 판정** — 행마다 그 바늘이 ⓐ **낱말 하나**(백틱에 싸인 식별자 하나 또는 좌표꼴 하나로
    끝난다) ⓑ **문장**(조건을 잇는 낱말이 함께 든다)인지를 값으로 낼 것.
    ⚠️⚠️ **좌표꼴 아홉이 낱말 쪽에 든다는 것을 값으로 보일 것**(두 판정이 서로 모순이 아님을
    이 자가 스스로 확인한다).
    ⓒ **붐비는 칸과의 대조** — ⚠️⚠️ **한 칸에 수가 여덟 넘게 든 행 18에서 *바늘이 하나*라는 사실을
    값으로 적을 것** — 그 행들은 바늘이 적혀 있어도 **어느 수에 어느 바늘인지**가 없다.
    (오늘 붐비는 칸 **18** · 그중 바늘이 적힌 행 **18** · 바늘이 여럿인 행 **오늘 세어 값으로**).
    ⓓ **래칫** — 행 **40** · 바늘 **39** · 좌표꼴 **9** · 낱말 하나 갈래 **오늘의 값을 하한으로** ·
    ⚠️⚠️ **하한을 고른 비용을 그 자리에 적을 것**(행이 사라지거나 갈래가 뒤집히는 것은 이 자가 못 본다).
    ⓔ **문서 0바이트** — 이 트랙이 문서를 **읽기만** 했음을 값으로 적을 것(부정 단언).
    ⓕ **사각** — 값과 하한으로 **넷 이상**: ⓐ ⚠️⚠️ **갈래가 *낱말 하나*라고 그 수가 재현되는 것은
    아니다** — 이 자는 **꼴만** 보고, 재현은 라운드마다 정찰의 걸음이 답한다(오늘 정찰의 표본 여섯이
    그 대리 지표를 지지했으나 **여섯은 표본이지 증명이 아니다**) · ⓑ **낱말/문장의 경계가 관례이지
    문법이 아니다** — 오늘의 정규식이 못 가르는 자리를 값으로 적을 것 · ⓒ **K~AH절의 옛 표는
    밖이다**(행 129) · ⓓ **정찰 노트의 바늘은 이 표 밖이다** — 같은 갈래가 노트에도 사는데
    (오늘 아홉 가운데 둘/일곱) 그것을 세는 자는 **0건**이다(⚠️ **재개 조건(결정형 · 손은 저장소 안):
    정찰 노트가 자기 바늘의 갈래를 값으로 적는 관례가 서는 날**).
    ⚠️ **전제 하한**: 재실측 표 행 **40 이상** · 바늘이 적힌 행 **39 이상** · 좌표꼴 **9 이상** ·
    붐비는 칸 **18 이상**.

- **F 판정 문서** (GAP-095F)
  - 소유: `docs/operations/known-limitations.md`(**AJ절 신설** — 판정 다섯 + AI의 답 다섯 +
    AJ-1~AJ-5의 *"다음 라운드가 먼저 세어 볼 만한 것"* + **결정형 재개 조건 통합표**
    ⚠️ **스물셋과 새로 선 셋을 한 표로 합쳐 스물여섯으로 세우고, 각 행에 *닫히는 범위*(한 파일 ·
    한 앱 · 저장소 전체) 칸을 더할 것** — AI-2의 답이 표의 꼴이 된다) ·
    `docs/qa/runtime-verification-required.md`(**#172~ 신설 행** — 후보 1의 세 자리를 실기기
    TalkBack·VoiceOver가 **한 번만** 읽는지) ·
    `docs/qa/accessibility-offline-checklist.md`(**#172~ 신설 행** · C-3 **스물아홉 라운드**) ·
    `docs/5차/launch-readiness-status.md`(총 테스트·PR 갱신)
  - 읽기: 다섯 트랙의 소유 파일 전부(**읽기만**)
  - 금지: ⚠️⚠️ **소스 0바이트**(F는 문서만 쓴다) · ⚠️ **판정을 두 자리에 옮겨 적지 말 것**
    (O-3·X-4) · ⚠️ **AI절 본문의 옛 수를 지우지 말 것**(두 시점으로) ·
    ⚠️⚠️ **재실측 표의 붐비는 칸에 바늘을 *여럿* 적기 시작할지는 F의 결정이고, 고른 길과 고르지
    않은 길을 둘 다 값으로 적을 것**(결정형 22의 나머지 절반이다)
  - 해야 할 일(값): ⓐ ⚠️⚠️ **AI-5의 숙제가 끝났다는 사실과 그 비용을 함께 적을 것** — 수만 있는
    행이 24 → **0**이 됐고 좌표꼴이 2 → **9**가 됐는데 **붐비는 칸이 2 → 18**이 됐다.
    **한 칸에 수가 여덟인데 바늘이 하나면 그 행은 여전히 풀 수 없다.**
    ⓑ ⚠️⚠️ **AI-1의 일반형이 표본 여섯으로 다시 확인됐다는 사실을 판정으로 적을 것** — 낱말/정규식
    하나였던 셋이 셋 다 정확히 재현됐고 문장이었던 셋이 셋 다 갈렸다.
    ⓒ **결정형 표에 *닫히는 범위* 칸을 더할 것**(8 · 8 · 7)과, **집힌 조건이 전부 한 파일이었다**는
    상관을 값으로 적을 것.
    ⓓ **공유 DB #14의 일곱째 시점**(9,545 · 8,888 · 753MB · **+69**)과 ⚠️⚠️ **오늘 처음 *만드는
    손*이 세어졌다는 사실**(17 · 정리 넷 · 정리 없는 열셋)을 함께 적을 것.
    ⓔ **C-3 스물아홉 라운드**와 짝 문서 셋의 정합을 함께 올릴 것.

---

> ⚠️ **이 노트의 수는 전부 하한이다** — 각 트랙은 자기 축의 전제를 **다시 재고**, 정찰과 갈리면
> **갈렸다는 사실 자체를 값으로** 적는다. ⚠️⚠️ **그리고 이 노트가 라운드 94보다 하나 더 지는 것이
> 있다 — 정찰이 낸 수의 *바늘 꼴*을 미리 적었다.** 위 「답 1」의 재현 표가 그것이고, 이 노트가 낸
> 수 가운데 **문장 바늘로 낸 것**(손 목록 56/26 · 사각 62/14 · 계약 쌍 602 · 자기 배제 17)은
> **다음 라운드가 다시 대면 갈릴 수 있다는 사실을 여기서 미리 값으로 적어 둔다.**
> ⚠️ **그리고 이 노트 자신이 재개 조건 대장의 사각 `round-notes`를 한 벌만큼 키운다**(53 → 54벌) —
> 정찰을 쓰는 걸음이 곧 그 사각을 키우는 걸음이라는 사실도 여기서 미리 적어 둔다.
