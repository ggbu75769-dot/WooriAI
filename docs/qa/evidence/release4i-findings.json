{
  "schemaVersion": 1,
  "release": "4I",
  "generatedAt": "2026-07-17T08:30:50.337Z",
  "summary": { "fixedP0": 1, "fixedP1": 4, "fixedP2": 2, "partial": 1, "openCodeP0": 0, "openCodeP1": 0 },
  "findings": [
    { "id": "R4I-P0-001", "severity": "P0", "actor": "unauthorized production caller", "input": "legacy x-admin-token path", "mission": "reach protected Admin mutation", "expected": "production rejects legacy token authentication", "actual": "guard had no explicit production rejection and returned a synthetic non-database identity", "rootCause": "development compatibility path crossed the production boundary", "fix": "reject outside development/test and resolve a deterministic persisted UUID operator only after token validation", "regressionTest": "apps/api/test/admin-token.guard.test.ts (5 tests)", "residualRisk": "none in code; approved production identity remains external" },
    { "id": "R4I-P1-002", "severity": "P1", "actor": "notification recipient", "input": "worker exits after claim", "mission": "receive the event once", "expected": "expired claim is recovered", "actual": "automatic abnormal-exit replacement and claim recovery were not qualified", "rootCause": "lease ownership and supervision diagnostics were incomplete", "fix": "DB-clock leases, worker identity, heartbeat/restart diagnostics, local-only crash failpoint and compose restart policy", "regressionTest": "release4i-worker-failpoint.test.ts plus local worker exit 86", "residualRisk": "external orchestrator policy" },
    { "id": "R4I-P1-003", "severity": "P1", "actor": "notification recipient", "input": "provider success then publisher crash", "mission": "avoid duplicate delivery", "expected": "unknown result reconciles using the same key", "actual": "post-provider pre-ack boundary lacked a persistent reconciliation state", "rootCause": "remote side effect cannot share the DB transaction", "fix": "persistent attempt, stable provider idempotency key, unknown reconciliation and bounded retries", "regressionTest": "50 deterministic ack-loss iterations plus local exit 87", "residualRisk": "real provider lookup unavailable" },
    { "id": "R4I-P1-004", "severity": "P1", "actor": "config operator", "input": "two APIs, rollout, Redis restart and rollback", "mission": "maintain one security configuration", "expected": "monotonic converged DB version", "actual": "revision history, CAS rollback and instance convergence were incomplete", "rootCause": "cache state was not backed by an explicit versioned activation contract", "fix": "RemoteConfigRevision, strict validation, CAS activation, rollback-as-new-version, heartbeats and diagnostics", "regressionTest": "release3-app-config.e2e.test.ts and Admin operations browser", "residualRisk": "external staging" },
    { "id": "R4I-P1-005", "severity": "P1", "actor": "import operator", "input": "MinIO and DB boundary failures", "mission": "avoid false applied state", "expected": "object identity and job state reconcile", "actual": "object/job hash identity and orphan/missing recovery were incomplete", "rootCause": "upload and DB state are separate transactions", "fix": "deterministic object key/hash, explicit states, apply CAS, dry-run reconciliation and active-object protection", "regressionTest": "release4i-import-object-consistency.db.test.ts and catalog admin E2E", "residualRisk": "external object store" },
    { "id": "R4I-P2-006", "severity": "P2", "actor": "operations operator", "input": "degraded service and queues", "mission": "understand and recover the fault", "expected": "role-protected summaries and bounded actions", "actual": "status required raw infrastructure access", "rootCause": "no consolidated operations contract", "fix": "redacted operations API and Admin page with confirm, single-flight and scoped refresh", "regressionTest": "admin-operations.browser.test.ts (2 tests)", "residualRisk": "real operator UX session" },
    { "id": "R4I-P2-007", "severity": "P2", "actor": "release engineer", "input": "new S3 SDK dependency", "mission": "pass supply-chain age policy", "expected": "no package younger than policy cutoff", "actual": "latest AWS/Smithy packages were published within 24 hours", "rootCause": "transitive caret ranges selected freshly published packages", "fix": "compatible exact S3 version and eight minimum-age-safe transitive overrides", "regressionTest": "frozen strict-peer install and release gate", "residualRisk": "review overrides on the next dependency upgrade" }
  ]
}
