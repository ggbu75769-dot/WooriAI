# Android 직접 써 보기 — 2026-07-30

## 좌표

- 기기: `sdk_gphone64_x86_64`, Android 15, 1080×2340
- 앱: `com.anonymous.wooriai`
- 프로필: standalone, embedded JS, test login
- 최초 지출 과업 APK SHA-256: `65A8812A7CE10E97F19F3A5FA9ED0161DAF9A0A5FD3F37FEA23EA6DDDA1B0C15`
- 키보드 수정 후 APK SHA-256: `6C4ABDE6DA0FD822B5C18D896A7425308275488ABD3A7D54AA3982A851057BBB`
- 수정 후 source snapshot SHA-256: `D6D6F3D363BC8F00570A2212CD1969B25C7821D4E3143D6B894B44060B4EE1F8`
- 두 실행 모두 built APK와 installed `base.apk` SHA-256 일치.

## 핵심 과업

처음 온 보호자가 동의·아이 설정을 마친 뒤 지출을 만들고, 날짜와 금액을 수정하고, 기록과 홈 합계 반영을 확인한다.

| 단계 | 하려던 일 | 제품이 말한 것 | 망설임·막힘·되돌림 | 기대 | 실제 결과 |
| --- | --- | --- | --- | --- | --- |
| 1 | 필수 동의 후 시작 | 이용약관·개인정보 필수 동의와 `동의하고 시작하기` | 없음 | 동의 전 CTA 비활성 | 두 항목 동의 후 CTA 활성 |
| 2 | 태어난 아이 정보 입력 | 상황 선택 → 이름·생일·성별 → 준비물 → 예산 → 검토 | 이름 입력 뒤 생일 달력을 닫아도 키보드가 남아 성별과 CTA를 가림; 수동으로 키보드를 닫고 스크롤함 | 날짜 선택 후 다음 필드가 보여야 함 | `WALK-20260730-01` 재현 |
| 3 | 지출 12,000원 생성 | 홈 `지출 기록` → 오늘·기저귀·금액 → 저장 | 없음 | 저장 후 기록에서 확인 | 기록 탭으로 이동, 기저귀 12,000원·동기화 완료 표시 |
| 4 | 지출 날짜·금액 수정 | 기록 행 → EXP-003 → 날짜 변경 → 달력 → 수정 저장 | 텍스트 날짜 입력 없음; 네이티브 Android 달력 정상 노출 | 7월 29일·15,000원으로 갱신 | 기록 목록에 15,000원 반영 |
| 5 | 홈 합계 확인 | 홈 월 지출 카드와 최근 지출 | 없음 | 월 합계와 최근 지출이 같은 금액 | 월 합계 15,000원, 예산 500,000원의 3%, 최근 지출 15,000원 |
| 6 | 키보드 가림 수정 재검증 | 아이 이름 입력 → 생일 달력 선택 | 되돌림 없음 | 달력 종료 시 키보드 닫힘 | content bounds `[0,136][1080,2274]`; 성별 3개와 `다음` CTA 모두 UI tree에 노출 |

지출 생성 시작부터 수정·홈 반영 캡처까지 약 167초가 걸렸다. 이 시간에는 각 단계의 adb dump와 screencap 대기가 포함된다.

## 발견·조치

### WALK-20260730-01 — CLOSED

- 재현: 아이 이름 TextInput에 포커스한 채 생일 DateField를 열고 날짜를 확정한다.
- 수정 전: Android 날짜 picker가 닫힌 뒤 소프트 키보드가 유지되어 성별과 하단 CTA를 가렸다.
- 원인: 공유 `DateField.openPicker`가 현재 TextInput 포커스/키보드를 정리하지 않았다.
- 수정: `Keyboard.dismiss()`를 native picker 실행 전에 호출.
- 재발 방지: `design-system-direct-render.test.tsx`에서 DateField press 시 keyboard dismiss 1회와 Android picker open 1회를 함께 검증.
- 커밋: `6a3f4a0`.

### WALK-20260730-02 — OPEN

- 재현: 기저귀 지출을 열어 EXP-003 진입.
- 관측: 현재 선택 카테고리 `기저귀·위생` 칩이 horizontal viewport 오른쪽 바깥에 있어 첫 화면에서 선택 상태가 보이지 않는다. UI dump bounds는 `[1035,1215][978,1319]`였다.
- 영향: 사용자가 현재 분류를 확인하려면 이유를 모른 채 가로 스크롤해야 한다.
- 다음: 선택된 카테고리를 진입 시 자동 reveal하고, 회귀 렌더/Android 증거를 추가한다.

## 증거

- `artifacts/walkthrough/2026-07-30/02-expense-saved.png`
- `artifacts/walkthrough/2026-07-30/03-expense-edit.png`
- `artifacts/walkthrough/2026-07-30/04-native-date-picker.png`
- `artifacts/walkthrough/2026-07-30/05-expense-updated.png`
- `artifacts/walkthrough/2026-07-30/06-home-total.png`
- `artifacts/walkthrough/2026-07-30/07-onboarding-keyboard-dismissed.png`
- UI tree: 같은 폴더의 `16-window.xml`부터 `26-window.xml`.
- fatal 확인: `adb logcat -d -t 500`에서 `FATAL EXCEPTION`, React Native `TypeError/ReferenceError`, AndroidRuntime fatal 0건.

## 실행 명령

- `pnpm android:build-apk`
- `pnpm android:build-apk -- --resume-after-clean`
- `adb install -r F:\WooriAI\wooriai-0.0.0-release-standalone.apk`
- `adb shell screencap -p /sdcard/<name>.png` + `adb pull`
- `adb shell uiautomator dump /sdcard/window.xml` + `adb pull`
- `adb shell sha256sum <pm path>`
- `pnpm release:gate`

이 증거는 내부 standalone Android 과업을 증명한다. 프로덕션 배포·실 OAuth·production signing·물리기기·iOS 증거는 아니다.
