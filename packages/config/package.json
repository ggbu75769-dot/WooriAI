{
  "name": "@wooriai/config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "//": "R20-D: 스텁 패키지. src/index.ts에 플레이스홀더 export 한 줄뿐이고 어떤 워크스페이스도 이 패키지를 의존하지 않는다(실제 설정은 각 앱의 env·스크립트에 있다). 검증할 동작이 없어 test 스크립트는 '테스트 없음'을 그대로 출력한다 — 종전의 'tests deferred to Batch 01 hardening' 문구는 실행되지도 않은 테스트를 통과처럼 보이게 했다. 실제 설정 코드가 들어오면 이 스크립트를 'vitest run'으로 바꿀 것(docs/5차/round5b-feature-review-and-sellable-design.md 참고).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "echo \"no tests (stub package: @wooriai/config)\""
  }
}
